# Changelog

## 2026-09-24

- **RC-001 complete (inventory only):** Inspected the adjacent macOS app checkout at revision `5515958f7e6cbb46bf33c16c3ff91df7e4dab021` and mapped Workspaces, Git, terminal/SSH, agent, Bots, scheduled workspace tasks and Bot routines, notifications, account/settings, and desktop/web journeys to backlog IDs and observable proofs in `plan/parity-inventory.md`. Independent review found workspace scheduled agent tasks were not covered by the Bot-routine-only backlog item, so RC-045's title, scope, and executable proof now explicitly cover both schedule types, and RC-051's UI scope/proof now requires creating a workspace scheduled task and comparing both task and routine state with the API. Token Saver, generic browser/preview, previous harnesses, and WatchOS/Live Activities (no target client in the product plan) are explicitly excluded; Bot computer sessions remain in scope. The app source is outside this planning repository and was not copied. This inventory does not accept any implementation task.
- **RC-002 remains blocked:** The original offline probe found no cached Linux Distill runtime; network was disabled at that time. After explicit user authorization of network use, downloaded the official v2.0.14 `distill-linux-aarch64` release binary and verified its SHA-256 (`abf7e29c56c164476ea81ef6cc6a1b056620b154839c03de3db3ae9a66bb6864`). The ELF binary's dynamic libraries resolved in the cached aarch64 Linux container, but running `--version` terminated with `Illegal instruction` (exit 132), so it is not usable in this guest. A follow-up x86_64 run could not be performed: the local Docker context has no amd64 variant of the guest image, and the ARM64 guest lacks the x86_64 dynamic loader (`/lib64/ld-linux-x86-64.so.2`). No emulation-compatible x86_64 guest is available in the current Docker context. No model credentials were inspected or model request made; authorized model test access and the file creation/interruption/restart/reconciliation lifecycle remain unverified.
- **RC-003 blocked after partial real Linux proof:** In the cached Linux image, the actual `linux-use` MCP stdio server ran under Xvfb; `doctor` confirmed Linux/X11, but `list_windows` failed safely because `wmctrl` is unavailable. Capture/input remain explicitly unsupported. This planning checkout has no Elysia/web app or container build source, and its cached image has no Linux Distill runtime, so the complete UI/API/remote-browser journey was not run. Probe output is in the goal scratch directory.
- **Evidence handoff:** Added `plan/checkpoint-evidence.md` with reproducible environment versions, exact observed Linux MCP results, completion boundaries, and remaining proof requirements for RC-001–RC-003.
- **Planning-page search fix and browser proof:** The owned Chrome background-tab check found that searching `RC-002` returned zero tasks. Updated the existing search to include each task element's ID; repeated the same interaction and confirmed exactly one result, `RC-002 — Prove main Distill on Linux`. The rendered summary still showed `1 completed (inventory only); 2 blocked`, phase 00 showed four tasks, and the parity-inventory link returned HTTP 200. The browser integration accepts HTTP(S) URLs only, so direct `file://` navigation was not tested.
- **Backlog dependency gate:** Parsed all 67 task cards in `plan/tasks.html` and extracted their dependencies and statuses. RC-001 is inventory-only; RC-002 and RC-003 are blocked; no remaining `To do` task has all prerequisites accepted. RC-004 is the first dependent task but cannot begin until RC-002 is accepted. The durable evidence note records the exact resume conditions; this planning-only checkout has no executable feature task to advance now.
- **Authority update:** After explicit user confirmation through the two authority questions, changed `.sam-harness/config.yaml` and `.sam-harness/DELEGATION.md` to set `network: true`, `commit: true`, and `push: true`. The change followed the network authorization. The Linux Distill v2.0.14 binary has since been downloaded and checksum-verified in temporary test-artifact storage, but cannot run in the available arm64 guest (Illegal instruction). No commit or push has yet been made; this checkpoint requires independent review and a decision about whether the remaining blocked state is ready to preserve.
- **Checkpoint authority:** No verified product milestone exists for an X post.
