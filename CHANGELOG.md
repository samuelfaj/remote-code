# Changelog

## 2026-09-26

- **RC-016 source checkpoint published and CI passed:** Commit `14f8ab0267cbba26871e117bc9d779ea1698c2e2` was confirmed as origin and open PR #2 head. Runs `36266983397` and `36266981720` matched the exact SHA and each passed `static` and `test` (four successful jobs). This confirms configured repository gates, not native/installed-client or hosted/self-managed deployment behavior. RC-016 remains unaccepted; RC-014 remains partial, RC-002 unknown/unaccepted, and RC-011 `REVISE`. Exact receipt: `plan/checkpoint-evidence.md#rc-016-publication-and-ci-receipt--2026-09-26`.
- **RC-016 compatibility implementation — partial, not accepted:** Added API/client version policy and capability reporting, legacy/current client headers, server-side input validation, HTTP 426 rejection before persistence, and WebSocket 4406 rejection before snapshot. A frozen prior browser client and the current browser client exercised the live snapshot/event interface, writes, and backend readback; incompatible-version cases proved no durable write and actionable UI guidance, including initial session validation and reconnect. Reviewer `01a0df28-087e-7513-bef0-9077fa44d953` found and prompted correction of missing/misleading 426 guidance in those session paths. A subsequent review identified an incomplete diff artifact, which was rebuilt with all new source/fixture files; reviewer `01a0df31-5c1a-74d0-bf38-4283d98c3a29` then found that “update the client” cannot help a client newer than the host. API/UI now say to update the host or use a supported client version. Focused API checks passed (27 tests/171 assertions), and focused browser compatibility checks passed (3). Full post-correction validation passed: API/client 41 tests/217 assertions, typecheck, Vite build, browser E2E 15 passed/3 environment-dependent skips, docs links, and whitespace; output is in the goal scratch file `rc016-review-correction-final.log`. Exact evidence: `plan/checkpoint-evidence.md#rc-016-compatibility-implementation-checkpoint--partial-not-accepted--2026-09-26`. The complete corrected patch received clean exact-diff reviews from `01a0df37-f502-7c41-b08a-c9f76ecf949c` and follow-up `01a0df3c-0990-7542-b181-e2416449d462`, which confirmed the date correction; review scope and limits are recorded in `plan/checkpoint-evidence.md`. RC-016 remains unaccepted pending publication and still-unverified native/installed client and actual hosted/self-managed deployments. RC-014 remains partial, RC-002 unknown/unaccepted, RC-011 `REVISE`. No X post.
- **RC-016 sequencing pivot reviewed:** Removed RC-014 task acceptance as a prerequisite for version compatibility because the version/capabilities and server validation slice is separable from supervised runs. Preserved RC-014 as partial/unaccepted and retained its run-disconnect criterion. The accepted proof for RC-016 must still run actual previous/current clients through the event/snapshot path they use, and verify actionable rejection before durable writes. Reasoning review approved the dependency removal with this necessary proof qualification; implementation and acceptance remain open. Details: `plan/tasks.html#rc-016`, `PIVOT.md#pivot-record--rc-016-dependency-pivot--2026-09-26`, and `plan/checkpoint-evidence.md#rc-016-dependency-pivot--reviewed-planning-change--2026-09-26`.

- **RC-012 route-scope definition reviewed:** Selected a concrete route-family, feature-owner, ownership-chain, and per-route proof matrix without changing RC-012 criteria or creating placeholder endpoints. Reasoning review found three initial scope defects (missing RC-021/RC-024 route owners and a universal 404 oracle that did not fit all route shapes); corrections were independently re-reviewed with no remaining concrete finding. Full handoff review caught this entry under the wrong date heading; it now appears under 2026-09-26. This is planning-only evidence; RC-012 remains partial and no route task is newly eligible. Details: `plan/rc012-resource-ownership-contract.md` and `plan/checkpoint-evidence.md#rc-012-concrete-route-scope-definition--planning-only--2026-09-26`.

- **Historical-head correction CI passed:** At that checkpoint, commit `e876c3957bc181cd29fe9a04b58c56f1dc853a1b` was confirmed as the origin branch and open PR #2 head. Runs `36261369876` and `36261373216` matched that exact SHA and each passed `static` and `test` (four successful jobs). This verifies the documentation receipt checkpoint only; no product acceptance changed.

- **Eligibility receipt follow-up CI passed:** At that checkpoint, commit `e2f924874d9d0a2ae26c83fff42de62a294c1f88` was confirmed on origin and as open PR #2 head. Runs `36261039777` and `36261042836` matched that exact SHA, each with successful `static` and `test` jobs. This documentation-only follow-up corrected the historical-head wording; a further reasoning review caught and corrected two stale present-tense claims about its predecessor. No product status changed.

- **Backlog recheck checkpoint published:** At that checkpoint, commit `15075e546ef28fcc073d8fb2d377b5101335e9f5` was confirmed on origin and as open PR #2 head. Runs `36260691812` and `36260689402` both matched the exact SHA and each passed `static` and `test`. The three-file handoff-only diff records the 68-task/242-edge recheck and preserves the unmet acceptance boundaries; no product task status changed. Follow-up review caught two superseded-head statements and they now identify the earlier SHA as current only at that prior recheck.

- **Full backlog eligibility recheck:** Reparsed all 68 unique tasks and their actual Starts/statuses/dependencies: 242 edges, no missing anchors or cycles. The previously reported RC-013 self-edge was a parser artifact. RC-012 and RC-014 meet their listed Starts and direct dependencies but remain partial with unfulfilled delivery/proof criteria; no safe additional implementation slice within their current task scope was identified. Other work remains gated, and the latest authoritative branch/PR/CI evidence shows no changed product prerequisite. Reasoning review caught and corrected an overstatement: RC-012/014 meet their listed Starts and direct dependencies, while their delivery/proof remains incomplete. Follow-up review also clarified that work may resume if a safe, scope-compatible slice within either task is identified; it need not depend on changing those already-satisfied prerequisites. No task status or criterion changed; RC-002 remains unknown and untouched. Details: [checkpoint evidence](plan/checkpoint-evidence.md#full-backlog-eligibility-recheck--2026-09-26) and [pivot handoff](PIVOT.md#pivot-record--full-backlog-eligibility-recheck--2026-09-26).

- **Latest receipt follow-up CI passed:** At that checkpoint, commit `ec6940ea7d27e55377ad9a725636d91337b9fa56` was confirmed as origin and open PR #2 head. Runs `36259389305` and `36259386898` both matched that exact SHA and each passed `static` and `test`. The follow-up records CI for `9db2e21c74d2a0cd2805200757d63f63df2abcdf`; it does not change product acceptance. Follow-up commit `33d9bb92e6b10f9a50fdb9005754a9f97079d3d8` passed both workflows in runs `36259584350` and `36259581415`, each matching that exact SHA with successful `static` and `test` jobs.

- **Receipt chronology qualifier corrected:** Reviewer `01a0deb1-0328-7221-a0b9-04aefa10cf9b` found the 7619 checkpoint still used present-tense head wording after it had been superseded by 85ce223. The CHANGELOG and checkpoint evidence now mark 7619's head as historical. Reviewers `01a0deb5-ba2e-7401-9ca1-e3e0ccfeb7ed` and `01a0deb7-4a78-7090-811c-e99ade7eea99` found no concrete defect; the latter reviewed the supplied patch, with live GitHub/CI verification remaining limited to the parent's recorded checks. Local documentation links and whitespace checks passed. This wording correction was published in `69f2b9c99fecdac66184a25d5838c1a5b54272f9`; runs `36258553869` and `36258551777` both completed successfully, each with `static` and `test` passing for that exact SHA. A later receipt review (`01a0debb-75df-7fc0-8b6e-f60b756ed3b5`) identified remaining present-tense references to the superseded 85ce223 head; these now say the head was confirmed at that checkpoint. Reviewer `01a0debd-8827-74d3-9729-5abfabf35884` found no concrete wording defect but could not inspect the complete Git diff or rerun checks. The parent reran documentation links, whitespace, and a focused chronology assertion. Reviewer `01a0debf-06c3-75f1-a2c5-960b0a465684` found no concrete wording defect; this was a current-file review without direct Git/check execution. The parent’s link, whitespace, and chronology checks passed. The correction was published in `c681d0075b1afb0708bbe48ebdc079ad6b58cf4c`; runs `36258982090` and `36258978553` both completed successfully with `static` and `test` passing for that exact SHA. No acceptance status changed. Follow-up receipt commit `9db2e21c74d2a0cd2805200757d63f63df2abcdf` passed runs `36259192188` and `36259189622`; each run matched that exact SHA and both `static` and `test` jobs succeeded.

- **Superseded-head wording receipt passed:** At that checkpoint, commit `85ce223df07fdc2cf367e796696bfb38c65f6e47` was confirmed as origin and open PR #2 head. GitHub runs `36257852142` and `36257849994` both passed `static` and `test` for that exact SHA. The historical wording correction remains documentation-only; no task acceptance changed.

- **Historical-head wording follow-up CI passed:** At that checkpoint, commit `7619adda0417708122f7aab2566a1c1949f3e5ee` was confirmed as origin and open PR #2 head. Runs `36257519408` and `36257515427` both passed `static` and `test` for that exact SHA. The older receipt claims now identify their checkpoint-time head instead of implying they remain current.

- **Historical PR-head correction verified:** At that checkpoint, commit `0740fb0f615a636b3d089ac6ff8bcc68c60a619c` was confirmed as the open PR #2 head on origin. Runs `36257258259` and `36257256288` both passed `static` and `test` for that exact SHA. The correction keeps older head claims historical; RC-002 remains unknown, RC-011 `REVISE`, and RC-012/014 partial.

- **PR-head chronology wording corrected:** Reasoning reviewer `01a0de9f-4e76-71e0-9ba8-0c358d865a49` found that some earlier receipt bullets still used present tense for commits already superseded on PR #2. Those statements now describe the PR head at the time of each checkpoint. The correction commit `0740fb0f615a636b3d089ac6ff8bcc68c60a619c` superseded `de2405438591048b38707e3e4cd87a4087a42ce3` as the verified remote head; the receipt update for `0740fb0` is recorded above. No product status changed.

- **Latest handoff CI receipt passed:** At that checkpoint, commit `de2405438591048b38707e3e4cd87a4087a42ce3` was confirmed as the open PR #2 head; runs `36256715616` and `36256713352` both passed `static` and `test` for that exact SHA. This is documentation-only evidence; backlog eligibility and product acceptance remain unchanged.

- **Final handoff receipt CI passed:** At that checkpoint, commit `c923e96b3d668740cefc38e4255e60e17f99ffc5` was confirmed as origin and open PR #2 head; runs `36256511530` and `36256508358` both passed `static` and `test` for that exact SHA. The handoff included the successful receipt for the prior checkpoint; no product behavior changed.

- **Eligibility receipt publication verified:** At that checkpoint, final receipt commit `fa3ac5c49a925ac2b6ec0a4dfb92dfd1941dbe1d` was the open PR #2 head on origin. Runs `36256311174` and `36256309420` both passed `static` and `test` for that exact SHA. It records CI for the unique-anchor eligibility handoff correction; no product code or task status changed.

- **Eligibility handoff anchor correction published:** Reasoning review caught that the prior generic evidence heading was duplicated, which made a changelog link target a stale 67-task recheck. Renamed the new 68-task evidence section and fixed the link. At that checkpoint, correction commit `f72b1d1e4ba5bf86156b799d274e3c517e01546e` was confirmed as PR #2 head; runs `36256110229` and `36256106489` both passed `static` and `test` for that exact SHA. Reviewer `01a0de93-625a-7bd2-87ae-60866ee90f73` found no concrete defect after the correction, but could not independently inspect Git or rerun checks. No task status or acceptance criteria changed. See `plan/checkpoint-evidence.md#backlog-eligibility-recheck-after-rc-012-receipt--2026-09-26`.

- **Backlog eligibility recheck after RC-012 receipt:** The 68-task inventory still has no newly eligible implementation task. RC-011's Start explicitly requires container routing that the current source does not provide and its design remains `REVISE`; RC-012 and RC-014 remain partial; RC-007/009 and the real Distill path remain gated by RC-002's unknown result; RC-016/017 and downstream work retain unmet dependencies. No gates or criteria were weakened, and no retry/authentication was attempted. The CI receipt follow-up `0495cee2a5d2edd1f5463e5415786eae68180e84` is published; its `static` and `test` workflows both passed in runs `36255823274` and `36255819839`. See `plan/checkpoint-evidence.md#backlog-eligibility-recheck-after-rc-012-receipt--2026-09-26` and `PIVOT.md`.

- **RC-012 source route inventory updated — partial audit:** The contract maps all six live workspace/profile/history routes to session-derived owner checks and RC-068 tests. Review found it supportable to treat `/api/actions` and `/api/events` as host action/event plumbing outside RC-012's enumerated resources, with cross-account isolation assigned to RC-011's gateway/container proof; this is not isolation evidence, and RC-011 remains `REVISE`. At publication, scope clarification commit `a6dcbf3140d8e82569a377cc6fb4a6641903eca5` was confirmed as the open PR #2 head. Both `static` and `test` workflows succeeded for that exact SHA in runs `36255579869` and `36255576596`. Bot, thread, file, workspace-task, and Bot-routine routes remain absent; RC-012 remains unaccepted. See `plan/rc012-resource-ownership-contract.md#current-source-route-inventory--partial-rc-012-scope` and `plan/checkpoint-evidence.md#rc-012-route-inventory-refresh--partial-audit--2026-09-26`.

- **RC-068 accepted — existing resource ownership proof:** At that checkpoint, commit `7bd3b82b74ed3f5e154880d08e74c7b224d59bc4` had been pushed and was verified as the open PR #2 head; `static` and `test` both passed in runs `36254504928` and `36254502319`. Real Elysia/SQLite integration tests with independently persisted A/B sessions verified workspace, profile, and history operations, ignored caller-supplied owner identity, foreign/nonexistent response equivalence, anonymous denial, valid-owner access, and unchanged durable history after rejected writes. The full API suite passed 25 tests / 160 assertions; typecheck and documentation checks passed. Independent review found no remaining concrete defect after two coverage corrections. This does not prove absent Bot/thread/file/task/routine routes and does not accept RC-012. Details: `plan/checkpoint-evidence.md#rc-068--existing-persisted-resource-ownership-proof-accepted--2026-09-26`.

- **RC-012 sequencing amendment — published:** At that checkpoint, commit `b3aaf9001c890dda0dfff26a9ada40724069def3` was the remote head of PR #2. Both `static` and `test` jobs passed in runs `36253446459` and `36253443302`. The 68-task graph is acyclic; RC-068 is now accepted for existing workspace/profile/history routes only. RC-012 remains partial because Bot, thread, file, workspace-task, and Bot-routine routes/proofs are absent; RC-021 remains ineligible because RC-016/018/020 remain outstanding. See `plan/checkpoint-evidence.md#rc-012-dependency-cycle-plan-amendment--published--2026-09-26` for the original amendment proof and review limits.

- **RC-012 contract review found a task-sequencing cycle (planning proposal only):** Added `plan/rc012-resource-ownership-contract.md` with identity, parent ownership, route enforcement, and per-route cross-user proof. Initial independent review `01a0dd91-9b66-7962-96dc-3c3d81ff1a77` found no concrete contract defect. A second reviewer `01a0dd94-5637-73f2-9ab0-29f5072ce61f` then found a real sequencing cycle: RC-012 requires routes delivered by downstream tasks that are themselves gated on RC-012. The proposal now recommends a separate authorization-foundation task, mandatory ownership proof as each feature route is introduced, and RC-012 as the final cross-resource audit without weakening its criteria. A third read-only review (`rc012-sequencing-independent-03`) found no issue in the revised proposal but could not certify the exact Git diff or rerun checks; it also confirmed RC-068 is not yet an implementation-ready ticket. Final review found this entry duplicated under September 24; removed the duplicate, and reviewer `01a0dd9b-68ac-7920-8c67-4877dcb5aa39` confirmed one remaining entry under September 26. The amendment is not applied to `plan/tasks.html`, and no feature task is newly eligible; RC-021 also awaits RC-016/018/020. The handoff records the review limits. Local documentation links and whitespace checks passed. No product code or task acceptance changed. RC-002 remains unknown/untouched; RC-011 remains `REVISE`. See `PIVOT.md` and `plan/checkpoint-evidence.md#rc-012-ownership-contract-and-sequencing-reassessment--2026-09-26`.

- **RC-012 ownership-sequencing proposal published:** At that checkpoint, commit `635a79b3c4873aa1e5047abf25828ffe1e873b1a` was the local and origin head of PR #2. Runs `36240995355` and `36240992600` both completed successfully; each passed `static` and `test`. This checkpoint adds only the ownership contract proposal and handoff records; it does not change the task graph or accept RC-012. Full review scope, CI and limits: `plan/checkpoint-evidence.md#rc-012-ownership-contract-and-sequencing-reassessment--2026-09-26`.

- **Final handoff review receipt:** At that checkpoint, commit `76bb2d33713fd7c9fc2fc96c08cb5d76c09698ee` was the local and origin branch head and the open PR #2 head. GitHub runs `36238563016` and `36238566020` both passed `static` and `test` for that SHA. The follow-up review independently confirmed the run/job data and found no factual defect; its exact-diff limitation and the parent's local checks are recorded. A subsequent reasoning review caught and corrected the present-tense wording here. These documentation-only updates do not accept RC-011 or alter any product task status. See `plan/checkpoint-evidence.md#final-handoff-publication--2026-09-26`.

- **Handoff receipt pushed:** Commit `b65c9cea79c78ff86af52454ebc9b406000d3d51` was published to the origin branch and open PR #2; CI runs `36238302742` and `36238304823` each completed successfully with both `static` and `test` jobs successful. The checkpoint changed only `CHANGELOG.md`, `PIVOT.md`, and `plan/checkpoint-evidence.md`; no product behavior or acceptance status changed. `scripts/__pycache__/` remained untracked and excluded. Exact receipts: `plan/checkpoint-evidence.md#final-handoff-publication--2026-09-26`.

- **Published RC-011 handoff and rechecked backlog:** Commit `108ca50c3adcc2fd556a46aa987fd86443ee3cf9` was the initial local and origin branch head; PR #2 remained open. GitHub runs `36237790347` and `36237792434` both completed successfully for that SHA, with `static` and `test` successful in each run. A corrected parser counted all 67 `<details class="ticket">` records. No new task is eligible: RC-011's explicit Start still requires container routing, which the current source does not provide; RC-012/014 remain partial, RC-016 and dependent work remain gated, and RC-002 remains unknown/unaccepted. No product code or task criteria changed. Full receipt and exact next step: `plan/checkpoint-evidence.md#publication-and-backlog-recheck--2026-09-26` and `PIVOT.md`.

- **RC-011 design review — not approved:** Froze a proposed gateway/tenant-routing design. Six conditional specialist seats reviewed first; the controller then began author cross-examination and fresh closure/system/arbiter verification before dispatching the six required baseline seats, which reviewed the original T-001 packet without peer output. The validated report is `plan/council/rc011-routing/council-report.json` (`VALID: REVISE`, one round, 13 objections); design and reviewer responses are in `plan/council/rc011-routing/`. The controller began author cross-examination and verification before the six required baseline seats were terminal; this protocol-order deviation is preserved in the report, so the result is not a procedure-conformant approval. The reviewers found unresolved direct-internal-port/host-boundary, tenant mapping and assertion replay, multi-gateway revocation, and measurable deadline/resource-bound risks. No code or Compose fixture was changed, and RC-011 remains unaccepted. The original criteria are unchanged; RC-012/014 stay partial; RC-002 remains unknown and untouched. Required council telemetry was unavailable (`distill subagent begin` returned `unrecognized subcommand`), recorded as a single proof gap. Exact-document reasoning review found and corrected objection-owner and report-path inconsistencies; follow-up reviewer `01a0dd5d-daa4-7141-afd8-8174a438dcca` found no remaining concrete defect in those corrections. The parent reran the council validator and documentation/whitespace checks; independent full Git diff review is not claimed. No prompt retry, credential or account change, deployment, or X post occurred. See `PIVOT.md` and `plan/checkpoint-evidence.md#rc-011-architecture-review--design-not-approved--2026-09-26`.

- **Backlog eligibility recheck:** Rechecked all 67 task records, relevant dependency/start gates, current source, and PR #2. No new task is eligible: RC-011 still lacks container routing; RC-012 and RC-014 remain partial and unaccepted; RC-016 and downstream tasks remain gated; real Distill tasks remain gated by RC-002's unknown prior effect. The pre-publication branch and PR head were `97ec6eb7307570da4cca184ebca987ca6125cd99`, with both static and test checks successful in runs `36235237914` and `36235240193`. No provider search, authentication, or retry occurred. The handoff update was published as `044548a1bb94e1e7bce5d550c928b51c07820a50`; its static and test jobs both passed in runs `36235828609` and `36235826165`. Full handoff: `plan/checkpoint-evidence.md#backlog-eligibility-recheck--2026-09-26` and `PIVOT.md`.

- **RC-012 partial authorization checkpoint (not accepted):** Workspace list/create, profile read/update, and workspace history read/create now use the validated persisted session identity for ownership filters. Logging out revokes only that user's sessions and event sockets. Review caught that cross-owner history GET returned an empty success and that anonymous storage routes were untested; history access now returns 404 for another owner's workspace (while an owned empty history remains 200), and API integration coverage exercises anonymous GET/POST for all six existing storage endpoints with persisted-state checks. Two seeded session identities verify cross-user isolation and that user B's WebSocket remains connected and can resynchronize after user A logs out. RC-012 remains partial: Bot, thread, file, and task routes do not exist, and the current single-identity login does not prove a real two-account/gateway journey. After review corrections, typecheck and full test/build/E2E/docs/whitespace checks passed: API/client 38 tests/185 expectations; browser 10 passed/3 configured skips. Corrected-patch reasoning review `01a0dd30-43a2-7161-a64e-35db9cafc753` found no concrete finding in inspected source/tests, but could not independently run Git or commands. Checkpoint `ff4d77e28b1ddbd2f76ed634a8202e08ebc88b4b` was pushed and read back from origin; PR #2 title/body were updated. Both GitHub runs `36235087681` and `36235089487` passed static and test jobs (four successful jobs). See `plan/checkpoint-evidence.md#rc-012--existing-persisted-resource-ownership-slice--partial-not-accepted--2026-09-26` and `PIVOT.md`.

- **RC-014 partial action-event recovery slice (not accepted):** Added stable SQLite action sequences with migration of existing receipt IDs, cursor-bearing WebSocket snapshots and events, authenticated snapshot repair, and a shared client reducer that deduplicates repeated events and requests a snapshot on cursor gaps. The web client clears private receipts on disconnect, requires session validation before explicit reconnect, and never resends the submitted action. API migration/snapshot tests, reducer tests, browser fault injection (lost event, reconnect snapshot, duplicate, dropped intermediate event/gap repair, single POST), 390×844 layout, session-expiry and logout regressions pass; root typecheck, Vite production build, docs links and whitespace checks pass. The real run-disconnection proof is still unverified because this product has no run executor; RC-002's prior Distill effect remains unknown and untouched. A stale E2E scenario that asserted an obsolete startup GET is removed because WebSocket snapshots are now the single startup state source. Exact commands and logs are recorded under the current goal scratch directory and in `plan/checkpoint-evidence.md`. Reasoning review found two P2 UI recovery defects: browser-forbidden WebSocket close code 1002 and a reconnect-in-progress state that could survive sign-out. Fixed both, added malformed-JSON browser fault injection, and re-ran checks: typecheck, API/client tests (34/148), Vite build, and full E2E (10 passed/3 configuration-dependent skips); focused malformed-message E2E passed. A second review found readiness could report healthy when startup migration failed; readiness now validates the durable sequence column, with a regression fixture that keeps all other dependencies available and a mutation check that fails when sequence validation is removed. Final post-correction checks passed: typecheck, API/client tests (35/150), Vite build, full E2E (10 passed/3 configuration-dependent skips), documentation links, and whitespace. Final source-level reasoning review `01a0dce1-32b5-73b0-b96b-a6e3990fe1b4` found no concrete defect in the inspected code/tests/records, but could not independently inspect the full Git diff or rerun commands; the reviewer limitation and parent-run validation are recorded in `plan/checkpoint-evidence.md`. Checkpoint commit `7826851e6831f5b4b9e7a76102972a4173e02657` was pushed and matched on origin; PR #2's title and managed description were updated. Both static and test checks succeeded in runs `36230723221` and `36230720873`. RC-014 remains partial/unaccepted. No X post was made.

- **RC-013 accepted:** Added a minimal Expo app and health state to the web screen, both using the shared typed Eden client. The bundled Release iOS simulator app called the real local API and showed ready → unavailable when the API stopped → ready after it restarted. The browser E2E exercised refresh failure and recovery; full suite: 8 passed, 3 configuration-dependent skips. Root typecheck, client tests (9/30), API tests (20/99), Vite build, docs links, and whitespace checks passed. A route response-field mutation failed typechecking at the shared typed response read. Review caught that React Native lacks static `Response.json`; the client now constructs the typed 503 with `new Response(...)`, covered by a no-static-helper timeout regression and verified in a rebuilt simulator app. The native behavior review did not rerun shell checks and noted that authentication/timeout classification were not directly demonstrated. A later retrospective review of the exact published patch found the missing harness design source as a P1 configuration defect but no additional product-source defect; the repair then passed remote static and test CI. The standalone Expo tsconfig remains unclean due to backend/Bun typing; the monorepo check is clean. Native timeout classification, native refresh input, Android, deployment, and the unresolved RC-002 Distill effect are not claimed. Per-platform contract proof now independently typechecks the web and mobile health service import graphs, and mutating both ready-route response branches from `status` to `state` fails both checks; the first one-branch mutation was insufficient and is not counted. After commit `70900248ccce91187de79a1ced6ecefaafd20212` was pushed, both GitHub static runs failed before checks because `design.source_of_truth` was absent; test jobs were skipped. Added the design source reference to existing mockups and task contracts, then passed the pinned v0.10.0 static pipeline locally. Retrospective review `01a0dcab-0435-7051-9a6b-20e07cf163a2` confirmed this missing field was the only concrete defect in the published source patch; the correction reviewer considered the value truthful but could not run Git or commands. Repair commit `95fdeaea110ad07df116eb3ba8221d6da0f42451` is published on origin and its two static plus two test CI jobs passed (runs `36227833469` and `36227831489`). `sam-harness plan` still reports unresolved design decisions; no UX plan approval is claimed. No X post was made; the previously stalled mac-use route was not retried. See `plan/checkpoint-evidence.md` and `PIVOT.md`.


- **Eligibility reassessment — planning only:** Rechecked RC-011 and RC-012 plus candidates RC-016–021 against exact Starts, dependencies, current API routes, ownership contract, and council procedure. RC-011 remains `REVISE` and has no container-routing prerequisite; no same-turn authorization was given to validate a revised council thesis. RC-012's accepted dependencies are satisfied, but the current records provide no additional bounded route slice beyond existing workspace/profile/history ownership; Bot/thread ownership through the workspace is defined in the accepted contract, but concrete Bot/thread/file/task route contracts and file/task ownership rules remain undefined. RC-016–021 remain gated by partial RC-012/014 and RC-016/017 dependencies. Independent read-only reviewer `01a0dd80-e09b-7721-8678-b1a225a3106f` confirmed no new task is eligible while noting a concrete future RC-012 slice could still proceed after its resource scope is defined. No product code, task status, or acceptance criteria changed. RC-002 remains unknown and untouched. Details: `plan/checkpoint-evidence.md#eligibility-reassessment--2026-09-26` and `PIVOT.md`.


- **Eligibility handoff published:** Documentation-only commit `f46255ae1b731eccd33265fe237099f85577bf29` is confirmed on the origin branch and open PR #2. GitHub runs `36239681543` and `36239683917` each passed `static` and `test` (four successful jobs). Reasoning review caught and corrected an overstatement: Bot/thread ownership via workspace is already defined, while route contracts and file/task ownership are not. Local documentation links, whitespace, and 67-task inventory checks passed. This checkpoint changes no product behavior or acceptance status; exact receipts are in `plan/checkpoint-evidence.md#publication-and-ci-receipt--2026-09-26`.


- **Handoff CI receipt:** Follow-up documentation commit `c7a6e12809c2b4471417a30d67bf4949b61eb4e3` was pushed and was the open PR #2 head at that checkpoint. Runs `36239787567` and `36239789672` both passed `static` and `test`. It records review/local-validation and CI evidence for `f46255a`; no product task status or acceptance changed. Details: `plan/checkpoint-evidence.md#follow-up-receipt-publication-and-ci--2026-09-26`.

## 2026-09-25

- **RC-013 initial implementation checkpoint (superseded by 2026-09-26 acceptance):** Centralized web/mobile health calls in the shared typed Eden client, adding credential/header forwarding, a bounded request timeout, caller cancellation, and typed error handling that preserves uncertain outcomes. Real API integration tests passed for both service modules, session cookies, unauthorized access, timeouts before headers, during JSON/text bodies and while consuming streams, plus cancelled requests. Root typecheck, API tests (20/99), client tests (6/23), docs links, and full local browser E2E (7 passed, 3 configuration skips) passed. A deliberate broken route failed typecheck. At that stage, independent review found that minimal mobile-app runtime proof was missing; the later iOS proof and accepted status are recorded in the 2026-09-26 entry. No native runtime compatibility was claimed at that checkpoint. RC-002 remains unknown and was not retried. See `plan/checkpoint-evidence.md` and `PIVOT.md`.


- **RC-010 accepted; final status patch review pending:** RC-010's unchanged auth/session/logout/expiry/private-route criteria are supported by recorded local and guest-Chromium Linux amd64-emulated evidence. Independent reviewer `01a0db17-6f9c-7962-a10c-061f20ca10bd` found no concrete defect in complete patch `44bf07a1b863f51a141c46900c766d0e922836fffbd5596de1a0c44d7ec79ad3` and confirmed the task remained in progress consistently until that clean review. A subsequent exact-patch review found outdated historical instructions in the handoff; these are now explicitly marked superseded, while current records agree RC-010 is accepted and only the final status-patch review blocks publication. The reviewer did not rerun tests or Linux proof. No native Linux or live external TLS proxy is claimed.

- **Historical RC-010 exact-diff review follow-up:** Independent reasoning review found two E2E assertion gaps: the lost-logout test asserted only before aborting the response, and the delayed-write test asserted before browser response processing. The first now waits for the actual `requestfailed` after abort and verifies signed-out state, cleared receipt, the unconfirmed-logout message, and private-route 401. The second waits for route fulfillment, browser `response` and `requestfinished`, then two animation frames before checking signed-out state and no receipt; the receipt remains visible after a later valid sign-in. Both focused tests passed after their respective changes. Then typecheck, API 20/99, full E2E 7 passed/3 configuration-dependent skips, separate expiry/WebSocket-loss E2E 2 passed, production build, documentation links, and whitespace checks all passed again. At that historical checkpoint, the exact patch still needed independent review; the later acceptance is recorded in the current entry above.

- **Historical RC-010 guest-browser proof; CDP invocation correction:** On Linux amd64 image `sha256:398dfc2639647b24469cf639efc73bc55db69cd139233bf574b6427103aeca54` under macOS arm64 emulation, guest Chromium driven by Node Playwright signed in, connected the authenticated WebSocket, fetched private data (200), created/read receipt `8a4bbf91-d3fe-48d4-afa7-ae139581db35`, signed out, returned to sign-in, and received 401 on the private route. On the same image and retained named volume, a 2500 ms TTL run created/read receipt `8c8a0fb3-ad8a-4bbc-b899-6f43aa8444a2`; session and private routes returned 401 after expiry and the receipt disappeared from the UI. Anonymous private access returned 401; separate-peer canonical and dot-segment cleartext login returned 403. Diagnosis: Bun's WebSocket request sent an Origin Chromium rejected; the same Playwright flow works under Node on the unchanged image. No application code or startup flag change was needed. Containers were stopped and the volume retained. Re-ran local typecheck, API tests (20/99), full E2E (7 passed/3 configuration skips), expiry/WebSocket-loss E2E (2 passed), production build, documentation links, and whitespace checks; all passed. At that historical checkpoint, fresh exact-diff review was still required before acceptance; the later clean review and acceptance are recorded in the current entry above.

- **Historical RC-010 WebSocket-loss privacy correction:** Independent review found private UI could remain visible after logout/expiry if the WebSocket was already unavailable. Sign-out now clears private UI immediately before awaiting the backend; any current-session WebSocket close also clears authentication and private state, with a sign-in message. New E2E relays the real WebSocket, deliberately disconnects it, verifies the receipt is hidden while the backend session is still valid, then observes the session expire to 401 with UI still clear. Focused 2500 ms TTL test passed. At that historical checkpoint, a full-suite rerun, Linux rebuild, and exact-diff review were pending; subsequent results are recorded in the current entry above.

- **Historical RC-010 path-normalization Linux proof:** Rebuilt image after the login-path guard correction: Linux amd64 manifest `sha256:9c07b52b0477898ed7e22bdfe239895e884403b32e932b3697addf2bc27a52ea` under macOS ARM emulation. A separate Docker peer using `curl --path-as-is` received 403 for `/api/auth/./login` and 403 for canonical login; anonymous private access returned 401. Guest Chromium logged in through localhost, wrote/read receipt `93e49084-c66e-4f2b-aa6f-8588fa2fd513`, signed out, and observed private 401. Container stopped; test volume retained. At that historical checkpoint, independent review of the latest patch remained required; the later complete-patch review and acceptance are recorded above.

- **Historical RC-010 ingress path-normalization correction:** The latest independent review identified that the web guard matched the raw canonical login path only. The middleware now decodes and normalizes URL paths before deciding whether to apply the loopback restriction. A raw `node:http` E2E preserves `/api/auth/./login`; both that path and canonical login from a non-loopback interface return 403 with no cookie. Focused test passed (1). At that historical checkpoint, re-review of the updated exact patch was required before acceptance; the later clean review and acceptance are recorded above.

- **Historical RC-010 proxy-ingress and uncertain-logout review checkpoint:** Independent review found non-loopback cleartext login could traverse the web proxy to the loopback-only API, a lost logout response could leave private data visible, and the delayed-login test did not wait for browser-side response handling. Vite now rejects login requests whose actual socket peer is not loopback; a non-loopback-interface Playwright request and a separate Docker peer both received 403/no session cookie, while local browser login remains available. If logout commits but its response is lost, a 4401 WebSocket revocation clears authenticated state and private UI even while the HTTP response is held; the E2E then aborts that response and verifies the old session gets 401. The delayed-login test observes the browser response, advances a browser task and render turns before checking the screen. Final local checks: typecheck, API suite 20/99, browser suite 7 passed/2 configuration skips, short-TTL expiry 1 passed, production build, docs, syntax, Compose, and whitespace. Rebuilt latest Linux amd64 image manifest `sha256:a3238a0c2f82f277b7564228fc3c4238b8e247651c776ffe2d1bba8a3771d9bf` on macOS ARM emulation: a separate peer's web-proxy login returned 403; Linux guest Chromium logged in, wrote/read receipt `8a9cdc2a-0d64-4e01-a4e7-7ad7175cedc7`, signed out, and received 401 on a private route. At that historical checkpoint, fresh independent review was required before acceptance; the later clean review and acceptance are recorded above. No native Linux or live external TLS-proxy claim.

- **Historical RC-010 review correction:** Independent review found the delayed-login E2E asserted sign-out immediately after releasing the held response, before proving that response was delivered. The test now signals after `route.fulfill` completes and waits for that signal before asserting sign-out and absence of private receipts. The focused Playwright regression passes (1 test). At that historical checkpoint, final exact-diff review was required after the correction; the later complete-patch review and acceptance are recorded above.

- **Historical RC-010 stale-login race correction and review checkpoint:** The independent review found that an overlapping login response arriving after logout could restore the private screen. Sign-in now advances and checks the auth epoch; sign-out advances it before awaiting the server, so earlier login responses cannot set the UI authenticated. A Playwright regression commits two separate login sessions, allows the newer sign-in, logs out, and then releases the delayed earlier response; the UI remains at sign-in, no receipt returns, and both tokens get 401. The rebuilt Linux amd64 image manifest is `sha256:408b9971ec53eba3522f5e888a28967ef464e7549a275bf440d22145babfdb99`. Local Playwright now passes 5 with 2 configuration skips; published-web Playwright against this Linux image also passes 5 with 2 configuration skips. The TTL 2500 ms browser expiry check passes 1; API tests pass 20/99; typecheck, Vite build, docs, syntax, Compose, and whitespace checks pass. The final image again shows anonymous 401, direct API HTTP login 403/no cookie, same-origin login/private 200, session after same-volume container recreation 200, forced expiry 401, logout 204, and post-logout private 401. At that historical checkpoint, the changed-line finding had been fixed and a fresh independent review was still required before acceptance/publication; the later clean review and acceptance are recorded above. No native Linux or live TLS-proxy proof is claimed.
- **Historical RC-010 review findings and in-progress checkpoint:** A fresh review found valid concerns about overlapping sessions in two tabs and a configured 500 ms TTL producing `Max-Age=0`. Logout from any valid session now deletes all persisted `local` sessions and closes every authenticated WebSocket; 500 ms or non-finite TTLs fail login with 503 before issuing a cookie, while wrong passwords remain 401. Regression coverage verifies two distinct simultaneously valid tokens both become 401 after one tab logs out, the other tab clears its private UI, 500 ms TTL has no cookie, and a valid short-TTL browser session expires. Typecheck, 20 API tests/99 expectations, default E2E (4 passed, 2 configured skips), expiry E2E (1 passed), Vite production build, docs links, Compose and syntax checks passed. On Linux amd64 emulation, the rebuilt image manifest `sha256:830759a01b10e055f09bf1ba2bd465728490191269d725363dc413dac69e2c69` passed full published-web Playwright (4 passed, 2 configured skips) plus anonymous 401, direct API HTTP login 403/no cookie, same-origin login/private access 200, persisted session after named-volume container recreation 200, expired route 401, logout 204, and post-logout route 401. A `docker restart` attempt failed to recover Xvfb in the prototype startup script; the proof therefore used container removal/recreation with the same retained test volume, as required by the persistence check, and does not claim restart recovery. At that historical checkpoint, independent exact-diff review was pending and RC-010 was unaccepted; the later complete-patch review and acceptance are recorded above. No native Linux host or live external TLS endpoint is proven.

- **Historical RC-010 security corrections and runtime checkpoint:** Review findings led to removing forwarded-protocol trust, enforcing actual socket-peer loopback for cleartext login, exact WebSocket origin validation, session-table readiness, and immediate socket revocation on logout. A further review concern about host-browser login was resolved by a host-side Playwright run against the Linux image's published web UI: login, action receipt, and mobile-width checks passed through Vite's same-origin API proxy; direct HTTP access to the separately published API port returned 403. Valid findings for idle-session expiry and delayed responses were corrected: WebSockets now close at persisted expiry, and an auth epoch prevents old action/list/socket responses from restoring private UI after logout or a later login. Final Linux amd64 manifest `sha256:9730b0fd3d60db6eae4a3881d60f43284c57b6b4df7b31e7ea5d7825be9c9ca9` (emulated on macOS arm64) showed anonymous 401, forged-Host login 403/no cookie, loopback login 200, valid private 200, expired 401, session after container recreation 200, logout 204, and post-logout private 401. Guest Chromium rejected an incorrect passphrase, authenticated its exact origin and WebSocket, wrote/read receipt `b25f22d1-4eb3-40eb-9703-dada2c0c6594`, passed 390×844 overflow validation, and lost private access after logout. Typecheck and Vite build passed; API tests passed 19/97; standard E2E passed 4 with 2 configuration-dependent skips; short-TTL expiry E2E passed 1; Docker-published UI E2E passed 1; links, syntax, and Compose configuration passed. At that historical checkpoint, fresh exact-diff review was pending and RC-010 was unaccepted. No native Linux hardware or live external TLS proxy was available; the later acceptance entry above preserves those limitations. See `plan/checkpoint-evidence.md` and `PIVOT.md`.
- **Review correction:** Restored the `.DS_Store`, `/TWITTER.md`, and `.sam-harness/evidence/` ignore rules; fixed the RC-008 accepted-evidence fragment and stale RC-010 task status. RC-008 is complete. The review also identified that the shared container startup environment is inherited by Distill; record this as an RC-015 isolation gap, not a verified agent exploit. RC-002 remains unknown and must not be retried.

- **Autonomous pivot policy:** Added [`PIVOT.md`](PIVOT.md) and linked it from the README. The initial assessment kept RC-002 unaccepted and RC-004 gated under the original dependency graph. A later entry on this date separates RC-004's non-executing contract from RC-002's live Distill proof; the provider effect remains unknown and no prompt was retried.
- **RC-002 local-source follow-up:** Rechecked Distill's local session list and targeted search, the retained sanitized source-check artifact, and Docker container names/status. Only the current Grok harness session is present in the inspected local history, and no RC-002/Distill test container appeared in the container metadata check. The saved timeout output reveals the ACP client's local request ID `4`, but no `session/new` response identifier or provider usage receipt. The original ACP scratch directory is gone, and the inspected sources do not resolve whether the provider processed or billed the prompt. No login or prompt retry was performed. RC-002 stays unaccepted while we wait for an authoritative Distill receipt or identifier. See `plan/checkpoint-evidence.md`.
- **RC-002 reconciliation check:** The earlier targeted local search returned `Total: 0`; four later searches each returned one entry whose ID matches this Grok harness session. `distill sessions list --limit 100` showed one unique local session ID, and `distill usage` requires a session ID. The ACP session ID was not retained, so these local results cannot be matched to or excluded from the ACP request. The existing Chrome profile's request-log URL redirected to the xAI console's team-creation page; no usage record was available, and no team was created. No login or prompt retry was performed. The provider effect remains unknown, so RC-002 stays blocked. At that point, RC-004 could not start under the original dependency graph; the later pivot on this date changes only the design-contract path. See `plan/checkpoint-evidence.md` for details.

- **RC-003 accepted against its prototype contract:** The Linux `linux/amd64` guest runs X11/Chromium, Elysia, React Native Web, and the local `linux-use` MCP. A guest-side Playwright helper runs through `docker exec`, with CDP bound to guest loopback and unpublished; `/proc/net/tcp` confirmed the loopback binding, and a peer container received connection refused on port 9222. The external browser created a backend receipt observed in guest Chromium; guest Chromium created another receipt observed externally. Both IDs and action descriptions were confirmed via the same guest `GET /api/actions`. All 3 guest-configured E2E tests passed. The Linux smoke invoked `screenshot`, `left_click`, and `type` and verified explicit unsupported errors; Distill MCP doctor reported the server healthy with 15 tools. This does not accept Linux Distill prompt execution (RC-002), persistent hosting, or unsupported GUI input/capture. See `plan/checkpoint-evidence.md` and RC-003 in `plan/tasks.html`.
- **Regression and verification:** Fixed a delayed initial action-list response overwriting newer WebSocket state by merging, deduplicating, and sorting receipts. A deterministic E2E covers that race. Typecheck, API tests (3 tests/7 expectations), Vite production build, default E2E (2 pass, expected guest-only skip), guest-configured E2E (3 pass), Linux-use smoke, Distill MCP doctor, documentation-link check, Python/shell/Node syntax checks, and `git diff --check` passed. The attempted `bun run test:api` referenced no defined script; the repository's defined API test command passed instead.
- **Initial gate assessment (superseded by the later plan pivot):** Under the original dependency graph, RC-004 and the foundation work behind it were gated on RC-002. The pivot now permits RC-004's non-executing contract only; the unknown prompt is still not reconciled or retried, and real Distill integration remains gated on RC-002.

- **Initial backlog readiness recheck (superseded by the later plan pivot):** Re-read the starts, dependencies, and statuses across all 67 planned tasks. Under the original graph, RC-004 was not eligible because RC-002's timed-out provider effect remained unknown. No product change or retry was made at that check. The later pivot allows RC-004's design-only contract to proceed, while keeping RC-002 unaccepted and the real Distill work gated. The connected MCP inventory has no Distill session, usage, or receipt tool. Evidence: `plan/checkpoint-evidence.md`.
- **Plan pivot and RC-004 design accepted:** RC-004 is complete as a non-executing ownership/state contract based on the accepted inventory and product plan. It explicitly preserves unknown Distill outcomes. The new `plan/state-ownership-contract.md` defines ownership, states, and deterministic outcomes for duplicate submission, lost client response, and host restart. RC-002 remains unaccepted and continues gating all real Distill execution/supervision. RC-005 is now the next eligible task. See `PIVOT.md` and `plan/checkpoint-evidence.md`.
- **RC-005 complete:** Added the `apps/mobile` workspace and feature-scoped typed health callers for web and mobile. Both use the shared Eden client, which now declares its `@remotecode/api` workspace dependency. The root typecheck compiled all workspaces; changing the health response type caused errors in both consumers. The first Linux image build exposed a missing mobile workspace copy in the Dockerfile. After adding the manifest and source copies, the amd64 image built and its root typecheck passed under emulation. API tests, web build, existing E2E checks, documentation links, and whitespace validation passed. No mobile UI or runtime compatibility is claimed. See `plan/checkpoint-evidence.md`.
- **RC-006 complete — Linux API lifecycle and health:** Added a Worker-isolated SQLite readiness check at the configured database path, a separate process-liveness route, a 500 ms readiness deadline, and readiness startup waiting. Readiness validates the essential `actions` table, not just database connectivity; API coverage confirms a missing table returns 503 while liveness remains independent. In the final Linux amd64 emulated container, startup returned ready; while SQLite was held in an exclusive lock with a 5-second busy timeout, readiness returned HTTP 503 in 417 ms and concurrent liveness returned HTTP 200 in 9 ms. Readiness recovered after unlock and after stopping/restarting the API. Typecheck, 7 API tests/23 expectations, documentation links, and `git diff --check` passed. An earlier synchronous-check attempt was rejected after independent review and is recorded as non-accepted in `plan/checkpoint-evidence.md`. This does not claim durable data; persistence is RC-008. RC-007 remains gated by the unresolved RC-002 prompt effect. See `plan/checkpoint-evidence.md` and `PIVOT.md`.
- **RC-008 remains in progress, not accepted:** The Linux named-volume proof establishes only the SQLite/action-receipt substrate: receipt `86b15209-5c7b-4180-92cf-42f2b7a68e74` and exact content survived container recreation with `rc008-proof-data-20260925`; during an exclusive SQLite lock readiness returned 503 in 0.416 s, liveness 200, and writes returned 500 without a receipt. The original delivery and failure criteria remain unchanged and still require database, workspace, history, and persistent-profile records to survive restart. Workspace, run-history, and profile records remain unimplemented. The E2E suite passed 2 tests and skipped the Linux-guest-only test because its separate RC-003 guest configuration was unavailable. RC-002 remains unknown and unaccepted; no retry was made. RC-010 may proceed only against the verified persistent SQLite/named-volume substrate, as its Start condition requires a persistent API; this explicit resequencing does not accept RC-008, and RC-009 remains gated on full RC-008 plus RC-002. See `PIVOT.md` and `plan/checkpoint-evidence.md`.

- **RC-008 domain persistence update — 2026-09-25:** Added SQLite-backed workspaces, history, and persistent profile routes. Final-source Linux amd64-emulated proof restored the exact domain IDs/content after container recreation with the same named volume. Under a real exclusive lock, readiness returned 503, liveness 200, and the authenticated write was rejected with 401; recovery left the workspace list unchanged. A distinct label-only image identity restored the same domain records and action receipt; application layers were unchanged, so this is not an application-version upgrade proof. RC-008 remains in progress pending exact-diff independent review. Full API tests passed (16/80), typecheck, E2E (2 passed, Linux guest case skipped), Vite build, documentation links and whitespace check passed. Browser interaction confirmed login, receipt creation/history, and logout; responsive E2E checked 1280×900 and 390×844. RC-010 remains unaccepted; RC-009 remains gated on RC-002 and full RC-008. Details and artifact identifiers are in `plan/checkpoint-evidence.md`.

- **RC-008 accepted after independent review — 2026-09-25:** The follow-up review inspected the exact source patch and current domain-volume logs and found no concrete changed-line defect or unmet criterion. Named-volume recreation restored exact workspace, history, and profile data; a distinct image ID restored the same records and receipt; a real database lock rejected the authenticated write without changing data. RC-008 is now complete against its original contract. The image replacement changed only image identity/label and did not exercise a new application version. Review also flagged agent environment-secret isolation for the separate RC-015 scope; no agent exploit was verified. RC-009 remains gated by RC-002. See `plan/checkpoint-evidence.md`.

## 2026-09-24

- **RC-003 prototype progress (not accepted at that time):** Added a Linux amd64 container with Xvfb/Openbox/Chromium, the Elysia API, React Native Web, pinned Distill v2.0.14, and pinned `linux-use`. In the live guest, Distill MCP doctor reported 15 tools healthy and `linux-use` confirmed X11, a visible Chromium window, and fail-closed screenshot/input calls. The external-browser receipt flow and mobile-width check passed against the running guest. Fixed an Eden `Date` render error and ran Vite on Node because Bun's WebSocket proxy crashed. The guest/external shared receipt test was still pending at that point.
- **RC-001 complete (inventory only):** Inspected the adjacent macOS app checkout at revision `5515958f7e6cbb46bf33c16c3ff91df7e4dab021` and mapped Workspaces, Git, terminal/SSH, agent, Bots, scheduled workspace tasks and Bot routines, notifications, account/settings, and desktop/web journeys to backlog IDs and observable proofs in `plan/parity-inventory.md`. Independent review found workspace scheduled agent tasks were not covered by the Bot-routine-only backlog item, so RC-045's title, scope, and executable proof now explicitly cover both schedule types, and RC-051's UI scope/proof now requires creating a workspace scheduled task and comparing both task and routine state with the API. Token Saver, generic browser/preview, previous harnesses, and WatchOS/Live Activities (no target client in the product plan) are explicitly excluded; Bot computer sessions remain in scope. The app source is outside this planning repository and was not copied. This inventory does not accept any implementation task.
- **RC-002 runtime probe:** The official v2.0.14 Linux ARM64 binary passed its published SHA-256 check but exited with `Illegal instruction` (132) in the cached ARM64 guest. The official x86_64 binary also passed its published SHA-256 check (`6e21372a858f7b563e038b6f0bdf3dd7da2e8d02f6cc9dfe2e9aca3b269e19a0`). An initial run could not start because the cached guest is ARM64 and lacks the x86_64 loader. After pulling Debian 12 `linux/amd64`, the same binary ran with networking disabled: `distill --version` returned `distill 2.0.14 (8e7101188723)` and `distill --help` succeeded. This verifies CLI startup in an emulated Debian guest, not the planned RemoteCode guest or RC-002's stdio session lifecycle. No credential values were inspected. A later authorized stdio prompt may have reached the provider, but its outcome and usage are unknown. The file creation/interruption/restart/reconciliation proof remains incomplete.
- **RC-003 initial blocker:** In the cached Linux image, the actual `linux-use` MCP stdio server ran under Xvfb; `doctor` confirmed Linux/X11, but `list_windows` failed safely because `wmctrl` was unavailable. This planning checkout initially had no Elysia/web app or container build source, and its cached image had no Linux Distill runtime.
- **Evidence handoff:** Added `plan/checkpoint-evidence.md` with reproducible environment versions, exact observed Linux MCP results, completion boundaries, and remaining proof requirements for RC-001–RC-003.
- **Planning-page search fix and browser proof:** The owned Chrome background-tab check found that searching `RC-002` returned zero tasks. Updated the existing search to include each task element's ID; repeated the same interaction and confirmed exactly one result, `RC-002 — Prove main Distill on Linux`. The rendered summary still showed `1 completed (inventory only); 2 blocked`, phase 00 showed four tasks, and the parity-inventory link returned HTTP 200. The browser integration accepts HTTP(S) URLs only, so direct `file://` navigation was not tested.
- **Backlog dependency gate — original graph (superseded by the 2026-09-25 pivot):** RC-004 then required accepted RC-002 evidence. The later pivot allows RC-004's design-only contract, while live Distill work remains gated on RC-002. Do not retry the uncertain provider prompt.
- **Publication:** Prior planning checkpoint `7aaeae3` and runtime evidence checkpoint `0c1623c` were pushed to `checkpoint/rc002-linux-runtime-evidence`. Pull request [#2](https://github.com/samuelfaj/remote-code/pull/2) tracks the branch. Keep it current after each later commit.
