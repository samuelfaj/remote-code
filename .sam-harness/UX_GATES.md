# User experience gates

The React Native Web action screen is user-facing. `plan/mockups.html` defines its recovery interaction, and `plan/tasks.html#rc-014` defines event-delivery behavior. Connection-state and recovery-control changes must be checked against both. New screens or layout and styling changes require updating the design source and rerunning `sam-harness plan`.
