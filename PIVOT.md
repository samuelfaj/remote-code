# Pivot policy

## Purpose

Keep safe, evidence-backed work moving when a task encounters a blocker. A pivot changes the route or sequence of work; it does not turn missing evidence into success or silently change a task's acceptance criteria.

## Response to a blocker

1. **Bound the blocked action.** Record the affected task, the exact missing prerequisite, what effect is uncertain, and which acceptance criteria are still unmet.
2. **Check authorized alternatives once.** Look for another already-authorized source, environment, or proof route that can answer the same question. Do not repeat a search that has already returned no usable evidence. While an external effect is unknown, do not authenticate, create accounts, provision paid resources, or retry the action. For other operations, cross those authority boundaries only when explicitly authorized.
3. **Choose the safest useful pivot.** Prefer, in order:
   - an equivalent proof route that preserves the task's full acceptance criteria;
   - an independent backlog task whose `Start` and dependencies are already satisfied;
   - bounded preparation that does not execute the blocked action, change production behavior, weaken acceptance criteria, or claim task completion.
4. **If no safe work is available, state the exact wait condition.** Do not invent parallel work, bypass dependencies, or repeatedly search the same source. Keep unrelated completed work intact and make the next action explicit.
5. **Keep the handoff current.** Record the decision in this file and update the task evidence and `CHANGELOG.md` when the blocker or chosen route changes. Review and publish only scoped, verified changes.

## Invariants

- A timeout or lost response leaves the effect unknown until authoritative state resolves it.
- Do not repeat an uncertain mutation merely to obtain a receipt.
- A Mac-only test cannot satisfy an acceptance criterion that requires real Linux execution.
- A new host, mock, local model, or alternative provider is not equivalent unless it exercises the same required product path and is allowed by the task contract.
- A pivot may defer or mark work unverified; only evidence can mark a task accepted. Scope reductions must be explicit and must not be represented as acceptance of the original contract.
- Do not cross account, credential, paid-resource, destructive, or deployment boundaries without the required authorization.

## Initial pivot record — RC-002 reconciliation (superseded for RC-004 sequencing) — 2026-09-25

- **Blocked task:** RC-002, “Prove main Distill on Linux.”
- **Missing prerequisite:** The timed-out ACP `session/prompt` has no retained Distill session identifier or authoritative session/usage record that can be correlated to it. The recovered request ID `4` is local to the ACP client.
- **Effect:** Unknown. The provider may or may not have processed the prompt; do not infer its outcome or billing from the timeout.
- **Routes checked:** Read-only local Distill history and usage capability, saved harness/terminal output, and the already-authorized browser profile. Local history identifies only the current Grok harness session; the Console route leads to team onboarding rather than a usage record. No credentials were read, no login/team creation occurred, and the prompt was not repeated. Detailed observations are in [`plan/checkpoint-evidence.md`](plan/checkpoint-evidence.md#rc-002--main-distill-on-linux).
- **Decision at the initial assessment:** Stop repeating those searches and keep RC-002 unaccepted. Under the original graph, RC-004 was also gated. The later record below revises RC-004 for non-executing contract work only. Do not run another lifecycle test while the old provider effect remains unknown. A Mac cannot satisfy RC-002's Linux proof.
- **Resume condition for RC-002:** Obtain an authoritative, sanitized record from an already-authorized source with identifiers and timing sufficient to correlate the prior attempt. Then compare it with every RC-002 acceptance and failure condition before resuming. If no such record exists, keep RC-002 explicitly unverified; do not claim acceptance or retry the operation.

## Pivot record — separate contracts from live Distill proof — 2026-09-25

- **Blocked path:** RC-002's timed-out ACP prompt still has no correlated provider record. It remains unaccepted, and the prompt will not be repeated while its external effect is unknown.
- **Safe resequencing:** RC-004 is a design-only ownership/state contract. Its start now requires the accepted RC-001 inventory and documented product flows, while explicitly modeling every unproven Distill result as `unknown`. This lets contract and backend-foundation work proceed without claiming the old prompt was reconciled or executing another provider action.
- **Unchanged gates:** RC-002 remains required before building or supervising real Distill runs, and remains unaccepted until the file-creation, interruption, restart, and reconciliation proof is obtained. Tasks that execute Distill retain that dependency. RC-015 now names RC-002 directly because its isolation proof requires the real Distill process.
- **Progress:** RC-004 is accepted as design only, RC-005 is complete for the monorepo/typecheck contract, and RC-006 is accepted for the API lifecycle and SQLite readiness contract. RC-002's receipt requirement remains unchanged; no task that runs or supervises real Distill can start until RC-002 is accepted, and the old prompt must not be restarted.
- **Next action (historical; superseded):** Proceed to RC-008, “Persist data outside the image,” which depends on RC-006 and can move forward without Distill. RC-007 and other tasks that execute or supervise real Distill remain gated on RC-002.

## Pivot record — RC-006 health check and RC-008 continuation — 2026-09-25

- **Reason for sequencing:** RC-006 needs to establish API process health and the current essential storage dependency, not execute or supervise Distill. The currently available dependency is SQLite; Distill is not part of API readiness at this foundation stage.
- **Verified scope:** Linux container execution proved start, stop, restart, SQLite exclusive-lock failure, bounded 503 readiness, independent 200 liveness, and recovery after unlocking. RC-006 is marked complete against those criteria.
- **Explicit limitation:** The database file currently defaults to `/tmp/remotecode.sqlite`; this gives the API a real dependency to check but is not durable storage. Data-volume persistence and restoration remain RC-008 and are not inferred from the health check.
- **Unchanged gate:** RC-002 remains unknown and unaccepted. RC-007 still depends on RC-002, and this pivot does not authorize another external prompt, authentication, or substitute Distill proof.

## Pivot record — RC-008 storage foundation — 2026-09-25

- **Eligible scope:** RC-008 depends on accepted RC-006 and does not require Distill. Current API state consists only of action receipts; workspaces, run history, and persistent profiles do not exist yet.
- **Safe implementation slice:** Persist the existing action receipts in the same configurable SQLite database used for readiness. Insert into SQLite before returning HTTP 201 or broadcasting the event. Configure the Linux image to use `/var/lib/remotecode/remotecode.sqlite` and expose that directory as a volume; Compose supplies the named volume.
- **Acceptance boundary:** This is progress, not RC-008 acceptance. The full task still requires workspace/history/profile storage and container destroy/recreate proof with the same volume. The task remains in progress until all of its delivery, failure, and executable proof criteria pass.
- **Unchanged gate:** RC-002 remains unverified and no uncertain Distill request is repeated. RC-007 and live Distill work stay gated on RC-002.
- **Observed proof:** Built `remotecode-rc008:local` as Linux amd64 emulation and ran it with named volume `rc008-proof-data-20260925`. A receipt survived container removal and recreation with the same volume; the rebuilt image also restored the same ID/content. Under a SQLite exclusive lock, readiness returned 503 in 0.423 seconds while liveness returned 200, and readiness recovered after unlock. A locked action write returned 500 and was absent from action history. See `plan/checkpoint-evidence.md` for IDs and commands/results.
- **Next work at that checkpoint:** Implement workspace, run-history, and persistent-profile storage; the initial RC-008 scope remained open.

## RC-008 domain persistence and volume proof update — 2026-09-25

- **Implementation and direct proof:** Added SQLite workspace, history, and persistent profile records and authenticated API routes. A final-source Linux amd64-emulated container restored exact workspace/history/profile IDs and content after container recreation with the same named volume. A distinct image ID restored those records and an action receipt as well. A real SQLite lock caused readiness 503, liveness 200, and rejected the authenticated write with 401; after unlock, readiness recovered and the workspace list remained unchanged.
- **Image-swap boundary:** The replacement image was derived from the base by adding a label, leaving application filesystem layers unchanged. The proof covers named-volume recovery across container/image identity, not application-version migration. The environment was Docker/OrbStack amd64 emulation on macOS arm64, not native Linux hardware.
- **Acceptance decision:** The first independent review lacked the exact patch and current domain-volume logs, so it was not used. A follow-up exact-diff reasoning review inspected the source patch and both successful proof logs, found no changed-line defect and no unmet RC-008 criterion, and confirmed the named-volume/image-identity and locked-write evidence. RC-008 is accepted against its original contract. The reviewer noted that the image identity change did not change application layers, so it is not an application-version migration proof. Original RC-008 delivery and failure criteria remain unchanged.
- **Historical dependencies at the RC-008 checkpoint (superseded below):** RC-010 was then in progress against the verified persistent API substrate and awaited its auth, Linux, and independent security proof. RC-009 remains gated by RC-002; full RC-008 is now accepted. RC-002's earlier Distill request remains unknown and must not be repeated. The reviewer also identified environment-secret isolation as a separate RC-015 verification requirement; no agent access was proven.

## Superseded interim decision — RC-008 acceptance — 2026-09-25

- **Historical decision:** This interim pivot kept RC-008 in progress because the first Linux proof covered only the SQLite/action-receipt substrate. It retained the original database, workspace, history, and profile criteria but preceded their implementation.
- **Current decision:** Subsequent work added those domain records, proved named-volume recovery and locked-write failure behavior, and passed an exact-diff independent review. RC-008 is complete against its original contract; current evidence and limits are recorded in `plan/checkpoint-evidence.md` and `plan/tasks.html#rc-008`.
- **Historical RC-010 dependency state (superseded by the later acceptance checkpoint):** RC-010 then continued on the persistent API and awaited authentication/session/logout/expiry/private-route criteria, Linux runtime proof, and independent security review. RC-009 remains gated by RC-002; RC-008 completion does not resolve RC-002.
- **Unchanged external uncertainty:** RC-002 remains unknown. Do not retry the earlier Distill request; RC-007 and live Distill work remain gated.

## RC-010 security-review correction — 2026-09-25

- **Reviewer findings:** The independent review rejected trusting client-supplied `X-Forwarded-Proto`, identified missing WebSocket origin validation, and found readiness did not verify the session table. It also found the RC-008 task link stale and protective ignore rules missing from the checkpoint diff.
- **Security decision:** Removed forwarded-protocol trust rather than assuming a trusted proxy. Remote cleartext login now fails closed, and only the request URL's HTTPS scheme enables a Secure cookie. Remote login through a TLS-terminating proxy is unsupported until authenticated ingress identity exists. Exact `REMOTECODE_WEB_ORIGIN` checks remain on the WebSocket route; readiness checks the `sessions` table. `.gitignore` protections and the RC-008 task evidence link are restored/corrected.
- **RC-015 assessment:** Current prototype startup passes the container environment to the shell and starts API, web, and Distill processes without distinct OS identities. This is a concrete isolation gap in the current process setup, but no agent-side read attempt has been run and RC-015 remains gated on RC-002. Do not claim secret isolation; handle it with RC-015's distinct-identity implementation and real agent-terminal proof.
- **Review corrections:** Fresh independent review found that HTTP `Host: localhost` could bypass the URL-host loopback check, and that signing out in one tab did not promptly clear another tab's private UI. Cleartext login now trusts only Bun's actual loopback peer address; a separate Linux container confirmed forged `Host: localhost` is rejected with 403 and no cookie. Logout now closes matching authenticated WebSockets with code 4401, and clients clear private state on that revocation. API coverage passes 18 tests/93 expectations; browser E2E passes 3 tests including two-tab logout. The root `test` script now limits Bun discovery to `apps/api/src`, because the earlier generic command tried to load Playwright specs in the wrong runner.
- **Observed proof:** Current-source Linux amd64 image `sha256:c114764fee755ec3901336a6e3e348458ede193e44e4a85ede553bc6880adb9a` ran under emulation on macOS arm64. API runtime proof: anonymous private 401, forged forwarded HTTPS 403, remote non-loopback with forged `Host: localhost` 403 and no cookie, local-peer login 200, valid private access 200, expired 401, session after container recreation 200, logout 204, post-logout private 401. Guest Chromium at `http://localhost:5173/` rejected an incorrect password, connected to authenticated WebSocket updates, wrote/read back receipt `97e82025-07b6-4952-bed1-34ade5bebcf8`, passed the 390×844 overflow check, and returned to sign-in after logout with private access 401. HTTPS Secure-cookie behavior is unit-tested directly; no external TLS proxy was available.
- **Final corrected checkpoint proof:** Rebuilt the corrected tree as `remotecode-rc010-final-checkpoint-20260925:local`, Linux amd64 manifest `sha256:2ebb28dd17955c3791ddc577f688922455487139657e4062d90ba347c65d81bd`, under OrbStack emulation on macOS arm64. Readiness 200; anonymous private route 401; non-loopback client with forged `Host: localhost` login 403 and no cookie; true loopback login 200; valid private route 200; forced expiry 401; persisted session after container recreation 200; logout 204; and post-logout private route 401. Guest Chromium on that image rejected an incorrect passphrase, authenticated to the exact origin, connected its WebSocket, wrote/read receipt `095aba29-cad1-4e88-b4d7-83d1c5b123ee`, passed the 390×844 overflow check, and returned to sign-in with private API access 401 after logout. Details are in `plan/checkpoint-evidence.md`.
- **Historical review follow-up and safe corrections:** At that checkpoint, independent review identified three concerns. The host-browser concern came from testing the directly published API port rather than the web UI. A host-side Playwright journey against the final Linux image's published web port passed login, action receipt, and mobile-width checks through Vite's same-origin `/api` proxy; direct API HTTP remains correctly rejected with 403. For the valid findings, authenticated WebSockets now close at persisted session expiry, and the UI clears private state on that close. An auth epoch prevents pending action/list/socket responses from restoring private UI after logout or a later login; submission state resets on revocation. API tests passed 19/97; standard E2E 4 passed / 2 configuration-dependent skips; short-TTL expiry E2E passed 1; host-published UI E2E passed 1. Final Linux image manifest: `sha256:9730b0fd3d60db6eae4a3881d60f43284c57b6b4df7b31e7ea5d7825be9c9ca9`; guest receipt `b25f22d1-4eb3-40eb-9703-dada2c0c6594`. Typecheck, build, docs, syntax, Compose and final runtime checks passed. At that historical checkpoint, fresh independent review of the corrected exact diff remained pending; the later complete-patch review and acceptance are recorded below.
- **Historical acceptance boundary (superseded by the later RC-010 acceptance checkpoint):** At this stage RC-010 remained unaccepted pending fresh exact-diff review. The later acceptance decision is recorded below. Linux amd64 is emulated on macOS arm64; native Linux host proof is not claimed.

## RC-010 guest-browser proof — CDP invocation pivot — 2026-09-25

- **Image and persistence:** Image `remotecode-rc010-final-20260925:local`, manifest `sha256:398dfc2639647b24469cf639efc73bc55db69cd139233bf574b6427103aeca54`, ran Linux amd64 under macOS arm64 emulation. A fresh container used the same named volume `rc010-wsloss-final-20260925` after the prior container's in-place restart failed at Xvfb startup.
- **Cause and pivot:** The earlier guest helper was invoked with Bun. Bun's CDP WebSocket handshake sent `Origin: http://localhost`; Chromium rejected that origin and Playwright surfaced a timeout. A raw handshake without Origin returned 101, and running the existing Playwright helper with Node connected to the same unchanged image. No app or Chromium startup code change was necessary; a temporary CDP origin-flag build was not used for acceptance evidence.
- **Behavior verified:** Guest Chromium logged in, connected the authenticated WebSocket, fetched the private actions route with 200, and created/read receipt `8a4bbf91-d3fe-48d4-afa7-ae139581db35`. UI logout returned to sign-in; private fetch after logout returned 401. A fresh container on the same image and volume with 2500 ms session TTL created receipt `8c8a0fb3-ad8a-4bbc-b899-6f43aa8444a2`, received 200 before expiry, and after expiry showed session and private routes returning 401 with receipt UI cleared. Anonymous access also returned 401. Separate peer canonical and dot-segment login requests returned 403.
- **Historical boundary at this proof checkpoint (superseded by the later acceptance checkpoint):** Containers are stopped and the named volume retained. Linux results are emulation, not native Linux hardware; no live external TLS proxy is verified. At that point, exact final-diff review remained required before acceptance/publication.

## RC-010 acceptance checkpoint — 2026-09-25

- **Decision:** RC-010 is accepted against its unchanged criteria after local and Linux amd64-emulated behavior evidence and a clean independent review of complete patch `44bf07a1b863f51a141c46900c766d0e922836fffbd5596de1a0c44d7ec79ad3` by reviewer `01a0db17-6f9c-7962-a10c-061f20ca10bd`. The review found no concrete defect and confirmed the handoff status was consistent. The reviewer did not rerun tests or reproduce Linux results; these remain supported by the recorded test and runtime evidence. No acceptance criterion was reduced.
- **Limits:** This is Linux amd64 emulation on macOS arm64, not native Linux. External TLS proxy behavior was unavailable. RC-002 remains unknown and is not retried. The acceptance-record patch itself still requires exact review before commit/push.

## RC-010 test-evidence correction — 2026-09-25

- **Review outcome:** Review of frozen patch `84b85d7097d2b03c3a5f1e23a31fb909a78e44b3e16655d1cda0966fb71f891f` found no demonstrated runtime defect in the core auth behaviors, but identified two P2 E2E evidence gaps: the lost-logout test asserted before the held response was aborted; the delayed-write test asserted before browser response processing.
- **Corrections and proof:** The lost-logout case now waits for `requestfailed` after abort and checks signed-out UI, absence of the receipt, the unconfirmed-logout message, and private-route 401. The delayed-write case now observes route fulfillment, browser `response` and `requestfinished`, and two animation frames before asserting signed-out UI/no receipt; after signing back in it confirms the committed receipt appears. Each focused Playwright test passed. Then typecheck, API (20/99), full E2E (7 passed/3 configuration skips), expiry/WebSocket-loss E2E (2 passed), Vite build, documentation links, and whitespace checks all passed.
- **Historical gate (superseded by the later RC-010 acceptance checkpoint):** The older frozen patch review was not final approval. The updated complete patch was subsequently reviewed cleanly; the accepted decision and evidence are recorded below.

## RC-010 follow-up review findings — 2026-09-25

- **Findings:** Independent reviews found that overlapping valid sign-ins could leave another browser tab's socket/private UI alive after logout, a TTL under one second produces a browser cookie lifetime of zero, and an in-flight successful sign-in response could restore the private screen after logout.
- **Corrections:** A valid logout for the sole configured `local` user now deletes all persisted sessions and closes all authenticated sockets. Invalid, non-finite, or sub-second TTL configuration fails login closed with 503/no cookie; wrong passwords still receive 401. `signIn` advances and checks the auth epoch before applying a response, and `signOut` advances it before awaiting the server. Regression tests cover two distinct valid tokens, cross-tab UI clearing, a 500 ms TTL, and a delayed login response after logout.
- **Proof:** Local typecheck, API (20/99), default Playwright (5 passed, 2 config-dependent skips), explicit 2500 ms expiry Playwright (1 passed), Vite build, links, Compose, syntax, and whitespace checks passed. Final Linux amd64-emulated image manifest `sha256:408b9971ec53eba3522f5e888a28967ef464e7549a275bf440d22145babfdb99`; published-web Playwright passed 5 with 2 config skips. Same-volume container recreation preserved the session (200); expired private access returned 401; logout returned 204 and private access returned 401. A standalone `docker restart` on an intermediate candidate failed at Xvfb startup with `Timed out waiting for X11 at :99`; restart recovery is not claimed.
- **Review correction:** The next independent review found that this regression could assert sign-out before the held login response was delivered. The test now waits for a signal emitted after the intercepted response is fulfilled before asserting that the private UI stays cleared. Focused Playwright passed 1 test. At that historical checkpoint, this correction required fresh review of the exact updated diff and acceptance/publication remained pending. The subsequent clean review and acceptance are recorded above.
- **Review corrections after the latest review:** A remote cleartext request through the Vite web proxy could reach API login because the proxied API request appeared to come from loopback. Vite now checks its own actual socket peer and rejects non-loopback login requests before proxying. The UI clears authenticated state and private receipts when logout's response is lost, including after the backend has committed logout. The delayed-login regression now waits for a response observed by Playwright and subsequent browser task/render turns before its final signed-out assertions.
- **Observed verification:** Non-loopback interface and separate Docker-peer login requests to the proxy returned 403 without a session cookie; loopback login returned 200. Browser E2E proved logout commit plus aborted response clears private UI and the old session returns 401. Rebuilt latest Linux amd64 image manifest `sha256:a3238a0c2f82f277b7564228fc3c4238b8e247651c776ffe2d1bba8a3771d9bf` under macOS ARM emulation: a separate peer received 403 at the web proxy; guest Chromium signed in locally, created/read receipt `8a9cdc2a-0d64-4e01-a4e7-7ad7175cedc7`, signed out, and observed private 401. Full local E2E 7 passed/2 configuration skips; API 20/99; typecheck/docs links passed; expiry E2E passed separately with 2500 ms TTL. The E2E now holds the logout HTTP response until after the WebSocket revocation clears private state. These checks do not prove native Linux or live external TLS behavior.
- **Further review finding and correction:** A fresh reviewer noted that a raw dot-segment path might be interpreted differently by Vite's guard and the API router. The guard now URL-decodes and POSIX-normalizes the path before matching. The new E2E uses Node's raw HTTP client to preserve `/api/auth/./login`; both it and the canonical path from a non-loopback interface returned 403 with no cookie. Focused ingress E2E passed. The final rebuilt Linux amd64 image manifest `sha256:9c07b52b0477898ed7e22bdfe239895e884403b32e932b3697addf2bc27a52ea` returned 403 for both canonical and dot-segment remote login requests (`curl --path-as-is`), 401 for anonymous private access, and guest-browser login/receipt/logout/private-401 succeeded with receipt `93e49084-c66e-4f2b-aa6f-8588fa2fd513`.
- **Historical latest review finding and correction:** At that checkpoint, a further independent review found private UI remained visible while logout was pending and after WebSocket loss. Sign-out now clears state immediately before the request; any WebSocket close for the current auth epoch clears private UI and requires sign-in. The error response keeps logout marked unconfirmed. A new real WebSocket E2E verifies receipt state disappears when disconnected, the API session is initially still 200, and the backend session later expires to 401 while the UI remains clear. Focused test passed with a 2500 ms TTL. At that historical checkpoint, full checks, a Linux proof rebuild, and fresh exact-diff review remained outstanding before acceptance/publication; the subsequent acceptance checkpoint above records their resolution.
- **Historical next step (superseded by the later RC-010 acceptance checkpoint):** Obtain an independent reasoning-model review of the corrected full diff before acceptance/publication. That review is complete and its decision is recorded below. The prior unknown RC-002 provider effect remains unchanged and must not be retried.

## Historical RC-013 shared client checkpoint — superseded by the 2026-09-26 acceptance below — 2026-09-25

- **Safe route:** After RC-010 was accepted, RC-013 met its declared prerequisites and does not depend on live Distill. The shared Eden client now drives web and mobile health service calls, forwards cookie credentials/headers, bounds requests, preserves cancellation, and reports typed errors. RC-002 remains unknown and no Distill request was retried.
- **Verified slice:** Live API integration exercised the web and mobile service modules under Bun, persisted-cookie access, unauthorized access, stalled JSON body timeout, and pre-aborted caller cancellation. The misspelled route failed root typecheck. The browser suite passed 7 with 3 configuration-dependent skips after correcting an HTTP error-handling incompatibility found by E2E. Independent review found and prompted fixes for body-timeout normalization and caller-signal propagation.
- **Unaccepted boundary:** RC-013's executable proof explicitly calls for the same call through a minimal mobile app. The current app workspace has only a service module; the tests do not run a native/mobile app runtime. Keep RC-013 **In progress**, do not advance its dependents, and do not claim mobile compatibility. `.sam-harness/UX_GATES.md` requires the harness configuration and plan to be updated before introducing a user-facing surface. Next action: make that plan/config checkpoint, then supply a runnable minimal mobile app runtime proof while preserving the current typed client contract.
- **Publication/posting:** No X update was made because this is an incomplete implementation checkpoint, not a complete product milestone; `TWITTER.md` requires posting only verified significant milestones at least one hour apart. Push only this scoped checkpoint after exact review and current-tree checks; do not include the pre-existing `scripts/__pycache__/` untracked files.

## RC-013 accepted; resume dependent backlog — 2026-09-26

- **Decision:** RC-013 now satisfies its original shared-client contract and executable proof: web and a minimal bundled iOS app used the shared typed health client; browser and simulator states showed ready/unavailable/recovery; independent web-only and mobile-only typechecks passed, and a deliberate response-field mutation across both ready-route branches failed each consumer typecheck. The initial review finding that React Native lacks `Response.json` was corrected. Retrospective review `01a0dcab-0435-7051-9a6b-20e07cf163a2` examined the supplied published patch, identified the missing required `design.source_of_truth` as its sole concrete defect, and found no additional product-source defects. The configuration correction is committed as `95fdeaea110ad07df116eb3ba8221d6da0f42451`, published, and passed both static and test checks in each of two GitHub runs. Evidence and verification limits are in `plan/checkpoint-evidence.md#rc-013-shared-eden-connection-accepted-2026-09-26`.
- **Limits:** No Android or deployment proof is claimed. Native timeout classification, authenticated native session behavior, and native button injection were not directly demonstrated; the standalone Expo tsconfig is not clean because backend/Bun typing is pulled into that project. None of these results justify claiming a separate server version or production compatibility.
- **Next eligible task:** RC-014, “Deliver events with reconnection,” has its listed prerequisites RC-004, RC-008, and RC-013 accepted. Inspect its Start, Delivery / end, Executable proof, Failure if, and Depends on fields before implementation. Keep RC-002 and all actual Distill execution/supervision tasks blocked by the unreconciled external prompt effect; do not retry or claim it resolved.
- **Checkpoint publication and repair:** Commit `70900248ccce91187de79a1ced6ecefaafd20212` is on origin. Its two static GitHub runs failed before configured checks because the newly enabled design gate lacked a source-of-truth value; tests were skipped. The config now points to existing `plan/mockups.html` and `plan/tasks.html`, and the exact v0.10.0 static pipeline passes locally. Correction reviewer passed the value as truthful design intent but could not inspect Git or execute commands. Retrospective review `01a0dcab-0435-7051-9a6b-20e07cf163a2` confirmed the published config defect was the only concrete finding in the source patch; the correction review passed the truthfulness of the new source reference but could not inspect Git or execute commands. The repair commit `95fdeaea110ad07df116eb3ba8221d6da0f42451` and synchronized docs are published; both static and test checks passed in each of its two CI runs (`36227833469`, `36227831489`). `sam-harness plan` has unresolved UX/design decisions, so no UX approval is claimed. No X post was made; the earlier `mac-use` route stalled, and no retry was made.

## RC-014 — action-event recovery slice — 2026-09-26

- **Eligibility and boundary:** RC-004, RC-008, and RC-013 are recorded as accepted, and the remote PR shows all four required static/test checks passed for the preceding RC-013 commit. RC-002 remains unknown, unaccepted, and untouched. The product currently has persistent action receipts but no run executor, so action-event tests cannot satisfy RC-014's unchanged requirement to disconnect during a real run. This checkpoint is explicitly partial; do not change the task's acceptance criteria or mark it accepted.
- **Implemented route:** Added a durable SQLite integer sequence, transactionally migrated existing receipt rows while preserving IDs/content/order, and attached sequence cursors to snapshots and `action.created` events. Authenticated clients can request a fresh snapshot; the shared client applies a snapshot as canonical state, ignores duplicate/old cursors, and detects gaps. Web starts connected only after a valid snapshot, clears private UI on disconnect, requires a current-session check before explicit reconnect, and never repeats POST during recovery. Socket generation and auth epoch checks reject stale callbacks; synchronization has a five-second deadline. The React Native Web recovery affordance is covered by the recovery mockup, task contract, and harness UX/source references.
- **Verification:** Tests include legacy database migration/reinitialization, cursor snapshot and repair, reducer replacement/dedup/gap/malformed cases, browser lost-event reconnect with response held and exactly one POST, duplicate event delivery, missing-intermediate-event repair, malformed JSON teardown, 390×844 overflow, cross-tab logout, pending write/logout, and short-TTL expiry while disconnected. Reasoning reviews found and prompted fixes for malformed-event close/state behavior, stale reconnect progress, and readiness not validating the migrated cursor schema. A readiness regression test now creates all other required tables before locking and has been mutation-checked. Current full checks passed: root typecheck; API/client 35 tests/150 expectations; Vite build; browser E2E 10 passed/3 configuration skips; documentation links; whitespace. See `plan/checkpoint-evidence.md` and the active goal plan for exact review IDs and outputs.
- **Review outcome and limitation:** Final source-level reasoning review `01a0dce1-32b5-73b0-b96b-a6e3990fe1b4` found no concrete defect in its inspected code, tests, and records. It could not inspect Git/shell, so it did not independently certify the complete base-to-working-tree diff or rerun tests; the current session separately checked the local diff and reran checks. This limitation is recorded in `plan/checkpoint-evidence.md`.
- **Published checkpoint:** Commit `7826851e6831f5b4b9e7a76102972a4173e02657` was pushed and matched by `git ls-remote`. PR #2 description/title now reflect partial RC-014 scope and verified evidence. Both `static` and `test` checks passed in runs `36230723221` and `36230720873` (all four check jobs successful).
- **Next:** No remaining task can advance past the run-executor dependency: RC-016 depends on RC-014 acceptance; real lifecycle tasks depend on unresolved RC-002. Keep RC-014 partial until supervised-run behavior exists and is proven. Do not retry or authenticate for the uncertain Distill prompt. Actual Distill lifecycle tasks remain gated; never retry or investigate the unresolved request through authentication. No X post was made because the RC-014 original acceptance proof is incomplete; do not retry the previously stalled mac-use route.

## Pivot record — RC-012 existing-resource authorization slice — 2026-09-26

- **Eligible task:** RC-012's direct dependencies RC-004 and RC-010 are accepted. Existing persistent workspace, profile, and history resources are available, so owner checks can be implemented without real Distill execution.
- **Implemented partial route:** The existing routes now derive the owner from the validated persisted session; reads and writes are owner-scoped. A workspace history lookup by another user's workspace ID returns 404, whereas an owned workspace with no history returns 200/empty. Logout revokes only that user's database sessions and event sockets.
- **Proof and review:** Real Elysia tests use two separately seeded session identities to verify cross-user isolation and selective socket revocation/resync. Anonymous calls to all six existing storage routes return 401 without creating workspace/profile/history records. First reasoning review found the history GET status distinction and missing anonymous tests; both were corrected. The corrected current-tree full check passed. Final independent review `01a0dd30-43a2-7161-a64e-35db9cafc753` found no concrete finding, with no shell/Git execution available to the reviewer. Checkpoint `ff4d77e28b1ddbd2f76ed634a8202e08ebc88b4b` and its CI passed; follow-up documentation commit `97ec6eb7307570da4cca184ebca987ca6125cd99` synchronized this receipt.
- **Explicit incomplete scope:** Bot, thread, file, and task routes do not exist. Current password login issues only the local host identity, so seeded sessions are not evidence of a full multi-account login/gateway flow. RC-012 remains partial/unaccepted until all its original delivery, executable-proof, and failure conditions can be exercised. Record this by appending the partial status to RC-012 in `plan/tasks.html`; do not change its acceptance criteria.
- **Published checkpoint:** Commit `ff4d77e28b1ddbd2f76ed634a8202e08ebc88b4b` was pushed and confirmed on origin. PR #2 now describes the RC-012 partial scope and limitations; both static/test jobs passed in runs `36235087681` and `36235089487`.
- **Next eligibility check:** RC-011's explicit Start requires container routing, which is absent. RC-021 remains gated by incomplete RC-012, RC-016 by incomplete RC-014, and real Distill work by RC-002. No next backlog item currently satisfies both its Start and dependency requirements; keep the criteria intact, leave RC-012/014 partial, and re-evaluate as prerequisites change. The unknown RC-002 request remains untouched.

## Pivot record — dependency recheck — 2026-09-26

- **Recheck:** Re-read the 67-task backlog and checked the relevant Starts, dependencies, and statuses against current product source and published PR #2. RC-011 still requires unavailable container routing; RC-012 still lacks Bot/thread/file/task ownership routes and a real multi-account/gateway journey; RC-014 still lacks a supervised real-run executor. RC-016 and downstream work remain gated by the partial RC-014; RC-021 remains gated by partial RC-012 and other incomplete prerequisites. RC-015 and real Distill execution remain gated by RC-002.
- **Decision:** No task is newly eligible. Keep the acceptance criteria unchanged, RC-012/014 partial, and RC-002 unknown/unaccepted. Do not retry or authenticate to investigate the prior Distill prompt. Resume only after authorized prerequisite evidence changes or another task's explicit Start and dependencies are met.
- **Published baseline checked:** Before this documentation update, local and remote branch head was `97ec6eb7307570da4cca184ebca987ca6125cd99`; PR #2 was open, with static/test successful on runs `36235237914` and `36235240193`.
- **Handoff publication:** Recheck records were published in commit `044548a1bb94e1e7bce5d550c928b51c07820a50` and confirmed at the remote branch and open PR #2. Static and test jobs passed in runs `36235828609` and `36235826165`. Full observations are in [`plan/checkpoint-evidence.md`](plan/checkpoint-evidence.md#backlog-eligibility-recheck--2026-09-26).
