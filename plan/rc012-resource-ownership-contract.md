# RC-012 resource ownership contract — proposal

## Status and scope

This is a design proposal for RC-012 review, not an implementation or acceptance record. It defines identity, stable resource IDs, parent relationships, and the ownership check every resource route must apply. It does not create placeholder APIs or claim that Bot, thread, file, or scheduled-task functionality exists.

RC-012 remains unaccepted until its original delivery, cross-user read/write proof for every implemented resource route, and failure condition have all been verified. A resource type with no route cannot be counted as proven by this contract.

## Identity and identifiers

- The API derives `userId` only from the validated, persisted server-side session. Request bodies, query strings, route parameters, headers, resource IDs, and parent IDs never select the principal.
- Every persisted resource has an immutable opaque UUID `id`, generated by the backend. IDs identify records; possession of an ID grants no authority.
- Ownership is derived through persisted parent relationships. Denormalized owner fields may support indexing only when database constraints and transactional writes keep them consistent with the authoritative parent; they never replace parent-owner verification.
- The existing `local` login remains a single host identity. Two-user authorization tests may seed two separately persisted sessions as fixtures to exercise backend owner enforcement; this proves route authorization, not account creation, gateway routing, or a two-account login journey.

## Ownership graph

| Resource | Authoritative parent | Required ownership rule |
| --- | --- | --- |
| Workspace | Authenticated user | `workspace.user_id = session.userId`. |
| Bot | Workspace | Its workspace must belong to `session.userId`; a Bot ID cannot authorize itself. |
| Thread | Workspace; optional Bot | Its workspace must belong to `session.userId`. If it names a Bot, that Bot must belong to the same workspace. |
| File | Workspace | Its stable file record must resolve to a workspace owned by `session.userId`. Filesystem path resolution and version/conflict behavior remain in the file feature task; path input is not an owner identity. |
| Workspace scheduled task | Workspace | Its workspace must belong to `session.userId`. This is distinct from a Bot routine. |
| Bot routine | Bot, then Workspace | Its Bot must resolve to a workspace owned by `session.userId`; the routine cannot be moved across owners by changing a supplied Bot/workspace ID. |
| Profile | Authenticated user | Profile key is the session user; callers cannot choose another profile owner. |
| Workspace history | Workspace | Both the workspace and every history row must resolve to the session owner. |

The `workspace_task` / `bot_routine` distinction follows the product backlog, which separately names workspace scheduled tasks and Bot routines. If a future API uses a shared schedule table, it must preserve these typed parent relationships and enforce the matching parent type.

## Route enforcement

- Private reads first authenticate, then query the requested resource through its authoritative parent and `session.userId` in the same database operation. A missing or foreign-owned resource returns the same not-found result (404) to avoid confirming another user's identifier.
- Creates accept a parent ID, validate that parent belongs to the session user, and insert the child in one transaction. Any owner ID supplied by the client is rejected by schema validation or ignored only if the existing API contract explicitly requires compatibility; it is never persisted as authority.
- Updates and deletes scope the mutation by the resource ID and owner chain, atomically. A zero-row mutation returns 404 and leaves all records unchanged. Child references (for example a Bot on a Thread, or a Bot on a Routine) must be checked for same-owner and same-workspace consistency before writing.
- List routes filter by the authenticated user's ownership chain. Nested lists additionally require the supplied parent to be owned; foreign and nonexistent parents both return 404.
- Anonymous access returns 401 before reading or mutating private records. Authorization failure never returns a successful empty collection for a foreign parent.
- The feature that introduces a resource route owns its endpoint schemas and domain behavior. It must import/use a reviewed owner-scoped query pattern rather than duplicate unscoped lookups. Do not add generic empty CRUD layers solely for RC-012.

## Proof required for each resource route

For every implemented read and write route for each resource type:

1. Create owner A and owner B records through the real route or fixture boundary, with independently persisted authenticated sessions.
2. Read A's resource using B's session and attempt update/delete/create-under-A-parent using B's session. Expect 404 and compare authoritative database state before/after to prove no effect.
3. Repeat with a nonexistent ID and confirm the external response does not distinguish it from a foreign-owned ID.
4. Attempt a request with an explicit foreign `userId` or parent substitution; verify the server continues to derive ownership from the session and rejects the cross-owner mutation.
5. Verify A's valid reads/writes and B's independent records still work, so isolation does not become a blanket denial.
6. Verify anonymous GET and mutation paths return 401 and create no durable records.

A resource type without shipped routes is explicitly unverified. Integration tests exercise Elysia plus SQLite and inspect persisted state; user-visible end-to-end proof is added by the task that introduces each usable resource journey. Seeded-session API tests do not substitute for gateway/two-account login proof in RC-011.

## Current source route inventory — partial RC-012 scope

Source audit of `apps/api/src/features/storage.ts` and the composed routes in `apps/api/src/app.ts` found these six persisted-resource endpoints:

| Route | Current owner enforcement | Executable proof |
| --- | --- | --- |
| `GET /api/workspaces` | Validated session; SQL filters `workspaces.user_id` to that session. | `apps/api/src/app.test.ts` owner-isolation test: A and B each list only their own workspaces. |
| `POST /api/workspaces` | Validated session supplies `user_id`; caller-provided `userId` does not select ownership. | Owner-isolation test posts with a spoofed B `userId` under A and reads the durable owner back as A. |
| `GET /api/profile` | Validated session selects the profile key. | Owner-isolation test reads B's empty/updated profile and A's profile without cross-read. |
| `PUT /api/profile` | Validated session supplies the upsert key; caller-provided `userId` does not change it. | Owner-isolation test attempts `userId: "user-b"` under A and confirms the response belongs to A; B's own update/read works. |
| `GET /api/workspaces/:workspaceId/history` | Validated session must own the parent workspace; history query filters by both session owner and workspace. Foreign and nonexistent parent IDs have the same 404 status/body. | Owner-isolation test verifies B cannot read A's history, can read B's, and compares foreign with nonexistent IDs. |
| `POST /api/workspaces/:workspaceId/history` | Insert-select writes only when the parent workspace belongs to the validated session; zero inserted rows return 404. | Owner-isolation test verifies B cannot create under A's workspace, A/B owner writes work, caller `userId` cannot override A, foreign/nonexistent responses match, and the complete history-row snapshot is unchanged after rejected writes. |

There are no Bot, thread, file, workspace-task, or Bot-routine routes in the current API feature modules. Those resource types remain unimplemented/unverified and must be added to this inventory with real owner-boundary proof when their feature routes ship. `/api/actions` and `/api/events` are authenticated host action-receipt/event plumbing, not a workspace, Bot, thread, file, workspace-task, or Bot-routine resource under RC-012's enumerated scope. The action table has no `user_id`; `GET /api/actions` reads the shared rows and `POST /api/actions` broadcasts to every currently authenticated socket (`apps/api/src/features/actions.ts`). Cross-account isolation for these host-scoped endpoints belongs to RC-011's gateway-to-container proof, whose current design remains `REVISE` and unaccepted. This classification does not prove account isolation or permit RC-012 acceptance without its required domain resources and route proofs.

## Scope boundaries and sequencing gate

- No Bot execution, thread run lifecycle, file-content API, schedule execution, account provisioning, container routing, gateway, or UI is introduced by this contract.
- RC-002 remains unknown and unaccepted; no provider search, authentication, or retry is authorized by this design.
- RC-011 retains its separate `REVISE` decision and explicit container-routing Start. This contract neither depends on nor satisfies RC-011.
- RC-012's Start (`IDs and identity defined`) has an accepted design basis in RC-004 and an authenticated persisted session in RC-010. The current implementation's workspace IDs are backend-generated UUIDs, and the current local identity is derived from the persisted session. RC-068 is now complete against existing routes, so RC-012's current direct dependencies RC-004/010/068 are accepted. This satisfies its Start/dependency gate but does not accept RC-012: Bot, thread, file, workspace-task, and Bot-routine routes and their per-route proofs are still absent.

### Backlog sequencing amendment — published and CI-verified

The present backlog creates a semantic acceptance cycle: RC-012 requires ownership proof for file, Bot, thread, and task/routine routes, while their real feature routes are introduced by RC-021/029, RC-022, RC-038/040, and RC-045, which are directly or transitively gated by RC-012. Accepting RC-012 without those routes would violate its delivery and proof; inventing placeholder CRUD would violate the feature boundaries.

The sequencing amendment is published in `plan/tasks.html` at commit `b3aaf9001c890dda0dfff26a9ada40724069def3`:

1. Added RC-068, “Prove ownership on existing resource routes,” dependent on accepted RC-004, RC-008, and RC-010. Its scope is the reviewed identity/ownership contract plus proof on existing real workspace/profile/history routes only. It explicitly does not accept RC-012 or claim absent resource categories are proven. Its proof exercises persisted A/B session fixtures through Elysia/SQLite, foreign IDs/parents, anonymous access, valid owner access, and database before/after state.
2. Replaced the final RC-012 dependency with RC-068 on RC-021, RC-028, RC-038, and RC-042. All other dependencies and Start fields are preserved. The backlog's common rule requires per-route ownership proof before acceptance of every feature task that introduces or changes a workspace, Bot, thread, file, workspace-task, or Bot-routine route. RC-060 and RC-063 retain RC-012 as a final security/hosting gate.
3. RC-012 remains open as the final cross-resource audit. Its original Delivery, Executable proof, and Failure if clauses are unchanged. Every real workspace, Bot, thread, file, workspace-task, and Bot-routine endpoint must appear in its route inventory and have verified ownership evidence; missing categories remain unverified.

RC-068 is accepted for the existing workspace, profile, and workspace-history routes only. Its full proof and limits are recorded in `plan/checkpoint-evidence.md`. RC-021 remains ineligible because RC-016, RC-018, and RC-020 are unaccepted. Other foundation work must be evaluated against its own unchanged Starts/dependencies; this amendment does not unlock them by implication.

RC-012 remains unaccepted until all real routes exist and per-route executable proof covers every required resource without changing RC-012's criteria.

**Review status:** The contract content received an independent read-only consistency review with no concrete defect. A follow-up review found the sequencing cycle. Reviewer `rc012-sequencing-independent-03` found no concrete issue in the first proposed amendment but could not inspect the exact Git diff or run checks. The current-file review `01a0de66-ff65-7342-a1eb-795fed5402ff` found no concrete defect in the corrected RC-012 dependency wording, but could not verify the exact diff or rerun checks; the parent independently checked the current graph and documentation. The amendment is published in `b3aaf9001c890dda0dfff26a9ada40724069def3`; its static and test CI jobs passed in runs `36253446459` and `36253443302`. RC-068 is accepted only for existing workspace/profile/history routes; RC-012 remains partial.

**Next step:** Re-evaluate every remaining task's Start, dependencies, executable proof, and failure conditions against current accepted evidence. RC-012 can advance only after all required routes exist and their ownership proofs are complete; the missing resource categories remain unverified.

## RC-012 concrete route and resource scope — selected and independently reviewed

This scope is a route-ownership audit and sequencing contract for the unchanged RC-012 task. It does not implement routes, change their domain behavior, or alter RC-012's original Start, Delivery / end, Executable proof, or Failure if. RC-012 is eligible now for this bounded scope-definition/review work because IDs/identity and dependencies RC-004, RC-010, and RC-068 are satisfied. It is not eligible for final acceptance until each required route family below exists and its real route proof passes.

| RC-012 resource | Route operations included in the final inventory | Owning feature task(s) that introduce the real routes | Authoritative ownership chain | Required evidence before that feature task is accepted |
| --- | --- | --- | --- | --- |
| Workspace | Existing list/create plus eventual get/open, rename, and archive operations. | RC-021 (minimal create/open), RC-028 (create/list/open/rename/archive). | Session user → workspace. | Owner A/B isolation for every shipped read/write route; foreign and nonexistent IDs indistinguishable; rejected writes leave SQLite unchanged; each owner can still perform valid operations. |
| Bot | Create, edit, hide, list, and any Bot-scoped settings, memory, skills, run, or graphical-session route. | RC-038 (identity/configuration), RC-039 (skills/memory), RC-040 (execution), RC-024 (initial authenticated screen stream/input and exclusive control), RC-041 (multiple graphical sessions). | Session user → workspace → Bot or Bot session. | Apply parent ownership to Bot/workspace or the Bot session; verify any nested Bot reference belongs to that same workspace; exercise foreign IDs in reads and mutations plus valid owner behavior. |
| Thread | Create/open/list/read and message/run/history operations whose route is keyed by a thread or returns thread-private data. | RC-022 (first thread), RC-034 (run in thread), RC-037 (query/restore history). | Session user → workspace → thread; if a Bot is attached, it must belong to the same workspace. | Cross-owner thread IDs and parent substitution fail without effect; thread messages/results/history are not returned through another user's thread; prove valid owner use and durable state. |
| File | Workspace file creation/save/read boundary in the minimal first journey, then file list/open/create/save/move operations and attachment/diff routes that expose or mutate file content. | RC-021 (first workspace file save/read), RC-029 (workspace file operations), RC-036 (attachments and changes). | Session user → workspace → stable file/attachment record; a thread attachment must also resolve to an owned thread/workspace. | Cross-owner file/attachment identifiers fail without reads or writes; rejected mutations preserve bytes/version; valid owner can read/write; path text never acts as owner identity. Path traversal, version conflicts, and content safety remain owned by the file feature's own contract. |
| Workspace scheduled task | Create/configure, list/read, pause/resume, history, and control routes for workspace agent tasks. | RC-045 (both schedule types); RC-051 only for UI, not backend route ownership. | Session user → workspace → workspace task. | Foreign task IDs and foreign workspace parents fail without effect; valid owner operations persist and remain distinct from Bot routines. |
| Bot routine | Create/configure, list/read, pause/resume, history, and control routes for Bot routines. | RC-045 (both schedule types); RC-051 only for UI, not backend route ownership. | Session user → workspace → Bot → routine. | Foreign routine/Bot/workspace substitutions fail without effect; Bot must belong to the same owned workspace; valid owner operations persist. |

Endpoint paths and HTTP methods are fixed by the owning feature ticket's reviewed API contract when those routes are implemented; this matrix fixes resource identity, operation families, owner chain, route owner, and required evidence without inventing endpoints before their domain features exist. The final RC-012 inventory records every concrete method/path, handler, authorization query/parent chain, integration test, test command/result, and relevant database before/after evidence. A route is not covered by a resource-level summary alone.

### Shared test contract and explicit exclusions

For each shipped route that reads or mutates a scoped resource, use the real Elysia handler and SQLite boundary with separately persisted A/B sessions. Match the counterexample to the route shape: for an identifier-addressed read, update, delete, or nested create, B's use of A's resource/parent returns 404 without a durable effect, and a nonexistent ID has the same external response. For a collection list, prove each session sees only its own rows and cannot mutate another owner's rows. For a root create with no existing parent ID, prove the server derives the owner from the session even when the request attempts to spoof owner fields, and inspect the durable owner; do not require a 404 for a valid create. For a session-keyed profile, prove A/B reads and updates remain separate and caller input cannot select the other profile; do not invent a profile ID. Anonymous requests return 401 before private data is returned or written. Include positive A and B operations so blanket denial cannot pass. Exercise every concrete route/operation; common data-driven helpers are allowed only if the test reports the concrete route and operation.

The existing `GET/POST /api/workspaces`, `GET/PUT /api/profile`, and `GET/POST /api/workspaces/:workspaceId/history` remain covered by accepted RC-068 evidence. Profile is supporting identity evidence, not an additional resource requirement invented for RC-012. `/api/actions` and `/api/events` remain host receipt/event plumbing and are excluded from this domain-route matrix; cross-account host routing and socket isolation are RC-011 proof, still `REVISE` and unaccepted. Any future endpoint that embeds or returns one of the listed domain resources is included regardless of its URL shape. No absent route may be represented as tested, and no placeholder CRUD endpoint may be added solely to satisfy RC-012.

### Eligibility decision

The listed resource families and feature owners make the audit scope concrete and reviewable now; the scope definition itself is the current RC-012 continuation. Route implementation remains with the owning feature task and cannot be claimed as RC-012 completion until its own Starts/dependencies are met. RC-012's final all-routes audit becomes executable only after those real features ship with per-route tests. This sequencing preserves the common backlog rule, prevents the previously identified acceptance cycle, and leaves the original RC-012 acceptance bar unchanged.

**Scope review receipt:** Initial reasoning review `01a0def7-9673-7f83-893c-ea882b83f675` found three P2 documentation defects: the matrix omitted RC-021's first file route and RC-024's initial graphical-session routes, and required an impossible ID/404 oracle for collection/root-create/session-keyed routes. Corrected the matrix and route-shape-specific test contract. Follow-up reasoning review `01a0defa-50fa-7803-b9a7-3ad549f2845a` found no concrete remaining defect and confirmed all three findings resolved, the original RC-012 acceptance criteria unchanged, and RC-002/RC-011 boundaries preserved. Reviewers could not execute Git or checks; parent-run diff, task-contract assertions, links, and whitespace checks are recorded in this checkpoint. This approval selects the audit scope; it is not product-route verification or RC-012 acceptance.
