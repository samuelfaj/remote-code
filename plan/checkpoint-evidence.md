# Checkpoint evidence

This file records local observations for the current backlog checkpoint. It distinguishes source inventory and environment probes from accepted product behavior. The raw command output was also captured in the session scratch directory during this run; this repository note preserves the observations needed for another agent to resume.

## RC-001 — current-app parity inventory

- **Result:** Accepted as inventory only, not as implementation proof.
- **Source checkout:** `../remote-code/macos-app`, branch `production`, revision `5515958f7e6cbb46bf33c16c3ff91df7e4dab021`; `git -C ../remote-code/macos-app status --porcelain` returned empty at inspection. Representative file existence and task-ID mappings were checked by script.
- **Artifact:** [`parity-inventory.md`](parity-inventory.md), linked from RC-001 in `tasks.html`.
- **Scope boundary:** The app checkout is adjacent to this planning repository, not copied into it. Reconfirm the pinned source revision before changing the mapping. No feature execution or product parity is claimed.
- **Verification:** `python3 scripts/check-doc-links.py` passed. A semantic check confirmed every referenced task ID exists, source paths exist, schedule execution/UI proofs cover both workspace tasks and Bot routines, and the explicit exclusions are present. `git diff --check` passed.

## RC-008 — durable storage foundation (accepted) — 2026-09-25

- **Status:** Accepted against the original delivery, executable-proof, and failure criteria after exact-diff independent review. The database, workspace records, history, and persistent profile records live on a named volume and restore on process start; no confirmed data loss occurred on a different image ID, and locked-database writes were not confirmed or persisted.
- **Implementation:** `apps/api/src/features/storage.ts` initializes SQLite tables for workspaces, profiles, and history on the configured database path and exposes authenticated list/create, profile read/update, and workspace history read/write routes. History inserts require ownership of the parent workspace. Readiness now checks the action, workspace, history, and profile tables. `prototype/compose.yaml` uses `/var/lib/remotecode/remotecode.sqlite` on the named `remotecode-data` volume. Local development defaults to `/tmp/remotecode.sqlite`.
- **Local checks on final source:** `bun run typecheck` passed; `bun test apps/api/src/app.test.ts` passed 16 tests/80 expectations, including domain record restoration after API recreation, missing-domain-table readiness, exclusive-lock write rejection, and recovery. Full `bun run test:e2e` passed 2 tests; the Linux-guest-only case was skipped without its separate RC-003 guest configuration. `bunx vite build --config apps/web/vite.config.ts`, `python3 scripts/check-doc-links.py`, and `git diff --check` passed.
- **Named-volume recovery proof:** Project `rc008-domain-proof-1790364667`, container ID `fef6c0b0abc33b92bc29d8c82bd746276fa8df1a25bd508655ad477dbf30ad9f`, named volume `rc008-domain-proof-1790364667_remotecode-data`, base image `sha256:13e27c5ca5b48cb8079200f1262fa48cb2160880aedb68d1372c3d74ff8641db`. On Linux amd64 emulation under a macOS arm64 host, workspace ID `6759d565-971c-481a-adb5-0d53e69a2f77` / name `named-volume workspace`, history ID `3d9cc6ae-c86b-4355-ac1a-e81e91453cb8` / content `named-volume history content`, and profile user `local` / display name `named-volume profile` were written, then the container was removed and recreated with the same volume. Readback returned the exact IDs and contents.
- **Failure proof:** With a real exclusive SQLite lock held, readiness returned 503 and liveness 200. An authenticated workspace write returned 401 because the session lookup could not read the locked database; after unlock, readiness returned 200 and the workspace list was unchanged. Thus no write was confirmed or added while persistence was unavailable. This test does not imply storage-degradation UI behavior, which remains in later tasks.
- **Image replacement proof:** Before replacement, action receipt `edb4287b-f098-4e57-a1b7-ce9a7757800e` was committed. A distinct image ID `sha256:7f3bb43cfafd88009ae2e81e9aa8bde5df577b23d344287f5980aa214ee01b68` then restored the exact workspace, history, profile, and action receipt using the same named volume. The replacement was derived from the base image by adding a label; application filesystem layers were unchanged. This establishes volume state independence from container/image identity but does not prove an application-version upgrade. Both images ran as Linux amd64 under emulation, not on a native amd64 host.
- **Browser behavior check:** Started the local API and web app with the test-only passphrase from `playwright.config.ts`, opened `http://127.0.0.1:37118` in Chrome, signed in, submitted `manual browser checkpoint verification`, and observed confirmed receipt `e3fc05fe-910e-42eb-9a4c-504dd62e743f` and the same record in action history. Signed out and observed the sign-in screen. The E2E test also sets the viewport to 390×844 and checks no horizontal overflow; desktop run uses 1280×900. No manual browser resize control was available.
- **Independent review:** The first pass was not relied upon because it did not have the exact diff/current artifacts. The follow-up reasoning-model review inspected the supplied 1,218-line source patch, acceptance wording, and both fresh proof logs. It found no concrete defect on changed lines and no demonstrated unmet RC-008 criterion. It explicitly cautioned that the replacement image changed identity/label, not application layers, so this is not an application-version migration proof. It also treated the separate Chromium user-data directory as outside RC-008's stated profile-record scope; secret exposure in the agent environment remains a separate RC-015 concern, not an RC-008 finding.
- **Historical next work (superseded by the accepted RC-010 checkpoint below):** At that point RC-010 was to continue against the persistent API substrate and remained unaccepted pending session/auth, Linux, and independent security evidence. RC-009 remains gated on RC-002 and full RC-008. RC-002's timed-out external effect remains unknown and was not retried.

## RC-010 — host authentication and private routes (accepted) — 2026-09-25

- **Acceptance contract and decision:** RC-010 is accepted against its original, unchanged delivery/proof/failure criteria: backend-validated sessions, logout and expiry; anonymous/private-route rejection; and expired sessions never accessing private data. The implementation and behavior evidence are recorded below. RC-008 is accepted and its named-volume API satisfies this task's start condition. No acceptance criterion was reduced.
- **Independent review:** Reasoning reviewer `01a0db17-6f9c-7962-a10c-061f20ca10bd` reviewed complete patch `44bf07a1b863f51a141c46900c766d0e922836fffbd5596de1a0c44d7ec79ad3`, found no concrete defect, and confirmed that task/evidence/handoff status consistently remained in progress until this clean review. Earlier implementation-only review `01a0db0e-82b3-77a0-ad0f-527ef534028c` did not cover the complete patch. The reviewer did not independently run tests or reproduce Linux results; those acceptance proofs are the recorded execution evidence below. This acceptance-record update will receive a final exact-patch review before commit/push.
- **Latest-image proof:** On Linux amd64 image `sha256:398dfc2639647b24469cf639efc73bc55db69cd139233bf574b6427103aeca54` under macOS arm64 emulation, guest Chromium via Node Playwright completed valid login, authenticated WebSocket connection, and private read (200); action receipt `8a4bbf91-d3fe-48d4-afa7-ae139581db35` was created/read; UI logout returned to sign-in and private read returned 401. On the same image and retained named volume with 2500 ms TTL, receipt `8c8a0fb3-ad8a-4bbc-b899-6f43aa8444a2` was created; private access was 200 before expiry, then session and private routes were 401 and the UI receipt was cleared. Anonymous private access was 401. Separate-peer canonical and dot-segment cleartext logins returned 403. The earlier CDP timeout was specific to invoking Playwright through Bun, whose Origin Chrome rejected; Node connected to the unchanged image. No application startup change was made.
- **Post-correction local checks:** Typecheck passed; API tests passed 20 tests/99 expectations; full E2E passed 7 with 3 configuration-dependent skips; focused expiry and WebSocket-loss E2E passed 2; Vite build, local documentation links and whitespace checks passed.
- **Environment limits:** Linux checks were amd64 emulation on macOS arm64, not native Linux. No external TLS-terminating proxy was available; HTTPS Secure-cookie behavior is unit-tested directly. No restart-recovery claim is made; the in-place Xvfb restart failed, so proof used fresh containers with the same image and named volume. The volume is retained, and proof containers are stopped.
- **Independent review findings and corrections:** Earlier review findings led to removing `X-Forwarded-Proto` trust, exact WebSocket `Origin` validation, and readiness checks for `sessions`; ignore protections and the RC-008 accepted-evidence link were restored. Follow-up review found that URL hostnames alone let an HTTP client forge `Host: localhost`, and that logout did not promptly revoke other open tabs. Login now permits cleartext only when Bun reports the actual peer address as `127.0.0.1` or `::1`; missing peer identity fails closed. Logout revokes sockets for the token with close code 4401, and the web client clears authenticated state, receipt, and private history on that close. A later review questioned host-browser login, idle-session expiry, and an in-flight write response after logout. The host-browser finding was disproved for the supported UI route: the actual host-side Playwright journey against the Linux image's published web port passed through Vite's same-origin `/api` proxy; direct HTTP access to the separately published API port correctly returned 403. For the valid findings, authenticated WebSockets now schedule closure at persisted session expiry and the UI clears private state on that close. An auth epoch guards action, list, and socket responses so a pending request from an earlier session cannot repopulate private state after logout or a subsequent login. `README.md` documents that remote TLS-terminating-proxy access remains unsupported until authenticated ingress identity exists.
- **Focused local results:** `bun run typecheck` passed. API suite passed 19 tests / 97 expectations, including forged-Host rejection, persisted sessions, logout, expiry, unavailable durable storage, Secure cookie for HTTPS, exact WebSocket origin, and automatic closure of an idle WebSocket at session expiry. Standard browser E2E passed 4 tests; the short-TTL expiry and RC-003 guest/external exchange were skipped in that run because their distinct configurations were not set. With `REMOTECODE_AUTH_SESSION_TTL_MS=2500`, the expiry UI E2E passed separately; the delayed-write-after-logout UI E2E passed in the standard run. A host-side Playwright run against the final Linux image's published web port passed the sign-in/action/mobile journey; directly calling its separately published API port returned 403, while calls through Vite's same-origin API proxy succeeded. Vite production build, documentation links, whitespace, shell/Node syntax, ignore rules, and Compose config with a disposable test-only passphrase passed. The earlier generic `bun run test` discovery issue remains fixed by targeting `apps/api/src`.
- **Final Linux API runtime proof:** Rebuilt the corrected source as Linux amd64 image `remotecode-rc010-final-checkpoint-20260925:local`, manifest digest `sha256:9730b0fd3d60db6eae4a3881d60f43284c57b6b4df7b31e7ea5d7825be9c9ca9`. Host: macOS arm64; container: Linux x86_64 under OrbStack emulation. On an isolated Docker network and temporary named volume, readiness returned 200; anonymous private route 401; a separate non-loopback container spoofing `Host: localhost` on HTTP was rejected with 403 and no `Set-Cookie`; actual loopback-peer HTTP login returned 200; valid private route 200; forced persisted-session expiry/private route 401; valid session after container recreation returned 200; logout returned 204; post-logout private route returned 401. The cookie carried HttpOnly and SameSite=Strict, without Secure on loopback HTTP. The proof script removed its container, volume, network, and cookie jar.
- **Final Linux guest browser proof:** Guest Chromium in the same image opened `http://localhost:5173/`, matching `REMOTECODE_WEB_ORIGIN`. Playwright rejected an incorrect passphrase; valid login reached “Live updates connected” over the authenticated WebSocket; creating action `guest browser 32809827-5a47-447f-860b-8adf4c68af70` produced backend receipt `b25f22d1-4eb3-40eb-9703-dada2c0c6594`; viewport 390×844 had no horizontal overflow. Logout returned to sign-in, and same-origin private `GET /api/actions` returned 401. A host-side Playwright run against the image's published web port passed login, action receipt, and mobile-width verification through Vite's same-origin proxy; direct HTTP access to its separately published API port returned 403 as intended.
- **Environment/security limits:** Secure-cookie behavior was unit-tested against a direct HTTPS request URL, not a live external TLS endpoint. No native Linux host was available; all Linux results are amd64 emulation on macOS arm64. `prototype/start.sh` runs API, web, and Distill without distinct OS identities and passes the startup environment to child processes. This is a confirmed process-environment isolation gap, not a verified agent-side secret-read exploit; RC-015 remains gated on RC-002. Do not run an agent-side exploit or claim secret isolation.
- **Follow-up review fixes — 2026-09-25:** The latest exact-diff review identified two actionable findings. When a valid `local` session logs out, auth now removes every persisted session for that user and closes all authenticated WebSockets, so simultaneous sign-ins cannot leave another tab's private UI active. A 500 ms, non-finite, or otherwise sub-second TTL now returns 503 without a cookie; invalid passwords remain 401. API regression coverage checks 500 ms/no cookie; browser E2E creates two distinct valid session tokens, signs out through the web UI, verifies both old tokens now return 401, and observes private UI cleared in the other tab. `bun run typecheck`, API tests (20 tests/99 expectations), default E2E (4 passed, 2 configuration-dependent skips), expiry E2E at TTL 2500 ms (1 passed), Vite production build, documentation links, Compose validation, shell/Node syntax, and whitespace checks passed.
- **Historical rebuilt Linux proof for these fixes:** At that checkpoint, RC-010 remained unaccepted pending complete review. Image `remotecode-rc010-reviewfix-20260925:local`, manifest `sha256:830759a01b10e055f09bf1ba2bd465728490191269d725363dc413dac69e2c69`, runs Linux amd64 under OrbStack emulation on macOS arm64. Full published-web Playwright against the image passed 4 tests with 2 configuration-dependent skips. Runtime observations: anonymous private route 401; direct published API HTTP login 403/no cookie; supported same-origin web-proxy login 200 and private session/action reads 200; persisted session after removing/recreating the container on the same named volume 200; forced persisted expiry private read 401; logout 204; post-logout private read 401. The test used image manifest digest above and disposable volume `rc010-reviewfix-data-20260925`. A separate Docker restart attempt exited and startup logs showed `Timed out waiting for X11 at :99`; therefore no restart-recovery claim is made. Container recreation with the same volume succeeded. At that historical checkpoint, independent exact-diff review remained pending and RC-010 was unaccepted; the later acceptance decision is recorded above.
- **Latest review finding and correction — 2026-09-25:** The exact-diff reviewer identified that an in-flight sign-in response could authenticate the UI after logout. `signIn` now advances/captures `authEpoch` before the request and ignores stale completions; `signOut` advances the epoch before awaiting the server. New browser regression test creates two persisted sessions, lets the newer login complete, signs out, releases the earlier successful response, and verifies the UI remains at sign-in without private receipts; explicit session checks for both tokens return 401. The next independent review found that assertions followed release without proving delivery of the held response. The test now awaits a signal emitted after `route.fulfill` completes before asserting the UI remains signed out. Focused Playwright passed 1/1 after this correction; the earlier full local E2E passes 5 with 2 configuration-dependent skips; API tests remain 20/99; separate 2500 ms expiry E2E passed 1. At that historical stage, a new exact-diff review was pending; the later clean review and acceptance are recorded above.
- **Final corrected artifact proof:** Rebuilt as `remotecode-rc010-reviewfix-final-20260925:local`, Linux amd64 manifest `sha256:408b9971ec53eba3522f5e888a28967ef464e7549a275bf440d22145babfdb99`. Published-web Playwright against this image passed 5 with 2 configuration-dependent skips. With disposable named volume `rc010-reviewfix-final-data-20260925`, runtime checks observed anonymous private route 401, direct API HTTP login 403/no cookie, same-origin login and private access 200, session after container removal/recreation with the same volume 200, forced expiry private read 401, logout 204, and post-logout private route 401. No restart-recovery behavior is claimed; a prior restart attempt on an intermediate candidate timed out waiting for Xvfb at `:99`.
- **Additional review corrections — 2026-09-25:** Review of the previous patch found that the Vite `/api` proxy could carry a non-loopback cleartext login to the API, where the peer appeared loopback; a lost logout response could leave private UI visible; and the delayed-login E2E did not wait for browser response handling. `apps/web/vite.config.ts` now rejects login requests from non-loopback socket peers before proxying. A close-code 4401 revocation now clears private state during a pending logout despite its epoch; the failure branch also clears state if the request eventually errors, while reporting that logout could not be confirmed. The E2E commits logout, holds its HTTP response, confirms WebSocket revocation clears the UI and private route returns 401, and only then aborts the held response. The delayed-login test waits for the browser-observed response, one browser task, and two animation frames before checking sign-in state and no receipt.
- **Proof for these corrections:** The local browser suite passed 7 tests with 2 configuration-dependent skips; non-loopback login E2E passed separately (1), delayed-login and lost-logout-response regressions passed (2), and short-TTL expiry passed separately with `REMOTECODE_AUTH_SESSION_TTL_MS=2500` (1). API suite passed 20 tests/99 expectations, typecheck, build and documentation checks passed. Local host loopback web-proxy login returned 200; login from a non-loopback machine interface and separate Docker peer returned 403 without a cookie. Latest Linux amd64 image `remotecode-rc010-final-20260925:local`, manifest `sha256:a3238a0c2f82f277b7564228fc3c4238b8e247651c776ffe2d1bba8a3771d9bf`, was emulated on macOS ARM. A separate peer received 403 through its web proxy. Guest Chromium logged in locally, created/read action receipt `8a9cdc2a-0d64-4e01-a4e7-7ad7175cedc7`, logged out, and received 401 on the private API route. The proof container was stopped and its test named volume retained. This is not native Linux or live external TLS proof.
- **Historical path-normalization falsification:** Following the then-latest independent finding, rebuilt the Linux amd64 image as manifest `sha256:9c07b52b0477898ed7e22bdfe239895e884403b32e932b3697addf2bc27a52ea` under macOS ARM emulation. From a separate Docker peer, `curl --path-as-is` got 403/no cookie for `/api/auth/./login`; canonical `/api/auth/login` also returned 403. Anonymous private route returned 401. Linux guest Chromium then signed in locally, created/read receipt `93e49084-c66e-4f2b-aa6f-8588fa2fd513`, signed out, and observed private route 401. Test container stopped; volume retained. At that historical checkpoint, review of the updated exact diff was pending; the later complete-patch review and acceptance are recorded above.

- **Historical additional ingress normalization correction:** At that checkpoint, a fresh review noted the middleware's raw path comparison could disagree with proxy/API normalization for a dot-segment path. The middleware now URL-decodes and POSIX-normalizes the URL path before matching the login endpoint. The new non-loopback E2E uses Node's raw HTTP request API to preserve `/api/auth/./login`; both canonical and dot-segment login returned 403 with no `Set-Cookie`. Focused E2E passed (1). At that historical checkpoint, review of the updated exact patch remained required; the later complete-patch review and acceptance are recorded above.
- **Historical latest review finding and correction:** At that stage, the reviewer found that private UI could remain visible after WebSocket disconnect or during a pending logout if revocation could not arrive. `signOut` now clears authenticated state and private data before awaiting the backend; a current-session WebSocket close also clears private state and returns to sign-in. An E2E relays the real WebSocket, disconnects it, verifies the receipt is cleared while backend session reads 200, waits for the configured 2500 ms lifetime, then verifies session 401 and that private UI remains absent. Focused test passed. At that historical checkpoint, the full suite and final Linux rebuild were still to be rerun; later full checks and image proof are recorded above.
- **Latest-image guest-browser proof — 2026-09-25:** Image `remotecode-rc010-final-20260925:local`, digest `sha256:398dfc2639647b24469cf639efc73bc55db69cd139233bf574b6427103aeca54`, ran Linux amd64 under macOS arm64 emulation. The named volume `rc010-wsloss-final-20260925` was reused after container recreation. A real guest Chromium session driven by Node Playwright logged in, connected the authenticated WebSocket, and fetched private actions (200); it wrote and read receipt `8a4bbf91-d3fe-48d4-afa7-ae139581db35`, signed out to the sign-in screen, and then the same-origin private fetch returned 401. An unauthenticated request also returned 401. A second fresh container on the same image and volume with `REMOTECODE_AUTH_SESSION_TTL_MS=2500` signed in, wrote receipt `8c8a0fb3-ad8a-4bbc-b899-6f43aa8444a2`, observed authenticated private access 200, then after expiry observed `/api/auth/session` 401 and `/api/actions` 401; the UI returned to sign-in and removed the private receipt. Separate-peer requests to the web proxy got 403 for canonical and dot-segment cleartext login. The earlier CDP timeout was caused by invoking the helper through Bun: Bun's WebSocket handshake sent `Origin: http://localhost`, which the guest Chrome rejected. A raw no-Origin handshake returned 101, and Node Playwright connected to the unchanged image. No application startup flag was added. The attempted in-place `docker start` of the earlier container again failed at Xvfb startup; proof continued by creating a fresh container with the same image and volume. All disposable proof containers were stopped; the named volume remains. Local re-verification after the browser work passed `bun run typecheck`, `bun test apps/api/src` (20 tests/99 expectations), full `bun run test:e2e -- --workers=1` (7 passed/3 configuration-dependent skips), focused 2500 ms expiry/WebSocket-loss E2E (2 passed), Vite production build, documentation links, and `git diff --check`. At this historical checkpoint, exact independent review was pending and RC-010 was unaccepted; the later accepted decision is recorded above. No commit/push had occurred at that checkpoint.
- **Independent review follow-up — 2026-09-25:** The review of frozen patch `84b85d7097d2b03c3a5f1e23a31fb909a78e44b3e16655d1cda0966fb71f891f` reported no demonstrated RC-010 runtime defect but found two P2 test-evidence gaps. The lost-logout-response E2E previously asserted privacy only while the response was still held; it now waits for browser `requestfailed` after abort and checks that the UI remains signed out, the receipt is absent, the unconfirmed-logout message appears, and private route returns 401. The pending-write-after-logout E2E now waits for `route.fulfill`, the browser response and request-finished events, and two animation frames before checking the UI remains signed out without the receipt; after signing in again it confirms the committed receipt is in history. Each focused browser test passed after its change. At that historical checkpoint, this invalidated the previous frozen-patch review and a clean review of the updated exact diff was still required; the subsequent complete-patch review and acceptance are recorded above. Post-correction verification has since passed: typecheck; API 20 tests/99 expectations; full E2E 7 passed/3 configuration-dependent skips; separate expiry and WebSocket-loss E2E 2 passed; Vite build; documentation links; and whitespace check.
- **Historical next step (superseded by the accepted checkpoint above):** At that point, freeze the updated patch and obtain another independent review before acceptance/publication. The complete-patch review and acceptance decision are now recorded above. RC-002 remains unknown and must not be retried.
- **X cadence check:** The account timeline showed the prior RemoteCode post, already recorded above as published at https://x.com/samfajreldines/status/2103575578008068405, as 29 minutes old. No new post was submitted because the user's one-hour minimum interval had not elapsed.
- **Historical next step (superseded by the accepted RC-010 checkpoint above):** At that stage, obtain fresh independent review of the complete patch before acceptance/publication. The clean complete-patch review and acceptance decision are recorded above.
## RC-002 — main Distill on Linux

- **Result:** Partially unblocked. Linux Distill v2.0.14 starts in a Debian 12 x86_64 guest under the available Docker/OrbStack emulation, and the authorized cached login reached ACP session setup. RC-002 remains blocked because the real prompt timed out before file verification and the provider effect is unknown; credentials were not inspected.
- **Environment:** macOS host; `distill --version` → `distill 2.0.14 (8e7101188723)`; Docker client/server `29.4.0/29.4.0`; cached `remotecode/computer:local` reports `linux/arm64`.
- **Action:** Ran the cached image with `--network none` and checked `command -v distill` inside it.
- **Observed at the original probe:** `Distill=unavailable`. Local Distill executables were all `macOS-aarch64` Mach-O binaries (versions 2.0.9, 2.0.13, and 2.0.14); `docker images` contained no Distill image. At that time `.sam-harness/config.yaml` set `network: false`, so no runtime or dependency was downloaded and no model was called.
- **ARM64 binary result:** After the user explicitly authorized network use, downloaded the official Distill v2.0.14 `distill-linux-aarch64` release asset; published SHA-256 `abf7e29c56c164476ea81ef6cc6a1b056620b154839c03de3db3ae9a66bb6864` matched. `file` identified an ELF 64-bit ARM aarch64 Linux executable. Mounted it read-only into `remotecode/computer:local` and ran it with networking disabled. The guest reported `aarch64`, glibc 2.36, and resolved dynamic libraries, but `distill --version` terminated with `Illegal instruction` (exit 132).
- **x86_64 runtime follow-up:** The official Distill v2.0.14 Linux x86_64 asset was checksum-verified earlier (SHA-256 `6e21372a858f7b563e038b6f0bdf3dd7da2e8d02f6cc9dfe2e9aca3b269e19a0`). The cached application image is ARM64 and did not support the first `--platform linux/amd64` attempt; executing the x86_64 binary in that ARM64 guest also lacked `/lib64/ld-linux-x86-64.so.2`. To isolate the architecture issue, pulled `debian:12-slim` after network authorization (image manifest digest `sha256:3783cc01769c7b2b1b83a5c5ad96c815348e28ed7da68e2e3687004faa906251`), mounted the binary read-only, and ran with `--network none --platform linux/amd64`. Both `distill --version` (`distill 2.0.14 (8e7101188723)`) and `distill --help` exited 0. Thus a Linux Distill runtime is available in an emulated amd64 Debian guest. The Debian image is only the runtime probe, not the planned RemoteCode guest/image; native target compatibility, model requests, and stdio-backed file-creation/interruption/restart/reconciliation are not established.
- **Authorized model access and stdio attempt:** The user authorized using the existing local Distill account and test profile without sharing secrets. Host `distill models` listed `grok-4.6`; the Linux ACP server offered the non-interactive `cached_token` method. The client received successful `initialize`, `authenticate`, and `session/new` responses with `grok-4.6` selected. It then sent a real `session/prompt` asking Distill to create `rc002-created.txt` in the mounted test workspace. The prompt RPC did not return within 360 seconds; the wrapper terminated the process, and a read-back confirmed the file was absent. The stderr capture was empty. The temporary ACP client only handled permission requests; it did not handle other agent-to-client requests, and it did not retain the incoming protocol transcript. This may explain the timeout, but the exact cause was not captured. The wrapper also failed to preserve the session ID or a usage receipt, so it is unknown whether the provider processed or billed the prompt. Do not retry until that effect is reconciled. No credential values were read or recorded.
- **Remaining proof:** After reconciling the unknown provider effect, fix the stdio test client to handle agent-to-client requests and retain a durable session/usage receipt. Then prove file creation, interruption, backend/process restart, and state/file reconciliation in the target-compatible Linux environment. RC-002 remains blocked; CLI startup, cached-token authentication, and session setup alone do not meet its acceptance criteria.

### RC-002 reconciliation check — 2026-09-25

- **Result:** The artifacts inspected for this check contained no authoritative Distill session or provider usage receipt. An earlier local `distill sessions search 'rc002-created.txt' --limit 5` returned `Total: 0`. That local result does not establish whether the provider processed or billed the prompt. The earlier ACP probe did not retain the `session/new` identifier or protocol transcript, and the prompt wrapper terminated after its 360-second timeout. The local `usage.json` belongs to this Grok harness session; it is not a Distill provider receipt and cannot establish whether the external prompt was processed or billed.
- **Follow-up local search:** Read-only searches for `rc002-created.txt`, `create a file`, `file creation`, and `Linux Distill` each returned one local session. Its ID matches this Grok harness session. Because the ACP `session/new` ID was not retained, the local result cannot be matched to or excluded from that session. The CLI help says session search covers summaries and first prompts, which explains why these terms can match this conversation. This local match is not an authoritative Distill receipt and does not resolve whether the timed-out prompt was processed or billed.
- **Local history lookup:** `distill sessions list --limit 100` exited 0 and showed one unique local session ID, matching the current Grok harness session. The `distill usage` command requires a session ID, and no ACP session ID was retained. This does not provide an authoritative record for the timed-out ACP request.
- **Console route check:** The existing Chrome profile's request to `https://console.x.ai/team/default/logs` redirected to `/welcome?path=/logs` and showed the team-creation onboarding page. No usage or request record was visible; no team was created and no form was submitted.
- **Boundary at that time:** No prompt was resent, no account login was performed, and no team was created. The effect and provider usage remain unknown. This does not satisfy RC-002's file creation, interruption, restart, or reconciliation proof, so RC-002 remains blocked. RC-004 was then gated on RC-002 under the original graph; the later pivot below changes only RC-004's non-executing contract path.
- **Follow-up local-source inventory — 2026-09-25:** Rechecked `distill sessions list --limit 100`, searched `rc002-created.txt`, inspected the retained sanitized source-check artifact, and checked existing Docker container names/status for an RC-002 or Distill test container. The CLI still exposes only the current Grok harness session; the container metadata check found no RC-002 container. No ACP request/session ID or provider usage receipt appeared in these inspected sources, so they cannot resolve the effect. The locally available xAI logs route had already redirected to team onboarding, so no authorized provider log view was available.
- **Captured-session trace check — 2026-09-25:** Searched the saved Grok session's `updates.jsonl`, `chat_history.jsonl`, `events.jsonl`, and captured terminal logs for the ACP attempt and its prompt. The retained ACP client script directory from the original run is absent. The UUIDs found in adjacent matching records are Grok session/prompt metadata or RC-003 action receipts; none is an ACP session identifier or a provider usage receipt. The saved artifacts therefore do not let the ACP request be matched to Distill history.
- **Recovered local request ID — 2026-09-25:** The saved background-task result contains the ACP client's timeout error, `Timed out waiting for Distill request 4`, from `receive_for(4, timeout=360)`. This recovers the client's local request ID, `4`, for the prompt call. The saved result contains no `session/new` response identifier or provider usage receipt. This local request ID does not establish whether Distill processed or billed the prompt and cannot by itself be matched to a provider record.
- **Resume condition:** Proceed only when an authoritative Distill session/usage receipt is available through an already authorized source. Otherwise, keep the uncertain operation unresolved; do not retry it.
- **Pivot policy — 2026-09-25 (historical assessment, superseded for RC-004 sequencing):** Added [`PIVOT.md`](../PIVOT.md) to stop repeated searches of exhausted sources and route future blockers to an equivalent authorized proof, dependency-ready independent work, or safe non-executing preparation. This policy does not weaken RC-002 acceptance, treat the Mac as Linux proof, or permit lifecycle testing while the previous provider effect is unknown. Local session history, saved traces, and the Console route had been checked. At that assessment, no independent task was eligible under the original dependency graph, so the wait condition was an authoritative correlated record. The later pivot permits RC-004's design-only contract; RC-005 is now eligible. RC-002 remains blocked.

## RC-003 — Linux GUI and remote clients

- **Result:** Blocked after a partial Linux/X11 MCP probe; this is not acceptance of the full React Native Web/Elysia/Distill journey.
- **Environment at probe time:** Cached `remotecode/computer:local` image, Debian 12, Linux arm64; `linux-use` source at `fe6b2048f71685672ceeec34a8e765540bc8ba55`, mounted read-only; `.sam-harness/config.yaml` then had `network: false`; Xvfb started on `:99` with `XDG_SESSION_TYPE=x11`. The network authority was explicitly enabled later, but this does not change the observed missing `wmctrl` or unavailable application source.
- **Action:** Sent real JSON-RPC stdio requests (`initialize`, `tools/call` for `doctor`, and `tools/call` for `list_windows`) to the mounted `linux-use/server.py` process in that Linux container.
- **Observed:** Server identified itself as `linux-use` 1.0.0. `doctor` returned `platform=linux`, `display=true`, `session_type=x11`, `xdotool=/usr/bin/xdotool`, `wmctrl=null`, and no supported native tools. `list_windows` returned `isError=true` with `Unsupported/unavailable: required system utility 'wmctrl' is not installed.` No window actions or input were attempted.
- **Known limitations:** `linux-use` documents capture, input, semantic actions, and Chrome automation as unsupported. This planning repository has no `apps/api`, `apps/web`, or container build source; the cached image has no Distill or `wmctrl`. The shared backend/client/browser journey remains unverified.
- **Verification:** `python3 scripts/check-doc-links.py`, RC-003 blocker consistency check, and `git diff --check` passed.

### RC-003 follow-up — Linux amd64 prototype (2026-09-24)

- **Historical result (superseded by the 2026-09-25 proof below):** Partial proof only; cross-browser shared-backend reconciliation was not yet proven.
- **Build/runtime:** Built `remotecode-rc003:local` for `linux/amd64` with the pinned Distill v2.0.14 binary checksum and `linux-use` revision `fe6b2048f71685672ceeec34a8e765540bc8ba55`. The isolated guest started Xvfb, Openbox, Elysia, Vite/React Native Web, and Chromium. Ports were loopback-published on the macOS host.
- **MCP/X11:** `distill mcp doctor linux-use --json` reported a healthy stdio server with protocol `2024-11-05` and 15 discovered tools. `scripts/linux-use-smoke.py` reported Linux/X11, `wmctrl=/usr/bin/wmctrl`, one visible RemoteCode Chromium window, and explicit unsupported results for `screenshot`, `left_click`, and `type`. No model prompt or credential use occurred.
- **Browser/API:** `bun run test:e2e` passed the local external-browser action receipt and mobile-width test (1 passed, the guest-only test skipped). Against the live guest (`RC003_WEB_URL=http://127.0.0.1:35173`, API `http://127.0.0.1:33100`), the external-browser action/receipt test passed. Typecheck, API tests (3 tests/7 expectations), and Vite production build passed. An initial React render defect (Eden supplies a `Date` for `createdAt`) was fixed by rendering its string representation.
- **Historical failure, resolved in the 2026-09-25 section:** The first guest/external test attempt could not connect because the host browser could not reach Chromium's loopback CDP endpoint. At that stage the reciprocal same-backend receipt proof was pending. The CDP design was revised so Playwright runs inside the guest via `docker exec`, and the final proof below completes the journey while keeping CDP loopback-only. Vite must run under Node in this image: Bun's WebSocket proxy failed with `socket.destroySoon is not a function`.
- **Historical commands:** `docker build --platform linux/amd64 -f prototype/Dockerfile -t remotecode-rc003:local .`; `RC003_WEB_URL=http://127.0.0.1:35173 RC003_API_URL=http://127.0.0.1:33100 bun run test:e2e -- --grep "external browser"`. Detailed command output was captured in this goal's private scratch directory.

### RC-003 final proof — same-backend guest/external browser exchange (2026-09-25)

- **Result:** RC-003 accepted against its current prototype acceptance contract. This proves the X11/API/web/MCP demonstration only; it does not prove Distill prompt execution on Linux (RC-002), persistent product hosting, security isolation, or unsupported capture/input capabilities.
- **Runtime:** Docker container `rc003-linux-proof-final`, image `remotecode-rc003:local`, `linux/amd64`. Host bindings were `127.0.0.1:33100 -> guest:3000` and `127.0.0.1:35173 -> guest:5173`; port 9222 was not published. The rebuilt Chromium command uses `--remote-debugging-address=127.0.0.1`. Inside the guest, `/proc/net/tcp` showed the port 9222 listener bound to `0100007F:2406`, and `http://127.0.0.1:9222/json/version` responded. A second container on the default bridge attempted to connect to the guest container IP at port 9222 and got `connect_ex: 111` (connection refused). After the guest Playwright helper disconnected, Chromium stayed running, its window remained listed by `wmctrl`, and the loopback CDP endpoint still responded.
- **Real browser roundtrip:** On the corrected image, command `RC003_GUEST_CONTAINER=rc003-linux-proof-final RC003_WEB_URL=http://127.0.0.1:35173 RC003_API_URL=http://127.0.0.1:33100 bun run test:e2e` passed all three E2E tests. The external browser created receipt `4b7382a1-7751-470c-9bcf-3a21953028e7` for action `external 577185e1-b5b9-4863-9f11-176913dc9b6f`; guest Chromium observed the same receipt ID. Guest Chromium then created receipt `59cbf5c6-7cd9-4124-9258-c208fcfefd5a` for action `linux guest c74698bf-a486-4727-bd7c-52aa6d94a81e`; the external browser observed that ID. A direct `GET /api/actions` from the host returned both records and IDs. The E2E generates new UUID-backed action descriptions each run and asserts IDs/content against backend history. The full output is preserved by the active goal session; rerun the command above to reproduce.
- **Fail-closed behavior on corrected image:** `docker exec rc003-linux-proof-final python3 scripts/linux-use-smoke.py` returned `proof_window_found=true`, Linux/X11, one window, and `unsupported_confirmed=["screenshot","left_click","type"]`. `docker exec rc003-linux-proof-final distill mcp doctor linux-use --json` reported a healthy stdio handshake, protocol `2024-11-05`, and 15 discovered tools. Each unsupported operation was invoked only after doctor reported it unsupported; the smoke verified an explicit unsupported/unavailable error and made no input action.
- **Automated checks on the corrected build:** `bun run typecheck` passed; `bun test apps/api/src/app.test.ts` passed 3 tests/7 expectations; `bunx vite build --config apps/web/vite.config.ts` passed; default `bun run test:e2e` passed 2 tests and skipped the guest-only test without guest settings; guest-configured full E2E passed all 3 tests. `python3 scripts/check-doc-links.py`, `python3 -m py_compile scripts/linux-use-smoke.py`, `sh -n scripts/start-linux-prototype.sh prototype/start.sh`, `node --check scripts/linux-guest-browser-proof.mjs`, and `git diff --check` passed. An earlier attempted nonexistent script name `bun run test:api` failed with “Script not found”; the defined API test command passed instead.
- **Review finding resolved:** Independent review found the previous launch setting used `0.0.0.0` and this evidence section was missing. The launch now sets `127.0.0.1`; the image was rebuilt, guest and external E2E rerun, listener binding inspected, a peer container's connection refused, and Chromium/CDP/window persistence checked after helper disconnect. This section records the corrected results; request a fresh review of the updated diff.
- **Initial next gate (before the plan pivot):** RC-004 was gated on RC-002 under the original dependency graph. Never retry the uncertain prompt absent authoritative reconciliation. This assessment was superseded by the later design-only RC-004 pivot; the guest container is owned by that earlier run and should not be touched unless still present and owned by the current run.

## Initial backlog gate check — original dependency graph (superseded by the pivot below)

- **Result at that point:** No implementation task was eligible under the original `Depends on` graph.
- **Method:** Parsed each `<details class="ticket">` entry in `plan/tasks.html` and extracted its `Depends on`/`Status` text across all 67 tasks.
- **Observed:** RC-001 is complete as inventory only; RC-003 is complete against its prototype acceptance contract. RC-002 remains blocked by its unreconciled timed-out prompt and unknown provider usage. Under the original plan, RC-004 also depended on RC-002.
- **Next action at that point:** Reconcile the timed-out stdio prompt without retrying it. This instruction was superseded when RC-004 was limited to a non-executing design contract based on RC-001. RC-002 remains required for every task that executes or supervises real Distill; RC-003 has separate completed evidence below.
- **Authority and publication:** The user authorized network, commit, and push; `.sam-harness/config.yaml` and `.sam-harness/DELEGATION.md` agree. Commit `7aaeae3b8e6e1cff590f072bb0d66e1702180dfb` is published on `checkpoint/rc002-linux-runtime-evidence` because direct push to protected `main` requires the `static` status check. The follow-up runtime evidence was committed as `0c1623ce17f481f800c897bdcab5e55aecb7bfbf` and pushed to the same branch. The user asked that each commit update an English pull request description edited with the humanizer skill. Pull request [#2](https://github.com/samuelfaj/remote-code/pull/2) now tracks the branch; its description records the runtime proof and remaining gaps.

## Authority and publication

- The user explicitly authorized network, commit, and push in response to separate authority questions. `.sam-harness/config.yaml` and `.sam-harness/DELEGATION.md` set all three to `true`. Earlier commit `7aaeae3b8e6e1cff590f072bb0d66e1702180dfb` is pushed to `checkpoint/rc002-linux-runtime-evidence`. This document records the follow-up x86_64 CLI-startup proof, committed as `0c1623ce17f481f800c897bdcab5e55aecb7bfbf`. No credential values were inspected or recorded. The stdio prompt may have reached the provider, but its outcome and usage are unknown.
- No product behavior milestone was verified, so no X post was made. `TWITTER.md` requires posts to describe only verified product milestones.
- Planning-page browser check: served the repository on localhost and opened `plan/tasks.html` in an owned Chrome background tab. Confirmed the rendered summary shows `1 completed (inventory only); 2 blocked`; selecting phase 00 displays four tasks, and the link to `plan/parity-inventory.md` returned HTTP 200. Searching for `RC-002` initially displayed `0 tasks` because search omitted the ticket `id`; added `ticket.id` to the search corpus. Repeated the same browser interaction and confirmed it displays exactly `1 task`, `RC-002`, “Prove main Distill on Linux.” On this follow-up, searched `RC-002` again and confirmed one result while the status summary remains `1 completed (inventory only); 2 blocked`. The latest task details are recorded in the HTML but the browser snapshot does not expose collapsed details as accessible elements. The page's complete `file://` route could not be tested because the browser tool accepts only HTTP(S).
- Raw outputs from probes were saved under the goal's private scratch directory while this goal is active; the summaries above are the durable handoff.

### Initial backlog readiness recheck — original dependency graph (superseded by the pivot below) — 2026-09-25

- **Authoritative source availability:** The connected MCP tool inventory returned `mac-use` and `tasks` tools, but no Distill session-history, usage, or receipt tool. No authoritative correlated record was available from the currently connected integrations. This inventory check did not access credentials, sign in, or repeat the prompt.
- **Result at that point:** No implementation ticket was eligible under the original graph. RC-002 was explicitly `Blocked`; RC-004 was `To do` and depended on both RC-001 and RC-002. RC-001's accepted deliverable was the inventory only, and RC-003 was complete against its separate prototype contract.
- **Check:** Re-parsed all 67 `<details class="ticket">` entries in `plan/tasks.html` and inspected candidate starts, dependencies, and statuses. Under the original RC-004 start condition (“Product flows and Distill outcome known”), RC-004 was not eligible because the prior stdio prompt's external effect remained unknown.
- **Decision at that point:** Keep RC-002 unaccepted and do not retry the uncertain prompt. This was superseded by the following pivot, which allows RC-004's non-executing design contract to use RC-001 as its prerequisite and explicitly model unproven outcomes as `unknown`. The change does not accept RC-002 or start real Distill execution. The record recheck did not establish whether the old request was processed or billed.
- **Local verification:** `python3 scripts/check-doc-links.py` and `git diff --check` passed for this checkpoint. Full command output is saved in the goal's private scratch directory.

### Plan pivot and RC-004 contract — 2026-09-25

- **Reason for resequencing:** The original dependency graph made the next contract/foundation sequence wait on RC-002 through RC-004. That wait is necessary for live Distill execution and supervision, but RC-004 is a non-executing contract definition. It can be completed from the approved inventory and documented product flows without assuming what happened in the timed-out ACP request.
- **Revised boundary:** RC-004 depends on RC-001 only. The contract explicitly represents unproven Distill outcomes as `unknown`; it does not reconcile or accept RC-002. RC-007, RC-009, and later tasks that execute or supervise Distill retain RC-002 dependencies. Review also found RC-015's security proof requires a real Distill process, so RC-002 is now an explicit RC-015 dependency rather than an assumed transitive gate.
- **Contract artifact:** [`state-ownership-contract.md`](state-ownership-contract.md) records backend/resource authority, run states, event and control ownership, and three exact transition cases. The duplicate case is injected after durable acceptance but before Distill starts; client-drop is after durable success; host restart is before a completion receipt. Each has one allowed outcome and a recovery rule.
- **Acceptance boundary:** RC-004 can be accepted only as this design contract. RC-002's Linux file creation, interruption, restart, and state/file reconciliation remain unverified. No provider prompt was sent during this pivot.

## RC-004 — state and ownership contract (design acceptance) — 2026-09-25

- **Result:** Accepted as a non-executing design contract only. The acceptance does not establish Distill's Linux execution behavior and does not accept RC-002.
- **Start:** RC-001's inventory is accepted. The product and failure flows are defined in the current plan. The open ACP prompt is recorded as unknown, not used as evidence.
- **Delivery:** [`state-ownership-contract.md`](state-ownership-contract.md) maps authority for users, workspaces, Bots, threads, runs, events, and human computer control; defines run states including `unknown`; and specifies retry/reconciliation rules.
- **Executable proof:** `python3 scripts/check-doc-links.py` passed. A focused semantic check confirmed RC-004 depends only on RC-001 and specifies three fixed-point cases: duplicate submission after durable acceptance but before Distill starts stays a single accepted run; a client reconnect after a durable success reads that success receipt; a host restart before a completion receipt yields `unknown` pending reconciliation. Output: the goal's private scratch file `rc004-contract-proof.txt`.
- **Failure condition:** Each case has one authoritative owner and outcome; clients cannot mark success without a durable backend receipt; an unknown external effect is not retried automatically. These are design rules, not runtime behavior proof.
- **Next eligible task at that checkpoint:** RC-005, “Create feature-based monorepo,” because its revised direct prerequisite RC-004 is accepted. All tasks that execute or supervise Distill continue to require RC-002 acceptance. RC-002 remains blocked; the prior prompt was not repeated.

## RC-005 — feature-based TypeScript workspaces — 2026-09-25

- **Result:** Complete for the task's monorepo and compile-contract scope. This does not establish a built or installed mobile client or client/server version compatibility at runtime.
- **Structure:** Bun workspaces contain `apps/api`, `apps/web`, `apps/mobile`, and `packages/client`. The Elysia backend remains one application organized by feature; web and mobile health callers live under feature directories. Both clients use the shared Eden client. The shared client declares its dependency on `@remotecode/api`, and the API exposes its app type through the package export. The Linux prototype Dockerfile copies the mobile workspace manifest before frozen install and its source afterward.
- **Positive check:** `bun install`; `bun run typecheck` passed for all four workspaces. `bun test apps/api/src/app.test.ts` passed (3 tests, 7 expectations). `bunx vite build --config apps/web/vite.config.ts` passed. `bun run test:e2e` passed 2 tests and skipped the guest-only test because no Linux guest was configured. The test run included the existing mobile-width rendering check; this change added no visible interface.
- **Contract mismatch proof:** Temporarily changed the API health response type from `"ready"` to `"healthy"`. `bun run typecheck` failed with TS2322 in both `apps/web/src/features/health/api.ts` and `apps/mobile/src/features/health/api.ts`. The source was restored; the clean typecheck passed afterward.
- **Linux image integration:** The first Docker build failed at `bun install --frozen-lockfile` because the Dockerfile omitted the new mobile workspace manifest. Added the manifest and source copies. `docker build --platform linux/amd64 -f prototype/Dockerfile -t remotecode-rc005-integrated:local .` then passed, producing image manifest list `sha256:2b8965fc65f5ea1826bc5898c1a1b24e1d6b39eaff42862081305bfa1859990b`. `docker run --rm --entrypoint bun remotecode-rc005-integrated:local run typecheck` passed inside the emulated amd64 Linux image. The expected platform warning notes the arm64 host; this is not native amd64 proof.
- **Failure boundary:** The shared root typecheck must fail when the API route contract changes without client updates. Both platform-specific typed call sites reported the mismatch. No Linux mobile build or installed-client compatibility test was part of RC-005.
- **Next eligible task:** RC-006, “Start Elysia API in the container.” RC-007 and real Distill execution remain gated on RC-002; RC-002's external provider effect remains unknown and no prompt was retried.

## RC-006 — Elysia lifecycle and dependency readiness — 2026-09-25

- **Result:** Accepted for the task's API lifecycle and health-check contract. This is Linux amd64 emulation on the macOS arm64 Docker host, not native amd64. It does not establish the persistence requirements of RC-008 or run Distill.
- **Essential dependency at this stage:** The API's configured SQLite database. Distill is not an API readiness dependency; its execution and supervision remain gated by RC-002. Actions are currently in-memory and are not claimed durable.
- **Implementation:** `apps/api/src/app.ts` configures `DATABASE_PATH` (default `/tmp/remotecode.sqlite`), initializes the SQLite file when possible, and gives health routes a read-only query against the essential `actions` table. `/api/health/ready` has a 500 ms route deadline; the SQLite query runs in a Worker terminated after 400 ms so a blocking synchronous SQLite call cannot stall the API event loop. A readiness failure returns HTTP 503; `/api/health/live` reports process liveness independently. The prototype startup waits for readiness. Web/mobile typed callers use the readiness route and include `not_ready` in the response type.
- **Prior attempt, not accepted:** The initial synchronous SQLite check passed 7 tests/20 expectations. Its earlier image, `sha256:a52e52ed9943719bfebe3298711139afe71e9d37c3a15ad7de982b2799e80724`, returned 503 after a 67 ms lock probe. Independent review correctly identified that this did not prove a blocking synchronous query could be interrupted; readiness was not accepted on that evidence. The probe was moved to a terminable Worker and acceptance was deferred until the final proof below.
- **Final local checks:** `bun run typecheck` passed. `bun test apps/api/src/app.test.ts` passed 7 tests and 23 expectations. One test holds SQLite `BEGIN EXCLUSIVE`, observes the API fail readiness before 500 ms with liveness still available, then uses a separate read-only query with a 550 ms busy timeout to verify the same lock continues to block SQLite for at least 500 ms. Readiness recovers after unlock. A separate never-resolving asynchronous check exercised the route's 500 ms fallback (observed about 503 ms including timer scheduling). `python3 scripts/check-doc-links.py` and `git diff --check` passed.
- **Final Linux image:** Built with `docker build --platform linux/amd64 -f prototype/Dockerfile -t remotecode-rc006-api:local .`. Image manifest list digest: `sha256:39c94314d77e7e3260dd165933015ee75c419cb424f18f18bbd850e6ef0bd415`; platform: `linux/amd64` emulated on the arm64 host.
- **Lifecycle and failure proof:** Started the API as the container's foreground Bun process using `docker run -d --platform linux/amd64 --name remotecode-rc006-linux-worker-proof-20260925 --entrypoint bun remotecode-rc006-api:local apps/api/src/index.ts`. Readiness returned HTTP 200. Inside the container, a separate Python SQLite connection set a 5,000 ms busy timeout, held `BEGIN EXCLUSIVE`, and remained alive; API readiness returned HTTP 503 in 0.417293 seconds while concurrent liveness returned HTTP 200 in 0.009226 seconds. After terminating the lock holder, readiness returned 200. `docker stop -t 5` reported `exited`; `docker start` restored readiness to 200; the container was stopped and removed afterward.
- **Failure condition checked:** A real SQLite query remained blocked beyond the readiness deadline, but the Worker-based readiness request returned within the 500 ms contract and did not block liveness. No production database, volume durability, or data recovery behavior is inferred; those remain RC-008.
- **Next eligible work:** RC-008, “Persist data outside the image,” can proceed from RC-006. RC-007 and all live Distill execution remain gated on RC-002; its provider effect is still unknown and no prompt was repeated.

## RC-002 reconciliation check — 2026-09-25

- **Result:** No new reconciliation source was checked as part of RC-006. The effect of the previous ACP prompt remains unknown. It was not repeated, and this API task does not change RC-002 acceptance.
- **Boundary:** RC-006 uses no Distill prompt, account, or credential. Continue to require an authoritative sanitized record for the earlier attempt before any retry or lifecycle test of that uncertain action.

## RC-013 shared Eden connection accepted 2026-09-26

- **Decision:** RC-013 is accepted against its original Delivery / end, Executable proof, and Failure if criteria after the shared API call was exercised through web and a minimal native iOS app, a route-field mutation was detected by typechecking, and the corrected complete change received independent reasoning review. The acceptance criteria were not reduced. Reviewer `01a0dc96-f386-7b90-8ece-a30a63f190b4` found no blocking defect and confirmed the root monorepo typecheck is the relevant shared-contract check for both consumers; the review was source/evidence limited and did not itself rerun tests or inspect Git. The final local check commands and their outputs are recorded below.
- **Eligibility:** RC-005, RC-006, and RC-010 are accepted in the task record. RC-013's start condition (exported Elysia API type) is met. RC-013 does not require live Distill execution; RC-002 remains unknown, unaccepted, and untouched.
- **Implementation:** One shared typed Eden client backs web/mobile health calls, credentials, caller headers, a 10-second deadline, cancellation propagation, and typed unknown-outcome handling. Its deadline uses `AbortController` plus a timer instead of `AbortSignal.timeout/any`, which the native runtime does not provide. Unknown outcomes are returned through `new Response(...)`, rather than `Response.json`, which is absent from the installed React Native fetch implementation. The web actions screen and a minimal Expo app display health state and expose refresh controls.
- **Direct checks:** Real Elysia integration invokes the web and mobile service modules and confirms both return `ready`; authenticated cookie forwarding succeeds and anonymous private session access returns 401. Client coverage verifies absent AbortSignal static helpers; timeout before headers, in JSON/text bodies, and while reading a stream; active and already-aborted caller cancellation; and timeout normalization with `Response.json` removed. Independent web-only and mobile-only TypeScript project checks both pass from the repository compiler settings. A temporary API contract mutation changing both ready-route response branches from `status` to `state` caused each consumer-only typecheck to fail in the shared client's typed response read; the API source was restored byte-for-byte. An earlier one-branch mutation did not fail because the response union retained a `status` field in its other branch; it is not used as acceptance evidence.
- **User-visible behavior:** The browser health journey verified ready → request unavailable → ready by clicking the page refresh control; the full E2E suite passed 8 tests and skipped 3 configuration-dependent cases. A Release iOS simulator app (`com.remotecode.mobileproof`, iOS 18.5) was rebuilt after the final client fix, installed with an embedded `main.jsbundle` (SHA-256 `633f49bbf688bbe2a0263eaad9ceb22716c8643c3458b4e743c82f1cc0930aaa`), and called the real local Elysia API. Screenshots in `{SCRATCH}` show ready, unavailable after stopping the API, and ready after restarting it. This proves the app call and recovery states, not Android or deployment.
- **Verification:** On final source, `bun run typecheck`; `bun test packages/client/src/index.test.ts` (9 tests/30 expectations); `bun test apps/api/src` (20/99); `bun run test:e2e -- --workers=1` (8 passed/3 skipped); `bunx vite build --config apps/web/vite.config.ts`; `python3 scripts/check-doc-links.py`; and `git diff --check` passed. Captured logs and three simulator screenshots are under `{SCRATCH}` (`rc013-broken-contract-both-platforms.log`, `rc013-mobile-response-fix-ready.png`, `rc013-mobile-response-fix-unavailable.png`, `rc013-mobile-response-fix-recovery.png`, plus terminal output files in the goal session).
- **Review finding and correction:** Reviewer `01a0dc90-f5a7-7ea2-9de8-cb3369c36a75` found a P1 incompatibility because React Native lacks static `Response.json`. Replaced it with standard `new Response(...)`, added a test that removes the static helper and exercises a real timeout, rebuilt the Release simulator app, and repeated the real API state journey. The follow-up review found no blocking defect. Its stated limits: it had no shell/Git tool and did not independently rerun checks; the native screenshots do not demonstrate native timeout classification or authenticated session behavior.
- **Limits:** A standalone `apps/mobile/tsconfig.json` check was attempted but is not clean because that Expo project traverses backend files and reports Bun/Elysia typing errors; the configured root monorepo typecheck is clean and includes both apps. Native timeout classification, native authenticated-session behavior, native refresh-button input, Android, deployment, and unresolved UX decisions from `sam-harness plan` were not verified. These are not claimed as completed by this task's stated proof. No X post was made; the previously stalled mac-use route was not retried. RC-002's provider effect remains unknown and no Distill action was repeated.
- **Next eligible work:** Recheck the dependency graph. RC-014 (“Deliver events with reconnection”) now has its listed prerequisites RC-004, RC-008, and RC-013 accepted; it does not authorize or require a Distill prompt. Preserve RC-002 as unaccepted and retain its gates for real Distill execution.

## RC-014 action-event recovery slice — partial, not accepted — 2026-09-26

- **Acceptance boundary:** Direct prerequisites RC-004, RC-008, and RC-013 are recorded accepted. PR #2's published RC-013 head `aba20f5996b764563b33424df8c79e1fa90b1101` was rechecked with `gh pr view 2 --json headRefOid,state,statusCheckRollup`; it is open at that SHA and both `static` and `test` checks succeeded in each of runs `36228061714` and `36228060027`. RC-002's prior Distill provider effect remains unknown and was not investigated, retried, or used. RC-014 is **not accepted**: existing action receipts are synchronous mutations, not supervised runs, and no real run executor exists to exercise the task's “disconnect during run” proof.
- **Implementation:** `actions.sequence INTEGER PRIMARY KEY AUTOINCREMENT` now provides a durable monotonic cursor. Initialization transactionally migrates the previous text-primary-key schema, preserving stored receipt IDs, descriptions, timestamps, and deterministic order. Snapshot and `action.created` messages carry cursors; snapshots return action history and its high-water cursor from the same synchronous database read. Authenticated `sync` requests re-read the snapshot. HTTP receipt responses remain unchanged. A failed event send no longer turns a committed receipt into an apparent failed write.
- **Shared-client and web behavior:** `packages/client/src/action-events.ts` validates event envelopes, replaces history from authoritative snapshots, applies only the next cursor, ignores duplicates/old cursors, and requests one snapshot on a gap. The web client removed the competing startup GET merge and waits for a valid snapshot before declaring the socket connected. It clears private UI on socket loss, requires an authenticated session check before manual reconnect, times out stalled synchronization after five seconds, rejects stale socket/HTTP callbacks, and does not resend POST during recovery. Existing logout/expiry behavior remains enforced. `plan/mockups.html`, `.sam-harness/config.yaml`, and `.sam-harness/UX_GATES.md` now describe the recovery interaction and its source contract. `sam-harness plan` was rerun; the output still lists unresolved overall adoption/design decisions, and no Harness plan was applied or UX approval claimed.
- **API/shared-client proof:** `bun run test` runs the configured API and client tests: 34 tests / 148 expectations passed. API coverage includes authenticated real WebSocket snapshot/cursor and `sync` recovery plus a legacy SQLite fixture migrated and initialized again with original and next sequence values preserved. Shared reducer tests cover authoritative replacement, sequential delivery, duplicate suppression, single repair request after a gap, snapshot recovery, and malformed input.
- **Browser proof:** `bun run test:e2e` passed 9 tests; 3 configuration-dependent tests were skipped. New Playwright tests used the real API/WebSocket through `routeWebSocket`: held a browser POST response after durable commit, suppressed its live event, disconnected and verified immediate private-state clearing, released the single response, explicitly reconnected, and observed the exact backend receipt in the authoritative snapshot. A duplicate was injected and history count stayed unchanged; exactly one POST was observed. Another test dropped an intermediate event, forwarded the next cursor, and observed snapshot gap repair. The reconnect control was checked at 390×844 with no horizontal overflow. A separate `REMOTECODE_AUTH_SESSION_TTL_MS=2500` browser run passed the disconnect/expiry/reconnect-authentication regression (1 test). Existing cross-tab logout and delayed-write/logout checks passed. The obsolete late-startup-GET test was removed because startup state now comes from the ordered WebSocket snapshot; its source of truth and relevant races are covered by reconnect and gap E2E.
- **Local commands:** `bun run typecheck`; `bun run test`; `bunx vite build --config apps/web/vite.config.ts`; `python3 scripts/check-doc-links.py`; `git diff --check`; `sam-harness pipeline . --phase static --receipt true`; full `bun run test:e2e`; and short-TTL E2E as stated above. Output files are saved in the goal scratch directory as `rc014-typecheck.log`, `rc014-unit-api-client-tests.log`, `rc014-vite-build.log`, `rc014-doc-links.log`, `rc014-diff-check.log`, `rc014-harness-static.log`, `rc014-e2e-full.log`, and `rc014-e2e-expiry.log`. This is macOS local verification; no native Linux, deployment, or production proof is claimed.
- **First full-suite findings:** The initial E2E pass exposed assertions that expected one visible label even though the accepted UI intentionally renders the receipt in both “latest” and history, plus a startup-GET race test whose premise no longer exists after adopting a single ordered snapshot channel. Updated the two duplicate-view expectations and removed that obsolete request-path test; the next full run passed. No product failure was observed in these corrected assertions.
- **Reasoning review and corrections — 2026-09-26:** Reviewer `01a0dcd8-ec92-7f22-ad91-bf560bf7ebe6` found two P2 web defects: malformed-event recovery called browser-forbidden close code 1002 without immediately clearing receipts/disabling writes; a reconnect session check invalidated by sign-out could leave reconnect progress stuck. Both malformed-message paths now clear private state, disable writes, expose reconnect and close with application code 4002; sign-out and session revocation reset reconnect progress while stale-response guards remain. Added malformed-JSON browser E2E; focused test passed. The next corrected-patch review `01a0dcdc-faa7-7c83-84fd-7bbd03ccbf42` found readiness could report healthy after legacy action cursor migration failed at startup. The readiness worker now validates the required `sequence` column. Regression fixture creates all required domain/auth tables and indexes before locking, leaving only legacy `actions`; targeted test passed. Mutation replacing cursor validation with generic table validation caused the test to fail as expected (readiness 200 instead of expected 503); source was restored and targeted test passed again. Full current-tree verification after the readiness logic change: `bun run typecheck`; `bun run test` (35 tests/150 expectations); Vite production build; `bun run test:e2e` (10 passed, 3 configuration-dependent skips); docs-link check; `git diff --check`; `sam-harness pipeline . --phase static --receipt true` (passed; receipt `.sam-harness/evidence/20260926T084445.218694000Z-pipeline-static.json`). Final source-level reasoning review `01a0dce1-32b5-73b0-b96b-a6e3990fe1b4` found no concrete defect in reviewed code/evidence and confirmed partial RC-014/unknown RC-002 boundaries. The reviewer could not access Git/shell and therefore did not independently certify the complete base-to-working-tree diff or rerun tests; local Git diff and test evidence were checked separately in this session. RC-014 remains partial/unaccepted.
- **Published source checkpoint:** Commit `7826851e6831f5b4b9e7a76102972a4173e02657` (`feat: add cursor-based action recovery`) was pushed to `origin/checkpoint/rc002-linux-runtime-evidence`; `git ls-remote` matched that SHA. PR #2 was retitled and its managed description updated to state RC-014 partial/unaccepted status, exact local proof and limitations. Both GitHub `static` and `test` jobs passed in each of runs `36230723221` and `36230720873`; the PR check rollup at that head showed all four successful.
- **Next safe work:** Recheck task dependencies after publishing. RC-014 remains in progress until genuine persisted/supervised run functionality can satisfy the original run-disconnection proof. RC-016 depends on RC-014 acceptance and is therefore not eligible yet. RC-002, RC-007, RC-009, RC-015 and other real-Distill dependencies remain gated; do not restart the uncertain prompt.


### Publication correction — static CI configuration — 2026-09-26

- **Published checkpoint:** Commit `70900248ccce91187de79a1ced6ecefaafd20212` was pushed to `checkpoint/rc002-linux-runtime-evidence`; the remote ref matched that SHA. `scripts/__pycache__/` remained excluded.
- **CI failure:** GitHub runs `36227231269` and `36227228622` both failed in the static phase before the configured documentation-link command ran. The harness rejected `.sam-harness/config.yaml` because `design.applicable: true` lacked `design.source_of_truth`; the test phase was consequently skipped. This was a configuration validation failure, not a product test failure.
- **Local correction proof:** Added a non-empty design source naming existing `plan/mockups.html` and the task interaction/failure contracts in `plan/tasks.html`, while retaining baseline profile and existing authority. The published harness command `go run github.com/samuelfaj/sam-harness/cmd/sam-harness@v0.10.0 pipeline . --phase static --receipt true` now passes locally and writes its static receipt. `sam-harness plan .` reports no repository files planned and leaves UX and other decisions unresolved; its production profile recommendation was not applied and is not treated as plan approval.
- **Review:** Retrospective reasoning review `01a0dcab-0435-7051-9a6b-20e07cf163a2` inspected the exact supplied RC-013 patch and identified one P1 in the published config: `design.applicable` lacked the required `design.source_of_truth`, preventing CI from reaching checks. It found no additional concrete product-source defects and verified the separate consumer mutation logs. The correction review `01a0dca5-c8af-7762-bdfa-770b84309cca` passed the content of the added design-source reference but could not run commands or inspect Git. The local v0.10.0 static receipt confirms the correction passes locally. Repair commit `95fdeaea110ad07df116eb3ba8221d6da0f42451` was pushed; static and test checks both passed in each of its two GitHub runs (`36227833469`, `36227831489`).
- **PR #2 and repair CI:** Description updated to cover RC-013, the repair, all verification limits, and the CI correction. Commit `95fdeaea110ad07df116eb3ba8221d6da0f42451` is the PR head; GitHub `static` and `test` checks each passed in both triggered runs: static run IDs `36227833469` and `36227831489`, test jobs in those runs completed successfully. The remote branch ref matched the commit. These CI checks validate configured static/test gates, not mobile runtime, Linux execution, deployment, or production behavior.

## RC-012 — existing persisted-resource ownership slice — partial, not accepted — 2026-09-26

- **Eligibility and scope:** RC-004 and RC-010 are accepted. The accepted persisted SQLite resource layer exists for workspaces, profiles, and workspace history. RC-012's overall delivery still includes Bot, thread, file, and task routes that do not exist, plus a complete multi-account user journey not provided by the current single-identity password login; therefore this is an implementation checkpoint, not task acceptance.
- **Implementation:** Existing workspace list/create, profile read/update, and history read/create now derive `userId` from the validated persisted request session and scope SQL by owner. History reads return 404 for a workspace owned by another user, while an owned workspace with no history returns 200 and an empty list. Logout deletes only sessions for the validated session's user and closes only that user's active event sockets.
- **Behavior proof:** Added API integration tests using two distinct persisted session identities. The tests verify workspace/profile/history isolation, no cross-user history write, cross-owner history read 404 versus owned-empty 200, and logout of A while B remains authenticated and can request a new WebSocket snapshot. A separate anonymous-path test exercises workspace GET/POST, profile GET/PUT, and history GET/POST; all return 401 and persisted workspace/profile/history counts remain zero. The sessions are seeded directly; this does not prove account creation/login or gateway routing for two real accounts.
- **Review:** First source review found two P2 issues: cross-owner history GET returned indistinguishable empty success, and anonymous storage routes were untested. Corrected history GET to establish workspace ownership and return 404, retained 200 for owned empty history, and added unauthenticated route and persistence assertions. Corrected-patch reviewer `01a0dd30-43a2-7161-a64e-35db9cafc753` found no concrete finding in the inspected source and tests. The reviewer had no shell/Git execution, so could not independently certify the complete diff or rerun commands; the review does not cover missing resource routes or a real multi-account login/gateway journey.
- **Verification (macOS local):** Targeted command `bun test apps/api/src/app.test.ts -t 'rejects anonymous workspace, profile, and history|isolates workspace, history, and profile access by persisted session identity|revokes only the logging-out user'` passed 3 tests / 35 expectations. Full post-correction command `bun run typecheck && bun run test && bunx vite build --config apps/web/vite.config.ts && bun run test:e2e && python3 scripts/check-doc-links.py && git diff --check` passed: root typecheck; API/client 38 tests / 185 expectations; Vite production build; Playwright 10 passed / 3 configuration-dependent skips; documentation links; whitespace. Full output is in this session's terminal result `call_Tn3isvxUCpxVfS59oIA2TdgS.log` and copied to `{SCRATCH}/rc012-full-verification.log` for this goal session. No deployment or post-deploy proof is claimed.
- **Publication:** Commit `ff4d77e28b1ddbd2f76ed634a8202e08ebc88b4b` (`fix: enforce ownership on persisted resource routes`) was pushed to `origin/checkpoint/rc002-linux-runtime-evidence`; `git ls-remote` returned the same SHA. PR #2 title/body were updated to describe partial RC-012 and its limits. On that exact head, GitHub runs `36235087681` and `36235089487` each completed `static` and `test` successfully (four successful jobs total). PR remained open.
- **Next task gate:** RC-011 depends on accepted RC-010, but its Start also requires container routing, which is not present in this checkout; no gateway or account-to-container routing exists. RC-012 remains unaccepted so RC-021 and dependents are gated. RC-016 depends on unaccepted RC-014. Real Distill tasks remain gated on RC-002. No additional planned task is eligible under its explicit Start and dependency criteria at this checkpoint. Continue by rechecking when prerequisites change; do not weaken criteria or retry/investigate the unknown RC-002 effect.

## RC-011 architecture review — design not approved — 2026-09-26

- **Purpose and current contract:** RC-011 depends on RC-010 and its Start says sessions and container routing must be available. Delivery requires a gateway that authenticates and forwards API/events to the correct container without passing its routing token to Distill or terminal. Executable proof uses accounts A and B; failure if A can read/act on B, access internal ports directly, or the gateway waits indefinitely for Docker. No task criterion was edited.
- **Current source boundary:** `apps/api/src/features/auth.ts` issues only the `local` identity; `apps/api/src/features/actions.ts` authenticates API/WebSocket via that local persisted cookie session; `prototype/compose.yaml` has one API/web service with host-published loopback ports. No gateway/account-container mapping exists. No source code was changed in this checkpoint.
- **Council:** Six conditional specialist seats first reviewed frozen T-001. The controller then began author cross-examination and the fresh closure/system/arbiter panel before dispatching the six required baseline seats, which later reviewed T-001 without peer output. The machine report includes all twelve seats and three verifiers and returns `REVISE`. This protocol-order deviation is recorded in `plan/council/rc011-routing/verification.md` and `council-report.json`; do not present the result as a procedure-conformant approval. The report returned `REVISE` and was validated with `python3 -B /Users/samuelfajreldines/.grok/skills/sam-council/scripts/scaffold_council_report.py finalize plan/council/rc011-routing/council-report.json --repo "$PWD"`; exact validator output: `VALID: REVISE, 1 round(s), 13 objection(s)`. Read the packet, verification record, and report for the thesis, author responses, objections and limitations. Council responses are design review only, not executable product evidence.
- **Unresolved material risks:** Original direct-internal-port and host/tenant boundary is not proven; account-to-service mapping may not satisfy task lifecycle requirements; replay and fixed audience configuration need real API verification; gateway session/socket revocation across multiple gateway instances is unspecified; deadline, cancellation, resource limits, socket-close bounds, and UX outcomes lack numeric/behavioral proof. RC-015 process isolation is not established. The verifier panel returned REVISE; no approval was reported.
- **Tool limitation:** The required first council telemetry attempt `distill subagent begin --node council-rc011-logic </dev/null` returned exit 2, `unrecognized subcommand 'begin'`. One telemetry proof gap was recorded, and later telemetry brackets were skipped. This was not a model prompt; no Distill history, credentials, or prior RC-002 request was accessed or repeated.
- **Reasoning review and correction:** Independent exact-document review `01a0dd5c-9312-7032-a9e8-d1accd9c9760` found inconsistent reviewer ownership for three objection IDs and a broken T-002 document path in the machine report. Corrected ownership to match the submitted responses and fixed all author-response paths. Follow-up review `01a0dd5d-daa4-7141-afd8-8174a438dcca` found no remaining concrete defect in those corrections. The reviewers could not independently retrieve native raw responses or execute Git/validator commands; the parent checked the corresponding council results and reran the validator. Full base-to-working-tree independent diff review is not claimed.
- **Decision / next:** Keep RC-011 unaccepted. Do not modify its task contract or implement the proposal under the current unresolved review. A later explicitly authorized council continuation must validate a revised thesis; the council procedure prohibits starting another round automatically. Re-evaluate dependencies when authoritative evidence changes. RC-012 and RC-014 remain partial, RC-016 and downstream tasks remain gated, and RC-002 remains unknown/unaccepted and untouched. No accounts, credentials, Docker fixture, deployment, or X post were created.

## Backlog eligibility recheck — 2026-09-26

- **Authoritative plan:** Re-read `README.md`, `plan/index.html`, and all 67 entries in `plan/tasks.html`; specifically checked Start, delivery, executable proof, failure condition, dependencies, and status for RC-011/012/014/015/016/017/018/019/020/021/022. Repository sources still have no managed-account gateway/container-routing implementation (`apps/` contains API, web, and mobile only; search of app source for gateway/container-routing implementation terms found no matches).
- **Current gates:** RC-011 cannot start without sessions plus container routing. RC-012 stays partial because Bot/thread/file/task resource routes and real multi-account login/gateway behavior are absent. RC-014 stays partial because the application has no supervised real-run executor to exercise its unchanged disconnect-during-run proof. RC-016 depends on RC-014; RC-017 depends on RC-016; RC-018/020 depend on RC-017; RC-021 depends on RC-012/016/018/020; later work remains downstream. RC-015 and real Distill lifecycle work remain gated by RC-002.
- **External-effect boundary:** RC-002's earlier timed-out prompt remains unknown and unaccepted. This recheck did not search provider history, authenticate, repeat the prompt, or alter any criteria.
- **Remote state:** Local branch and `origin/checkpoint/rc002-linux-runtime-evidence` both resolve to `97ec6eb7307570da4cca184ebca987ca6125cd99`; PR #2 is open at that head. Runs `36235237914` and `36235240193` each have successful `static` and `test` jobs (four successful jobs total).
- **Decision:** No new task meets its Start and dependency conditions. Keep RC-012/014 partial and the goal active. Resume when an already-authorized, authoritative prerequisite changes or when an independently eligible task appears; never convert missing or unknown evidence into acceptance.
- **Handoff publication:** Documentation-only commit `044548a1bb94e1e7bce5d550c928b51c07820a50` (`docs: record backlog eligibility recheck`) contains this recheck and the synchronized `PIVOT.md` / `CHANGELOG.md` entries. The exact source patch was independently reviewed by reasoning reviewer `01a0dd40-81de-7322-8e0f-24a3d79cd0b1`, who found no concrete defect; `python3 scripts/check-doc-links.py` and `git diff --check` passed before commit. The remote branch and PR #2 head were read back at that SHA. GitHub runs `36235828609` and `36235826165` each passed `static` and `test` (four successful checks). PR #2 remains open. Untracked `scripts/__pycache__/` was preserved and excluded.

## Publication and backlog recheck — 2026-09-26

- **Published revision:** Current repository `HEAD` is `108ca50c3adcc2fd556a46aa987fd86443ee3cf9` (`docs: record RC-011 routing review`); `git ls-remote origin refs/heads/checkpoint/rc002-linux-runtime-evidence` and `gh pr view 2` both returned this SHA. PR #2 remains open.
- **CI:** Runs `36237790347` and `36237792434`, both for head SHA `108ca50c3adcc2fd556a46aa987fd86443ee3cf9`, completed with `success`. The PR check rollup showed both `static` and `test` successful in each run (four successful job instances).
- **Backlog parse:** A corrected parser using `re.findall(r'<details class="ticket".*?</details>', text, re.S)` counted 67 task blocks and extracted IDs, titles, statuses, and dependency links. A preceding parser attempt returned zero because its pattern incorrectly expected a word boundary after the quoted class; that output was rejected and not used. Earlier scratch parser output claiming one task is also invalid. RC-011 remains `To do`, with RC-010 as its listed dependency, but its task `Start` explicitly requires container routing, not found in current app source. RC-012 and RC-014 remain partial/in progress; RC-016 remains gated on RC-014; RC-002 remains blocked because the prior provider effect is unknown. The other currently listed `To do` tasks either depend on these unaccepted tasks or have unsatisfied Starts; no task met both its Start and dependency requirements.
- **Decision:** No product source, status, dependency, or acceptance criterion changed. RC-011's reviewed proposal remains `REVISE`; no new council round or gateway implementation was started. RC-002 was not searched, authenticated against, or retried. X was not used.
- **Exact commands:** `gh run view 36237792434 --json databaseId,status,conclusion,headSha,displayTitle,url`; the same command with `36237790347`; `git rev-parse HEAD`; `git ls-remote origin refs/heads/checkpoint/rc002-linux-runtime-evidence`; `gh pr view 2 --json headRefOid,state,statusCheckRollup`; and the Python parse command recorded in this session's terminal history.
- **Next action:** Do not mark RC-011 eligible until its container-routing Start is met through a safe, reviewed design and implementation path. Do not resume RC-002-dependent work without authoritative resolution of the prior unknown effect. Continue only if a separate task's Start and dependencies become satisfied.

## Final handoff publication — 2026-09-26

- **Commit and remote:** `b65c9cea79c78ff86af52454ebc9b406000d3d51` (`docs: record RC-011 publication and CI receipt`) was pushed to `origin/checkpoint/rc002-linux-runtime-evidence`. `git rev-parse HEAD`, `git ls-remote`, and PR #2 all reported this SHA; the PR is open.
- **CI:** Runs `36238302742` and `36238304823` both completed with conclusion `success` for that exact SHA. Each contained successful `static` and `test` jobs; the PR rollup showed four successful check jobs.
- **Scope:** Three documentation files only: `CHANGELOG.md`, `PIVOT.md`, and this evidence file. The pre-existing untracked `scripts/__pycache__/` was neither staged nor altered.
- **Local verification:** `python3 scripts/check-doc-links.py` passed (`Checked local links in README.md and 3 plan pages`); `git diff --check` passed; corrected parsing confirmed 67 unique backlog task IDs. Exact output is saved at `{SCRATCH}/rc011-handoff-checkpoint-local.log`; monitor output at `{SCRATCH}/rc011-handoff-ci-b65c9cea79c78ff86af52454ebc9b406000d3d51.json`.
- **Independent review:** Reviewer `01a0dd6d-2d94-7aa3-8f83-ee996710fd5f` found no supported factual or handoff defect in inspected files. Follow-up reviewer `01a0dd71-291b-7ec3-bcff-de4a11a9784e` independently retrieved both GitHub runs and found their commit/job claims consistent, but could not certify the exact uncommitted diff or rerun local checks. Parent verification provides those separate facts; neither review is represented as full exact-diff certification.
- **Acceptance boundary:** This publication does not accept RC-011 or change any product task status. RC-011 still fails its stated Start because container routing is absent and its current design review is `REVISE`. RC-012/014 remain partial, RC-016 and downstream work remain gated, and RC-002 remains unknown/unaccepted.

### Follow-up commit verification — 2026-09-26

- **Commit:** `76bb2d33713fd7c9fc2fc96c08cb5d76c09698ee` (`docs: add final handoff evidence`) was pushed and confirmed as the open PR #2 head.
- **CI:** Runs `36238563016` and `36238566020` both completed with conclusion `success` for this SHA; both runs passed `static` and `test` (four successful job instances).
- **Review:** Reviewer `01a0dd71-291b-7ec3-bcff-de4a11a9784e` independently retrieved both runs and confirmed their job results; it found no concrete factual defect. Follow-up review `01a0dd76-20ed-72f3-9264-0ce1a58e07df` caught a present-tense claim in the changelog that made the checkpoint sound current; changed it to checkpoint-qualified past tense. The follow-up reviewer found all CI and acceptance-boundary claims consistent, but could not inspect the exact diff or run local commands. The parent independently checked scope, documentation links, whitespace, backlog count, remote refs, and PR state.

## Eligibility reassessment — 2026-09-26

- **Branch baseline:** At inspection, `HEAD` and `origin/checkpoint/rc002-linux-runtime-evidence` were `d432e7b0f8981831478df2156014cc72e209c232`. The only pre-existing worktree item was untracked `scripts/__pycache__/`; it was preserved. No source changes were made in this reassessment.
- **RC-011:** `plan/tasks.html#rc-011` requires “Sessions and container routing available” at Start; `apps/api/src/app.ts` registers health, authentication, actions, and storage only, while `apps/api/src/features/auth.ts` issues the single `local` identity. No gateway or account-to-container route was found. Its frozen council result remains `REVISE`, with 13 objections and a recorded process-order deviation (`plan/council/rc011-routing/verification.md`). The `sam-council` procedure prohibits another round without explicit same-turn authorization to validate a revised thesis; this turn did not provide that authorization. No implementation or council continuation was started.
- **RC-012 continuation:** Direct dependencies RC-004 and RC-010 are recorded accepted. The accepted state/ownership contract defines backend ownership for user, workspace, Bot/thread through their workspace, run, events, and control, but does not define file/task ownership or concrete Bot/thread/file/task route contracts. Current `apps/api/src/features/storage.ts` exposes workspace list/create, profile read/update, and workspace-history read/create only. Therefore, the current material does not identify a further bounded existing-route slice. This does not make RC-012 dependent on RC-011; it means additional resource route work needs its own concrete, reviewed resource contract. RC-012 remains partial/unaccepted.
- **Candidate check:** Exact task text confirms RC-016 depends on RC-013/014; RC-017 on RC-004/016; RC-018 on RC-006/008/013/017; RC-019 on RC-007/017; RC-020 on RC-008/017; and RC-021 on RC-008/012/013/016/018/020. RC-014 is still partial/unaccepted; RC-012 is partial/unaccepted; no candidate among RC-016–021 satisfies its dependencies. Independent repository-only reviewer `01a0dd80-e09b-7721-8678-b1a225a3106f` confirmed this assessment and distinguished the absence of a new independent task from permission to continue a concrete RC-012 slice.
- **RC-002 boundary:** The previous timed-out provider operation remains unknown and unaccepted. This assessment did not retry, authenticate, or search provider records.
- **Decision and next action:** No product implementation is justified from current contracts. Keep the backlog status and acceptance criteria unchanged. Next, either obtain the explicitly authorized council continuation for a revised RC-011 thesis, or define and review a concrete RC-012 resource/ownership contract; then re-evaluate before product changes. This is a planning-only checkpoint, not acceptance or completion. No X post was made because there is no new verified product milestone.

### Publication and CI receipt — 2026-09-26

- **Commit and remote:** Documentation-only commit `f46255ae1b731eccd33265fe237099f85577bf29` (`docs: record backlog eligibility reassessment`) was pushed to `origin/checkpoint/rc002-linux-runtime-evidence`; `git rev-parse HEAD`, `git ls-remote`, and PR #2 all returned this SHA. PR #2 remains open.
- **CI:** Runs `36239681543` and `36239683917` completed successfully for this SHA. Both `static` and `test` passed in each run (four successful jobs total). `gh pr checks 2 --json name,state,link` returned all four jobs as `SUCCESS`.
- **Review and local checks:** Initial reasoning review `01a0dd84-06ca-78d3-88a3-618e22a3e8e5` found one factual wording defect about Bot/thread ownership; it was corrected. Follow-up reasoning review `01a0dd85-41fb-79b2-9c5b-2d967c225ea5` confirmed the correction and found no other concrete factual issue, while noting it could not inspect Git or execute checks. The parent reran `python3 scripts/check-doc-links.py`, `git diff --check`, and the 67-task/67-unique-ID parser successfully; logs are in `{SCRATCH}/eligibility-reassessment-final-checks.log`. The exact staged patch was inspected before commit.
- **Scope and acceptance:** Only `CHANGELOG.md`, `PIVOT.md`, and `plan/checkpoint-evidence.md` were committed. Pre-existing untracked `scripts/__pycache__/` remained excluded. No product behavior, task status, or acceptance criteria changed; no product test is applicable to this handoff-only checkpoint. RC-002 remains unknown and untouched.

### Follow-up receipt publication and CI — 2026-09-26

- **Commit:** `c7a6e12809c2b4471417a30d67bf4949b61eb4e3` (`docs: record eligibility checkpoint receipts`) was pushed to the same branch and confirmed as the open PR #2 head. It records CI and review evidence for `f46255a`.
- **CI:** Runs `36239787567` and `36239789672` completed successfully for `c7a6e12809c2b4471417a30d67bf4949b61eb4e3`; each passed `static` and `test` (four successful jobs total). `gh pr checks 2 --json name,state,link` returned all four as `SUCCESS`.
- **Scope:** Only `CHANGELOG.md` and this evidence file were included in the follow-up; the pre-existing untracked `scripts/__pycache__/` remained excluded. This is documentation evidence only and makes no task acceptance or product behavior claim.

## RC-012 ownership contract and sequencing reassessment — 2026-09-26

- **Contract proposal:** Added [`rc012-resource-ownership-contract.md`](rc012-resource-ownership-contract.md). It defines the server-session identity source, opaque backend IDs, resource-parent ownership, equivalent 404 behavior for foreign/nonexistent private IDs, atomic no-effect mutation failures, and per-route cross-user/anonymous/positive-owner proof. It creates no routes and does not claim acceptance.
- **Independent review:** Reviewer `01a0dd91-9b66-7962-96dc-3c3d81ff1a77` found no concrete consistency defect in the contract against the current accepted ownership contract and the recorded RC-004/RC-010/RC-012 evidence. This was read-only document review; no tests or runtime behavior were independently reproduced by the reviewer.
- **Gate result:** RC-012's stated Start and direct dependencies (RC-004, RC-010) are satisfied at the design/session level; current workspace IDs are generated UUIDs and the authenticated principal comes from the persisted session. RC-012 remains partial because concrete Bot/thread/file/task routes and executable proofs are absent.
- **Sequencing finding:** Follow-up reviewer `01a0dd94-5637-73f2-9ab0-29f5072ce61f` identified a semantic acceptance cycle: RC-012 demands proofs for routes whose feature implementations are directly or transitively blocked by RC-012. It verified the relevant task graph and noted RC-021 additionally requires RC-016, RC-018, and RC-020, none accepted. No feature task was unlocked by the contract review.
- **Proposed pivot:** The contract now proposes (not applies) an authorization-foundation task RC-068 for existing real routes; per-feature owner checks before accepting each new route; and RC-012 as the final cross-resource audit with its original clauses unchanged. Replacing dependencies, adding a task, and setting a release/security gate require a separate exact backlog review. `plan/tasks.html` and task statuses are unchanged in this checkpoint.
- **Third review:** Reviewer `rc012-sequencing-independent-03` found no concrete issue in the revised proposal and confirmed RC-068 is bounded for planning only, but it could not inspect the exact Git diff or rerun checks. The proposal is not an implementation-ready/accepted ticket; a separate review must define RC-068's full acceptance contract and dependency/gate changes.
- **Current-tree checks:** The parent ran `python3 scripts/check-doc-links.py` and `git diff --check` after each documentation edit; the final run passed. A changelog-specific assertion confirmed the entry appears once under `## 2026-09-26`. No product test applies to this planning-only checkpoint.
- **Final review correction:** Reviewer `01a0dd99-0110-7e92-ba27-8d0b32e48260` found the new changelog entry under the wrong date because an attempted move had left a duplicate. The duplicate was removed; reviewer `01a0dd9b-68ac-7920-8c67-4877dcb5aa39` confirmed exactly one entry remains under `## 2026-09-26`. The final checkpoint reviewer `01a0dd9c-66d2-7a71-ae9c-502a83b56cb3` found no concrete defect in the handoff, confirmed the correction and proposal/acceptance boundaries, but could not inspect Git diff or run checks. The parent separately reran link, whitespace, date-uniqueness, task-count, and scope checks.
- **Checks and scope:** `python3 scripts/check-doc-links.py` and `git diff --check` passed after the proposal edit. This is planning-only; product tests and runtime acceptance do not apply. RC-002 remains unknown/unaccepted and untouched; RC-011 remains `REVISE`; no X post, provider access, credential change, deployment, or product-code change occurred.
- **Next:** Review and apply the task graph amendment in a separate checkpoint, then parse and verify Starts/dependencies for all tasks before choosing implementation. RC-012 is not accepted.

### RC-012 proposal publication and CI — 2026-09-26

- **Commit/remote:** Documentation checkpoint `635a79b3c4873aa1e5047abf25828ffe1e873b1a` (`docs: define RC-012 ownership sequencing proposal`) is confirmed as local and origin branch head and PR #2 head; PR #2 remains open.
- **CI:** Runs `36240995355` and `36240992600` both completed successfully for that exact SHA. Each passed `static` and `test`; `gh pr checks 2 --json name,state,link` returned all four jobs as `SUCCESS`.
- **Scope/acceptance:** Only `CHANGELOG.md`, `PIVOT.md`, `plan/checkpoint-evidence.md`, and new `plan/rc012-resource-ownership-contract.md` were committed. `scripts/__pycache__/` remains untracked and excluded. The task graph is unchanged; RC-012 remains partial/unaccepted, RC-002 remains unknown, and RC-011 remains `REVISE`.
- **Follow-up review:** Reviewer `01a0dda0-5c55-78a1-b537-4a87d7e52661` found no textual inconsistency in the receipt and confirmed job names against the workflow file. It could not independently verify live GitHub state, raw CLI output, the exact follow-up diff, or run local checks. The parent previously read back the remote head, PR, and run/job states, and reran links/whitespace for this follow-up.

## RC-012 dependency-cycle plan amendment — published — 2026-09-26

- **Plan change:** Added RC-068, “Prove ownership on existing resource routes,” in Phase 01. It is `To do`, depends on accepted RC-004/008/010, covers only actual workspace/profile/history routes, and explicitly does not accept RC-012. The common backlog rule now requires ownership proof for each feature task adding/changing a resource route before that feature task is accepted.
- **Dependency amendment:** RC-012 now also depends on RC-068; RC-021, RC-028, RC-038, and RC-042 depend on RC-068 rather than final RC-012. Their other dependencies and Start fields remain unchanged. RC-060 and RC-063 continue to depend on final RC-012. RC-012's four original Start/Delivery/Proof/Failure clauses were asserted byte-for-byte unchanged in the current file.
- **Exact parent validation:** Parsed the base and current `plan/tasks.html`: 67 → 68 unique tasks; only the five intended existing dependency lists changed; all references resolve; graph is acyclic; RC-060/063 retain RC-012. `python3 scripts/check-doc-links.py` and `git diff --check` passed.
- **Executable evidence for proposed RC-068:** `bun test apps/api/src/app.test.ts` passed 25 tests / 142 assertions on Bun 1.3.13. The real Elysia/SQLite tests include persisted A/B session identities, positive workspace/profile/history operations, foreign history read and write (404/no durable write), profile isolation, and anonymous GET/POST denial. This does not yet cover every RC-068 condition: explicit caller-supplied foreign `userId`, full foreign/nonexistent equivalence, or all possible resource-ID routes remain to be checked before RC-068 acceptance. Output captured at `{SCRATCH}/rc068-authorization-tests.log`.
- **Browser proof:** `mac-use__browser_status` timed out twice, so the connected Chrome path was unavailable. As the documented closest substitute, headless Playwright loaded the actual `plan/tasks.html`, searched and opened RC-068, exercised phase filtering, searched RC-012, and checked 1280×900 and 390×844 viewports with no horizontal overflow. The temporary Python server was stopped and port 8765 returned unreachable. The first Playwright attempt used an incorrect text locator; after correcting it to target `#rc-068`, the full interaction run passed. This is local browser proof, not shared-Chrome proof.
- **Independent review:** Reviewer `01a0de62-ad1c-73c1-9385-bce0828a665a` found no concrete defect in inspected current files and confirmed RC-060/063 and RC-021's remaining dependencies, but lacked shell/Git access. Reviewer `01a0de64-c02e-7090-9d1e-31e1ba06e4bd` found the stale RC-012 dependency sentence in the contract. The parent corrected it and reran documentation and graph checks. A read-only follow-up (`01a0de66-ff65-7342-a1eb-795fed5402ff`) found no concrete factual defect in the distinction between the satisfied Start basis and incomplete dependency gate, but could not inspect Git or rerun checks. Parent exact diff/parser checks above establish the local graph. Commit `b3aaf9001c890dda0dfff26a9ada40724069def3` published the amendment; its static and test CI jobs passed in runs `36253446459` and `36253443302`.
- **Status at amendment checkpoint:** RC-068 had not yet been accepted; RC-012/014 remained partial; RC-021 was still ineligible because RC-016/018/020 were unaccepted. RC-002 remained unknown/unaccepted and untouched; RC-011 remained `REVISE`. RC-068's later proof and acceptance are recorded in the following section.

## RC-068 — existing persisted-resource ownership proof accepted — 2026-09-26

- **Decision:** RC-068 is complete for the existing workspace, profile, and workspace-history routes. No Bot, thread, file, workspace-task, or Bot-routine route was present or claimed. RC-012 remains partial and unaccepted; its original criteria are unchanged.
- **Start/dependencies:** The published task depends on accepted RC-004, RC-008, and RC-010. Their accepted evidence predates this checkpoint. The reviewed ownership contract is `plan/rc012-resource-ownership-contract.md`; independently persisted A/B session fixtures are created in `apps/api/src/app.test.ts` and the real Elysia handlers resolve those sessions from SQLite.
- **Executable proof:** `bun test apps/api/src/app.test.ts` passed 25 tests / 160 assertions on Bun 1.3.13. The owner-isolation test exercises A and B workspace list/create, profile read/update, and history read/create. B receives the same HTTP status and body for foreign and nonexistent workspace IDs on both history GET and POST. Foreign writes return 404 and a full readback of history rows before/after is identical. Requests with `userId: "user-b"` under A's session cannot change workspace/history/profile ownership; durable `user_id` values remain `user-a`. Anonymous workspace/profile/history reads and writes return 401, and row counts remain zero.
- **Focused verification:** `bun test apps/api/src/app.test.ts -t "isolates workspace, history, and profile access by persisted session identity"` passed with 38 expectations. Full API output is captured at `{SCRATCH}/rc068-api-final.log`.
- **Other checks:** `bun run typecheck` passed. `python3 scripts/check-doc-links.py` and `git diff --check` passed. The tasks page was opened in headless Playwright and the real RC-068 search, details, phase filter, RC-012 search, and completion status were exercised; desktop 1280×900 and mobile 390×844 had no horizontal overflow. `mac-use` Chrome status had timed out earlier, so no shared-Chrome proof is claimed. The temporary server was stopped and port 8765 verified closed. Browser output: `{SCRATCH}/rc068-plan-browser.log`.
- **Independent review:** Reviewer `01a0de6f-ab94-7f11-a0e6-a1b75d1a92fa` identified two assertion gaps (checking status equality for nonexistent IDs and proving B's workspace appears after creation); both were corrected and the targeted test rerun. Final test review `01a0de72-28b9-78a2-81e1-1317e15f6f96` found no concrete defect in the current test. Checkpoint review `01a0de79-4fc5-7f13-b7ea-53c02f6cbff3` found no concrete consistency or acceptance defect across the evidence and handoff files. Neither reviewer ran tests or Git commands; runtime evidence above is from the parent. Both confirmed absent resource types were not claimed.
- **Failure conditions:** No cross-owner read or write was observed; valid owner operations succeeded; rejected writes did not change durable history; missing categories remain unverified. RC-068 therefore meets its bounded acceptance contract.
- **Publication/CI:** Commit `7bd3b82b74ed3f5e154880d08e74c7b224d59bc4` (`test: prove ownership on existing resource routes`) was pushed to `origin/checkpoint/rc002-linux-runtime-evidence` and read back as the branch and open PR #2 head. Both `static` and `test` passed on runs `36254504928` and `36254502319` (four successful jobs total). Local and remote PR state were inspected with `gh`; PR #2 remains open.
- **Next gate:** Backlog dependency scan found no newly executable downstream task. RC-011's explicit container-routing Start is absent and its design remains `REVISE`; RC-012 has accepted direct dependencies but cannot be accepted before Bot/thread/file/workspace-task/Bot-routine routes and route-level proof exist; RC-014 remains partial pending real-run lifecycle proof; RC-021 remains ineligible because RC-016/018/020 are unaccepted. RC-002 remains unknown/unaccepted and untouched.

## Backlog eligibility recheck after RC-012 receipt — 2026-09-26

- **Method:** Parsed all 68 task `<details>` records in `plan/tasks.html`; checked statuses and the exact Start/dependency statements for remaining candidates against the current plan and source inventory.
- **Result:** No new implementation task is eligible to begin under its full contract. RC-011 requires existing container routing, which is not present in this source, and its architecture remains `REVISE`. RC-012 and RC-014 remain partial. RC-007/009/015 need RC-002, whose prior external action result remains unknown; no retry or provider authentication was done. RC-016 depends on RC-014, RC-017 depends on RC-016, and RC-021 also depends on RC-016/018/020; downstream Starts therefore remain unsatisfied.
- **Checks:** Backlog parser found 68 records. `python3 scripts/check-doc-links.py` and `git diff --check` passed after this note was added. No product code or task status changed, and no acceptance criteria were changed.
- **Review/publication:** The first reasoning review found that the generic heading duplicated an older 67-task checkpoint and the changelog link resolved to the stale section. Renamed this section to a unique heading and updated the link; the documentation-link checker and anchor assertion now pass. The review also found no other concrete defect, but could not inspect the exact patch, compare HEAD, or run commands. Commit `0495cee2a5d2edd1f5463e5415786eae68180e84` is confirmed as PR #2 head; runs `36255823274` and `36255819839` both completed successfully for that exact SHA, with `static` and `test` passing in each workflow. The unique-anchor correction is local pending its own review and publication.
- **Boundary:** RC-002 remains unknown/unaccepted and untouched; RC-011 remains `REVISE`; RC-012/014 remain partial. No X post was made because this is a planning/evidence maintenance checkpoint, not a newly verified user-facing milestone.

## RC-012 route inventory refresh — partial audit — 2026-09-26

- **Scope:** After RC-068 acceptance, continued RC-012's existing-route audit without claiming final acceptance. Source review of `apps/api/src/app.ts` and `apps/api/src/features/storage.ts` identified the six workspace/profile/history methods listed in `plan/rc012-resource-ownership-contract.md#current-source-route-inventory--partial-rc-012-scope`.
- **Proof linkage:** Each listed handler's session-derived owner check is tied to the persisted A/B integration test in `apps/api/src/app.test.ts`; latest full run passed 25 tests / 160 assertions. The inventory distinguishes authenticated `/api/actions` and `/api/events` endpoints: action rows have no `user_id`, GET reads shared rows, and POST broadcasts to all authenticated sockets (`apps/api/src/features/actions.ts`). This checkpoint classifies them as host action/event plumbing, not one of RC-012's enumerated workspace/Bot/thread/file/scheduled-task resources; cross-account isolation belongs to RC-011's gateway-to-container proof. RC-011 remains `REVISE` and unaccepted, so no account-isolation proof is claimed.
- **Absent routes:** Current API features expose no Bot, thread, file, workspace-task, or Bot-routine routes. These categories remain unimplemented/unverified and are required for eventual RC-012 completion under the unchanged delivery/proof criteria.
- **Independent review:** Reviewer `01a0de80-a5cb-7c00-9fd8-8a509a1fd98a` found no concrete route-inventory mismatch or acceptance-boundary defect. It confirmed action rows are not owner-filtered and events are broadcast to authenticated sockets, so the classification remains open. Reviewer could not inspect Git diff or execute checks; parent reran the source/inventory assertions, documentation-link checker, and whitespace check. Follow-up `01a0de83-9d6d-76c1-b7c0-7762defafdf2` confirmed the factual description. Scope review `01a0de86-d84e-78d3-8b3b-d7654b18cb4b` found it supportable to assign host action/event isolation to RC-011 because RC-011 explicitly governs API/events routing and A/B host separation; this is not proof of current isolation or an exemption if these routes later carry domain resources. Neither reviewer reran commands.
- **Published checkpoint and CI receipt:** Scope clarification commit `a6dcbf3140d8e82569a377cc6fb4a6641903eca5` is confirmed as the open PR #2 head. GitHub runs `36255579869` and `36255576596` both completed successfully for that exact SHA; each workflow passed its configured `static` and `test` jobs. The PR remains open. The prior inventory-only checkpoint `86ba3c99a2940bed94874a73172cefe841f6c61d` and its successful runs `36255108565`/`36255105815` remain separately recorded; the clarification is no longer pending publication.
- **Status:** This documentation narrows the current audit surface and handoff; it does not add product routes, weaken criteria, or accept RC-012. RC-021 remains gated by RC-016/018/020; RC-014 remains partial pending real-run lifecycle proof; RC-011 remains `REVISE`; RC-002 remains unknown/unaccepted and untouched.
